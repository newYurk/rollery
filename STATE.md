# Где мы находимся — «Ролльня» (Rollery)

Обновлено 01.09.2026. Этот файл — точка входа: с него можно начать новый чат.

## ◆ КУДА МЫ ИДЁМ

**Направление задано владельцем 31.08.2026.** Мы достраиваем механику — **конструктор суши**,
который потом, уже вне шестнадцатибитного арта, можно продавать отдельно. Ролл не
раскручивается обратно: один раз свернул — всё склеилось. Поэтому попробовать, как надо
положить, чтобы вышел задуманный рисунок, в жизни негде — и человеку нужен инструмент, где это
можно проверить. Значит конструктор обязан собирать всё, что делает настоящий повар, и по всем
его правилам.

**Продукт — математика. Арт — шкура поверх неё.** Это объясняет, почему весь август уходил
в геометрию и источники: они и есть товар.

Три следствия, которые это меняет в приоритетах:

1. **Аудит честности чисел (#109) — не гигиена, а продуктовая работа.** Пока это игра, «54 %
   чисел — догадки без пометки» терпимо. Как только мы говорим «здесь так, как делают повара»,
   каждое непомеченное число становится обещанием, которого мы не давали.
2. **Урамаки (#3) — не «известная проблема», а блокер.** В каталоге прямым текстом:
   «главное свойство не смоделировано, считается как футомаки, это заглушка». Конструктор суши,
   не умеющий урамаки, — не конструктор суши. То же с потерей 7 % площади риса (#32) и с нори,
   которая внутри в 7,5 раза толще, чем снаружи (#109).
3. **Обратный поиск (#80) — то, ради чего игру откроют, а не побочный опыт.** Он буквально и отвечает на
   вопрос, с которого начинается всё направление: как класть, чтобы получить задуманный
   рисунок. Сейчас лежит сбоку.

## ▶ С ЧЕГО ПРОДОЛЖИТЬ

> **Состояние на конец 01.09.2026.** 34 коммита, закрыто 12 задач, открытых 60 (в фундаменте 29).
> Всё запушено, оба сторожа прогнаны, рабочее дерево чистое.

### Что закрыто 01.09 — и почему это важно

| | |
|---|---|
| **#98** | **начинки в центре.** Ядром ролла стали начинки, весь рис ушёл в стенку. Решение владельца: «начинки выедут в центр — это же по-японски». Ядро 0,511 R при источниковых 0,48–0,54 |
| **#115** | нори вокруг куска — оболочка по форме куска (решение владельца: «одно тело»), а не четыре несходящиеся плашки |
| **#130** | нори между слоями риса. Владелец увидела хвост на срезе; путь замера 40 → 12 → 8 → 2 луча из 144 |
| **#121** | контуры среза больше не рвутся: обход ячеек вместо опроса лучами |
| **#116** | футомаки режется на 8, хосомаки на 6 — как в источниках |
| **#114** | сторож впервые заходит в `?flat` |
| **#126** | четверть круга (いちょう切り) заведена, список форм под сторожем |
| **#119**, **#120**, **#122**, **#123**, **#127** | дубли констант, инструмент наклона, история из доков и HTML, площадь по телу |

### Первые точки калибровки — главное за день

Числа нашлись **не на фотографиях, а в паспортах суши-машин**: SUZUMO, 株式会社トップ и AUTEC
дают один ряд сечений до миллиметра, а トップ — вес риса на класс. Модель попала в коридоры:
доля начинки 27,5 % при 23–29, √доли 0,524 при 0,48–0,54.

⚠ **Урок, который дороже чисел:** прежде чем записать величину в «искать негде», проверить,
не нормирует ли её кто-то **по работе** — производитель, регламент, диетология, патент.

### Что ждёт решения владельца

- **#131 — что считается настоящим маки: машинный ролл или ручной?** Мы берём РАЗМЕРЫ у машин,
  а МЕХАНИКУ у поваров, и требуем от модели сойтись с обоими. Пока вопрос открыт, доведение
  диаметра до промышленного — подгонка под цель, которую не выбирали.
- **#125** — фаска: обнулена по справочнику (углы бруска прямые), но путь «скругление в намотке»
  остаётся открытым.

### ✅ Кольцо влито 01.09 (#132) — модель больше не строит невозможный ролл

Спираль давала обхват 212,7 мм при листе нори 210: **отрицательный нахлёст**, то есть ролл,
который нельзя обернуть его же листом. Проверено по граммам — так было при любом весе риса.

Теперь рис лежит **одним кольцом** вокруг начинок, нори обходит его снаружи один раз плюс
нахлёст. «Один оборот» источников встал на место: он про нори, а не про рис.

| | было | стало |
|---|---|---|
| футомаки ⌀ | 67,7 | **61,7** |
| хосомаки ⌀ | 34,4 | **33,4** (замер рулеткой 25–30) |
| нахлёст футомаки | −2,7 | **+15,8** |
| сторож практики | 18 сошлось, 1 провал | **19 сошлось, 0 провалов** |

### Что делать дальше по модели

1. **#132 — перевести намотку со спирали на кольцо.** Веер 01.09 доказал: нынешний ролл
   **невозможно обернуть** — обхват 212,7 мм при листе 210, нахлёст отрицательный. Формула
   кольца по средней линии (`a₀ = L/4 + t`, ядро `(L/4−t)/(L/4+t)`) воспроизводит весь
   промышленный ряд SUZUMO из его же паспортов, а ядро и доля начинки **выпадают сами**.
   Сторож это уже ловит красным.
2. **#129** — уплотнение риса введено (1,19), но диаметр не поддался: ядро раздувается обратно.
3. **#19** — форма живёт только в отрисовке; перенести её в модель.

### Документы — где читать

| | ссылка |
|---|---|
| документ ядра | https://claude.ai/code/artifact/6a91264c-3dce-4ed9-ae23-e338a45f393a |
| кинематика | https://claude.ai/code/artifact/86c31399-17b6-44b6-b74d-9bc9a2dfd7cf |

⚠ **Это ВРЕМЕННЫЕ ссылки, опубликованные 01.09 вечером.** Прежний артефакт документа ядра
(`5bf8feb0-…`) остался в другой организации владельца, и из этой сессии до него не дотянуться
— ни обновить, ни прочитать. Когда владелец переключится обратно, обновить прежний по его
URL и эти два снять, чтобы не расходились.

Источник обоих — файлы в репозитории, они всегда свежее ссылок:
`docs/reports/piece-body.html` и `docs/reports/kinematics.html`.

### Документы

01.09 документ ядра починен в семи местах (Ф-1 перестроена из справочника нарезки, углы бруска
стали прямыми, полумесяц получил плоские грани, добавлены приёмы нарезки рыбы, числа приведены
к сегодняшней модели, вёрстка выровнена). Артефакт обновлён по прежней ссылке.

**Аудит обоих HTML веером проведён** — результат в `docs/docs-audit-0109.md`. Четыре прохода:
противоречия между документами, документы против кода, ядро против источников, кинематика
против источников.

⚠ **Скептик не отработал** (лимит сессии), поэтому список НЕ просеян: там вперемешку настоящие
находки, придирки и ошибки самих агентов. Каждую проверять перед тем, как чинить.

**Семь находок уже исправлены** — все оказались моими ошибками того же дня: правка модели
сделана, а проза не обновлена. Главная: кинематика полдня писала «#98 правка не начата»,
ссылаясь при этом на таблицу Т-3 документа ядра, где та же строка стоит «✓ исправлено».

Остальные находки аудита — работа на следующий заход, начиная с раздела «Проход: против кода».

**Мандат владельца 31.08:** она подключается руками, когда (1) готово **ЯДРО** — математика,
она и есть продукт; (2) открытых задач **10–20** (на 01.09 вечер — **64**; 31.08 утром было 86, вечером 53, и счёт вырос,
потому что вечерняя сверка нашла больше, чем починила — это честный рост, а не откат);
(3) **оба HTML** в идеальном порядке. Порядок именно такой: **сначала ядро, счётчик ужимается
следом.** Закрывать задачи ради счётчика, не двигая модель, — обман этого мандата.

### Milestone: что где лежит (перестроено 31.08)

Прежде «Полнота модели» держала 34 задачи из 53 — это была не milestone, а свалка, и по ней не
было видно, где ядро. Разделено по вопросу «что эта задача меняет»:

| Веха | Открытых | Что это |
|---|---|---|
| **Полнота модели** | 27 | Ядро: что модель ГОВОРИТ. Подворот, карта высот, формы начинок, урамаки, правила скрутки. Это и есть продукт. |
| **Оболочка и режимы** | 14 | Что игрок ДЕЛАЕТ: кисть, пазл целиком, обратный поиск, интерфейс, подсказки. Пазл сюда входит весь — отдельная milestone ему не нужна. |
| **Инженерия и регрессия** | 11 | Не что модель говорит, а насколько безопасно её менять: выход из монолита, фикстуры, сторожа, аудит чисел, производительность. |
| **Референс sim** | 6 | Лаборатория. Не в работе, 12–20 дней. |
| **Смысл и мир** | 4 | Зачем вообще крутить второй ролл, имя, ёкаи, замеры на людях. |

Без milestone не осталось ни одной задачи — ни открытой, ни закрытой. Восемь закрытых висели
без milestone с тех пор, как milestone вообще завели; разложены 31.08.

### План на следующий заход, по порядку

*(Сверяется с гитом и с доской, а не с памятью: по этому разделу начинают ориентироваться.)*

**A. Кластеры — СДЕЛАНО, осталось два хвоста.**
Свёрнуто 31.08: урамаки (#25–#29 → #3), симуляция (восемь → #60), пазл (семь → #112),
карта высот (#31, #32 → #1), кромки (#33, #20 → #24), краска (#47 → #17). Закрыто как
отпавшее: #2, #5, #63, #64, #87, #104, #106, #11. Итог дня: 86 → 53.

Осталось проверить по коду, не отпали ли после правки подворота и площади ядра:
**#6, #15, #16, #55, #56, #57** — кластер «скрутка». Их описания писались до 31.08.

**B. Ядро — что осталось на самом деле.**
1. **#98 правила скрутки по типам** — фундамент, и он же следующий по очереди.
   **Замер 31.08 сделан, развилка названа** (см. комментарий в задаче): у повара центр ролла —
   САМИ НАЧИНКИ, рис оборачивается вокруг них; у нас центр — ближняя часть листа, и кусок из
   середины постели попадает на 0,68 R вместо центра. Малой правкой не обойтись: вернуть
   «сгиб до конца кластера» — ролл перестаёт замыкаться (посчитано).
   ⚠ **Это не рефакторинг, а смена модели**, и у неё есть своя развилка, которую источники не
   решают: если центр задаёт кластер начинок, то **пустой лист** заворачивать не вокруг чего —
   он даёт спираль в 2,1 оборота. Пустой маки в жизни не существует, но на нём стоят все наши
   эталонные диаметры. Решить это надо ДО переписывания, иначе поедут REF и слепок разом.
2. **#3 урамаки** — блокер: не замыкается на рыхлой руке (0,83–0,99 витка). Правки 31.08 его
   не вылечили; порядок работ записан внутри задачи, и первым там идёт «топология намотки».
3. **#74 три класса геометрии** — тело / мембрана / масса. Прямоугольный патч описывает
   только первое; салат-лист и цуна-майо он изображает враньём. Разбор — `docs/ingredients.md`.
4. **#10 профиль ВДОЛЬ оси** — то, ради чего задача заводилась. Все куски сейчас призмы,
   поэтому невозможны свёрнутая креветка, конус клубники, долька с сужением.
   ⚠ Входит в тело **и в `patchSRange`** разом, иначе домен и отрисовка разойдутся.
5. **#110 кисть** — после того, как ядро перестанет ехать. Дешёвая страховка, которую стоит
   сделать первой: у ссылки-пазла нет поля версии.

**Что из ядра ЗАКРЫТО 31.08** (чтобы не искать заново): подворот решается уравнением (#113),
площадь ядра считается по веществу (#1), шов больше не рвёт лист (#11 — скачок 14,6 мм → 0,01),
формы начинок приведены к источникам, заведён канон футомаки, лист рисуется в честной пропорции.

**B-бис. Пазл сейчас НЕ делаем — решение владельца 31.08.**
Игровую логику пазла не пишем, ошибки в ней не чиним. Проверено по коду: **ядро он не
блокирует.** Пазл только ЧИТАЕТ модель и навязывает ограничения себе
(`play/modes/puzzle.js`: `S.turns = lv.turns`, `p.dv = 0.22` у повёрнутых, `u ≤ 0,92`,
`wave` исключён из поворота) — обратно в модель ничего не течёт.

Отдельная milestone пазлу НЕ нужна: он весь в «Оболочке и режимах» (#112, #38, #83, #85, #80,
плюс #110 и #34).

⚠ **Одно исключение, которое всё-таки про ядро: #83.** `sheetLen()` имеет ДВЕ ветки — обычный
режим берёт физический лист (`sheetCm`), пазл выводит длину из числа витков уровня. Это второе
определение длины листа, та же болезнь, что лечили у `cutSpan` и у свёртки карты. Сам пазл
трогать не надо: ветка включается только когда `S.turns` задан.

**C. Два HTML — до идеала.**
Сторож теперь сверяет только БЛОК ФОРМЫ (`⟦ФОРМА · ЕДИНОЕ ОПРЕДЕЛЕНИЕ⟧`, 105 строк).
Текст вокруг него не проверяет никто — там и искать. Пройти оба документа подряд и сверить
с кодом каждое число: витки, диаметры, некруглость, размеры начинок, число обёрток (пять),
классов нарезки (восемь), профилей (шесть). Проверить мёртвые упоминания снятого: бисквит,
рулет, блин, шоколад, крем, перец, сыр, помидор, ветчина, салат, укроп.

⚠ **ГЕОМЕТРИЯ НЕ ЗАМЕНЯЕТ ИСТОЧНИК** — правило записано в шапке `play/test/practice.js`.
Геометрия говорит, что ВОЗМОЖНО; источник говорит, что ДЕЛАЮТ. Заводя константу или формулу,
сначала спросить «а как это делают повара», и только потом считать. 31.08 на этом попались:
потолок «сгиб не шире ролла» был выведен безупречно и чинил видимую вещь, но возвращал
спираль, которой у поваров не бывает. Снят в тот же час.

⚠ **ПРАВИЛО, РОДИВШЕЕСЯ 31.08 ИЗ ДЕФЕКТА: у каждого кадра в отчёте подписывать, из какого
прогона и какой даты он взят.** Отчёт по кинематике полгода показывал кадры от 26.08 06:41
под заголовком «четыре фазы, как у повара», хотя кинематику переписали в тот же день в 13:53.
Кадры честно показывали, что делал прибор, — но читались как описание нормы, и заметить это
было нельзя: происхождение кадра нигде не стояло.

⚠ **И более общее: документы правились ДОПИСЫВАНИЕМ, а не переписыванием.** Каждая поправка
добавляла раздел снизу, ничего не заменяя сверху. Так у документа накопилась верная нижняя
половина при неверной верхней — а читатель видит сначала верхнюю. Сторожа это не ловят: они
сверяют формулу, а не картинки и не текст вокруг. **Правя документ, править то место, где
ошибка, а не приписывать поправку ниже.**

⚠ **Три грабли воркфлоу, все проверены падением.** Собраны здесь, чтобы не наступать заново.

1. **Ключи схемы — латиницей.** 31.08 упало мгновенно: JSON Schema требует имена полей по
   `^[a-zA-Z0-9_.-]{1,64}$`, кириллические ключи отвергаются на входе, семь агентов не стартовали.
2. **Обратные кавычки в тексте задания ломают скрипт.** 01.09: в шаблонной строке промпта
   стояло имя переменной в обратных кавычках — она закрыла строку, и парсер упал на середине
   русского слова. Если промпт длинный и с кодом внутри — собирать его массивом строк и
   `.join('\n')`, а не шаблоном. Тогда кавычки внутри безопасны.
3. **Имена переменных в bash — тоже латиницей**, и многобайтовый символ вплотную к `$имя`
   съедает первый байт: `«$stamp»` даёт «unbound variable». Фигурные скобки обязательны.

### 1. #110 «раскладка кистью» · Фундамент

**Решение владельца 31.08:** игрок не раскладывает куски поштучно. Он выбирает начинку и ведёт
штрих; куски ложатся вдоль него, ориентацию задаёт направление. **Как разложить — решает игра.**

Карточка заведена именно затем, чтобы не построили другое: контракт до сегодняшнего вечера
говорил «перемещать · поворачивать · удалять», и следующий, кто взялся бы за раскладку, сделал
бы drag&drop. Контракт переписан (§5.3, §5.4, §8, §10), документ ядра Ф-9 переписан.

**Приятное:** менять геометрию почти не нужно. Прямой штрих — это ровно один патч: центр в
середине, `rot` по углу, `dv` по длине. **Модель уже описывает мазки** — их просто кладут по
одному. Есть и готовые образцы: `wrapInNoriList` (один жест → четыре патча с правильным
переводом в доли) и жест скрутки циновки (сэмплирование, медиана вместо среднего).

**Что придётся добавить:** группа `p.grp` (иначе `restack` построит из штампов лестницу, и
группа обязана войти в `patchKey`) · `dv` уезжает из каталога в штрих · сущность «мазок» в слое
рецепта плюс детерминированный компилятор в физических координатах · `phase` от seed штриха.

⚠ **Обязательная часть, без которой кисть неиграбельна:** чем игрок исправляет неудачный штрих.
Единица отмены должна стать штрихом, а не последним куском.

⚠ **Дешёвая страховка, которую стоит сделать первой:** у ссылки-пазла нет поля версии. Добавить
его сейчас, до появления мазков, — один коммит; потом старая вкладка будет отдавать `null`.

### 2. #10 «разобрать каждую начинку» · Фундамент

Огурец стал сектором по источнику (14 × 9,9 мм, площадь 3,08 ед² против 2,14 у прежнего бруска).
Кисть делает #10 несущей: «положить кусок вдоль штриха» осмысленно только если у куска объявлена
своя ось, а у несимметричного — ведущая сторона. Игрок больше не крутит, значит решает игра.

Осталось: профиль вдоль куска — ⚠ обязан войти в тело **и в `patchSRange`** разом.

### 3. #109 «аудит честности чисел» · Уборка, но важная

Восемь неверных пометок исправлено, две структурные вещи сделаны (нори-условность объявлена и
проверяется, сторож больше не пропускает класс целиком). Осталось **~65 догадок без пометки** —
числа менять не надо, нужна только честная пометка статуса. Список в `docs/number-audit.md`.
Работа механическая и безопасная; хорошо идёт под уставшую голову.

### 4. #98 «правила скрутки по типам» · Фундамент · **ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА**

Разобран до формулы 01.09: ядро на 100 % рис, подворот кончается за 1,7 мм до первой начинки,
причина — площадь ядра засчитывает подвёрнутый рис содержимым, хотя он стенка трубки. Расчёт
исправленной структуры сходится: ядро 0,450 R, витков 1,448, ⌀ 63,7 мм (квадратом 50,0 —
это 極太巻, тоже промышленный класс). Числа и план правки — в комментарии к задаче.

**Вопрос владельцу:** если начинки уезжают в центр, что рисует узор на срезе? Мой ответ —
узор становится правильным (порядок вдоль листа задаёт порядок в ядре, это и есть кадзаримаки),
но смотреть надо глазами на готовый срез. Правка трогает `wind`, `materialAt` и все слепки,
поэтому делается одним заходом, а не по частям.

Другой слой: #105 про отдельный КУСОК, #98 про поведение ЛИСТА. Разблокирует #97 (острый
угол в носу подворота) и даёт форму закона для деформации. Опирается на закрытый #91:
прогоны лаборатории впервые проходят порог разрешения, значит числам можно верить, а все
прежние выводы о скрутке сняты при ppc ≤ 1,9 и недостоверны.

Ключ к разбору — формулировка владельца: **это не три отдельных типа, а один континуум по
количеству положенного сверху**. Тонкий слой → спираль с кольцами; толстый → один оборот со стыком;
слишком толстый → не замкнётся.

### 5. Дальше по очереди

- **#15 + #74** — деформация и классы материалов: §5.9 контракта отсутствует в модели
  целиком, кусок едет по витку жёстким телом. Самая большая дыра модели, но после #105.
- **#17 + #47** — рис как материал рисования: принципиальный вопрос закрыт (рисуем и формой,
  и цветом), осталась механика ввода.
- **#72** — следующий шаг миграции, границы записаны заранее: перевести ТОЛЬКО read-only путь
  `materialMap`/`similarity`, не трогая GRAIN_C, touchModel, UI, пазл и модель рецепта.
- **#103** — брейншторм «зачем крутить второй ролл» (milestone «Смысл и мир»): порядок там свой —
  сперва замеры на живых людях (#79), потом выбор цели, потом мир.

### Что НЕ трогать сейчас

Полировку вида листа (до #105) · пазл и режимы (milestone «Оболочка», отложена) · ремонт
симуляции целиком (milestone «Референс sim»).

### Свежее (31.08)

- **Лист рисуется в честной пропорции** — решение владельца 31.08, «честный масштаб всегда».
  Раньше лист заполнял рамку целиком, и в коде прямо стояло «пропорция листа больше не
  изображает физику». Замер показал цену: у футомаки почти честно (×1,03 — его лист сам
  близок к квадрату), а у **хосомаки и урамаки вдвое** — лист 105 × 190 мм в рамке 504 × 468 px,
  и кусок 10 × 10 мм рисовался 25 × 48. Теперь анизотропия ровно 1,000 на всех базах.
  ⚠ Цена: на коротком ландшафтном экране (1024×768) ось узора даёт 2,79 px/мм вместо 3 —
  честность запрещает листу быть шире, чем высота / 0,905. Держится как ИЗВЕСТНО, не как провал.
  ⚑ Владелец допускает, что дизайн экрана потом переделаем или **будем подстраивать под тип
  маки** — тогда у хосомаки будет своя раскладка, а не общая рамка.
- **Окно «что внутри» — отладочное, а не игровое** (решение владельца 31.08). Крупный срез
  в правом верхнем углу листа, без тени, включается кнопкой-глазом. Нужно, чтобы сверять
  глазами: то ли легло и туда ли попало. Когда сверка станет не нужна — снять целиком.

- **Иконки ингредиентов — нарисованные спрайты** (#104): девять PNG 40×40 с альфой в
  `play/assets/icons/`, Draw Things + `tools/pixel-icons.py`, настройки в скилле draw-things.
- **Пиксельный вид включён по умолчанию**, `?flat` возвращает прежний. Вид листа при этом
  признан незаконченным — см. #105.
- **Тема игры**: `tools/chiptune.py`, 25 с, коридор длительности 13–30 с вшит в генератор.
- Проверка состояния гит-сущностей и доков — `./tools/state-check.sh`.
- **Третий сторож — сверка с практикой поваров** (`play/test/practice.js`, печатается в
  `?check`). Два первых следят за целостностью изнутри; этот спрашивает, сходится ли модель
  с тем, как делают на самом деле. 32 утверждения с источниками: 22 меряются, 10 — пробелы
  (источник есть, мерки нет) и печатаются, чтобы не пропасть. Расхождение, у которого есть
  заведённая задача, печатается отдельным списком, а не красным. У каждой записи
  есть поле `можетБыть`: **ошибка бывает и обратной** — мы считаем, что чего-то нельзя,
  а мастер именно это и делает.
- **Подворот садился втрое ближе, чем на кухне — ЗАКРЫТО 31.08** (#113). Владелец увидела
  хвостик нори внутри ролла с рисом по обе стороны — сначала в лаборатории, потом в самой игре.
  По одиннадцати источникам край ведут до дальней кромки риса, 88–95 % листа; замер давал
  **хосомаки 14,3 %, футомаки 23,1 %**, лист складывался втрое, кромка тонула в рисе.
  Как починено: в игре подворот решается **равенством, а не потолком** (`geometry.js`, 12:54);
  в лаборатории заведено **одно определение на всех** — `sim/fold.py`, посадка на дальнюю
  кромку риса, 87 % листа. `FOLD_CAP` исчез отовсюду, кроме восьми замороженных попыток, где
  он оставлен намеренно (их метрики получены тем кодом; граница выписана в `sim/fold.py`).
  ⚠ **Формула жила не в трёх файлах, а в ОДИННАДЦАТИ** — три живых редакции успели разойтись
  между собой. Строка «чинить надо все три» продержалась в этом файле до вечера.
  ⚠ **И мерка сторожа врала дважды.** Сначала мерила `spreadEnd` (докуда размазан рис, 89 %)
  вместо места посадки края — диапазоны случайно совпали, зелёное подтвердило ложь. Потом
  `sFold / L` против коридора 88–95 % — тоже не то: 88–95 % это куда ПРИЕЗЖАЕТ край за оборот,
  а не длина складки. Мерит теперь глубину вхождения нори по 36 лучам (коридор 0,75–1,0):
  величину, которую видно на срезе и которую источники действительно утверждают.
  Разбор — `sim/KINEMATICS.md`, раздел от 31.08.
- **Обёрток пять**: нори, рисовая бумага, соевая, омлет 薄焼き卵, **гюхи 求肥** — японская
  замена снятому бисквиту, сладкий ролл без европейского рулета.
  ✅ **Рисовая бумага ОСТАЁТСЯ — решение владельца 31.08.2026** («рисовая бумага будет тоже
  нашей темой»). Вопрос был открыт с утра: бумага вьетнамская, и под правилом «только японская
  тематика» её место казалось спорным. Решено в пользу бумаги, и это не отменяет японскую
  тематику: правило снимало блины, лаваш и бисквит как ЕВРОПЕЙСКИЕ форматы, а рисовая бумага —
  азиатская обёртка, которую в японской кухне используют (生春巻き продают в японских магазинах
  наравне с прочим). Развилка #111 в этой части закрыта.
- ⚠ **ЧИСЛО ОТКРЫТЫХ ISSUES — 55, а не 30.** Вечером 31.08 я дважды назвала владельцу 30 и 32:
  `gh issue list` **без `--limit` молча отдаёт тридцать** и не говорит, что список обрезан. Ровно
  об этом стоит предупреждение в `tools/state-check.sh`, написанное после такой же ошибки 30.08
  («30 открытых, 70 закрытых» вместо 82 и 18) — я его прочитала и через час повторила. Комментарий
  в чужом файле не спасает, поэтому проверка теперь в `tools/end-of-day.sh` полосой: любой
  `gh … list` в скриптах обязан нести явный предел. По milestone правда такая: Полнота модели 25,
  Оболочка и режимы 13, Инженерия и регрессия 8, Референс sim 5, Смысл и мир 4.
  **Мандат владельца (10–20 issues) дальше, чем казалось.**
- **Вечерняя сверка заведена скилом** (`/end-of-day`, 31.08.2026). Повод: за один день
  нашлось четыре расхождения ОДНОГО сорта — формула в одиннадцати файлах, шапка файла против
  его же кода, сторож, меривший не ту величину, документ ядра, сверявшийся по датам. Ни одно не нашлось
  само: все четыре нашлись, когда владелец ткнула пальцем. Механическая часть — `tools/end-of-day.sh`
  (маркеры кэша, кодировка, чистота git, кальки в русском, честность ссылок, копии определений);
  то, что требует прочитать и рассудить, — девять полос в скиле, тремя глубинами. На первом же
  запуске скрипт нашёл девять расхождений; все девять исправлены в тот же вечер.
- **Удалённого не бывает — бывает «сделали не так»** (правило владельца 31.08, отменяет
  прежнее «зачёркивать с датой» для КОДА). Запись о том, что убрано, живёт в одном месте —
  `docs/journal.md`. В коде остаются только **ловушки** («не пиши это число») и **причины**
  («почему устроено так»), но не пересказ прежнего текста. Проверочный вопрос: писали бы эту
  строку, начиная с нуля сегодня? **Для основных документов правило то же** — история и туда
  не идёт. Мест, где она законна, ровно два: комментарий к задаче и `docs/journal.md`.
- **Русский язык, правило владельца 31.08.** Устоявшийся термин НЕ переводим (коммит, пуш,
  issue, milestone, baseline, pipeline, хардкодить). Убираем кальку-метафору — буквальный
  перевод, который по-русски не значит того, что значил в оригинале: «сгнить» из *bit rot* это
  просто «сломаться незаметно». Граница: есть обычный русский глагол — берём его; вещь без
  русского имени — оставляем как есть.
- **Глубокая сверка 31.08 разобрана не вся, и остаток вынесен в #117.** Найдено 72
  расхождения; скептиками проверено 45 (39 подтверждено, 6 опровергнуто), и все 39 разобраны
  в тот же вечер. Остальные 29 отсечены порогом `MAX_VERIFY = 5` — это было записано логом,
  а не замолчано, но жили они только во временной папке прогона. Перенесены в задачу целиком,
  с уликами. ⚠ Это **сырьё**: из проверенных каждый седьмой оказался ложным.
- **Контуры отладки — кнопка 📐 в шапке и клавиша `L`** (идея владельца 31.08).
  ⚠ Первая редакция дала ТОЛЬКО клавишу, и режим оказался невидим для того единственного
  человека, ради кого сделан: владелец смотрит игру с телефона, где клавиатуры нет. Проверять
  надо там, где смотрят.
  ⚠ И рисовалось сперва только на ЛИСТЕ, а нужен был СРЕЗ — там геометрия и интересна.
  Теперь на обоих: на листе линия почти совпадает с пиксельной кромкой (все куски пока
  прямоугольные), на срезе показывает кромку нори, окружность ядра и контур каждой начинки. Пиксели остаются, а граница куска
  рисуется вектором ПОВЕРХ. Ценность не в отладке пикселей: контур берётся из координат
  **модели**, а не из того, что нарисовал рендер, — значит он эталон, независимый от стиля.
  Подойдёт пиксель, гладкий или любой будущий: видно, где арт разошёлся с моделью.
  Как ищутся границы: по 240 лучам от центра отмечается СМЕНА КЛАССА материала — это разом
  даёт кромку, ядро и контур каждой начинки, ничего не перечисляя руками (перечисление
  разошлось бы с моделью). Цена: 10 мс на трассировку, дальше из кеша картинки.
  По умолчанию выключен, вне слепка.
- **Цвета кусков на листе исправлены** (замечание владельца «креветка выглядит коричневой»).
  Причина оказалась не там, где искали трижды: не текстура, не пиксельная палитра (куски к ней
  вообще не снаппятся) и не толщина граней, а `AMBIENT = 0,34` — доля света, которая есть
  всегда. Любая поверхность, отвёрнутая от источника, падала до 34 % цвета, а 34 % от
  бледно-тёплого — это коричневый. Замер с холста: лосось #512f23. Поднято до 0,62;
  ⚑ число эстетическое, источников на освещение кухни нет.
- **Отмена и возврат убраны из ряда кнопок** (решение владельца): осталась одна «Очистить».
  Сама история цела — ⌘/Ctrl+Z и Backspace работают, `pushHistory` пишет каждый шаг.
- **Звук по умолчанию выключен**, при старте — заставка из трёх нот по пентатонике кото.
  Заиграть при загрузке она не может (браузер не даёт звук до жеста), поэтому взводится и
  срабатывает на первое касание либо на включение звука кнопкой.

Дальше: **#73 fixtures F01–F08** — то, чем полнота доказывается, а не заявляется.
Делать ПОСЛЕ #11+#25 (намотка по площади), иначе эталоны протухнут в тот же день.

**Не браться сейчас:** пазл и режимы (milestone «Оболочка и режимы»), ремонт
симуляции целиком (12–20 дней, milestone «Референс sim»; индикатор годности уже
починен 30.08 — прибор больше не рапортует «здоров», когда неисправен).

**Границы лаборатории записаны — issue #90.** Читать до того, как задавать ей
вопрос: осевых ответов она не даст никогда, а про некруглость и порог разрыва
вернёт наше же допущение как измерение.

Папка проекта: `~/Desktop/Home/Projects/rollery/` — сам репозиторий (публичный `newYurk/rollery`, GitHub Pages). Рабочее имя «Ролльня» / `rollery` — из треда («ролльня, а не суши-бар»); имя игры не выбрано (кандидат Roll-It), перед выбором — проверка товарных знаков (issue #54).

**Задачи и идеи — в GitHub issues** (доска — проект «Roll-It»): `фундамент` блокирует, `ждёт решения` — за владельцем, `идея` — замыслы, `sim` — референс. **Архитектура системы** (слои, сущности, P0, миграция монолита) — `docs/domain-contract.md`. **Словарь терминов** — `docs/glossary.md`.

## Чем занимаемся сейчас (решение владельца 29.08.2026)

**Полнота механик модели** — и только она. Механика считается готовой, когда её
можно применить к любому куску, результат виден в срезе и повторяется. Проверять
на минимуме ингредиентов (трёх достаточно). **Пазл, уровни, прогресс, альбом —
не трогаем**; как они будут устроены, пока не решаем.

⚠ **Здесь стоял второй счётчик задач, и он противоречил таблице milestone в начале этого же файла.**
Внешний читатель 31.08 попался именно на нижней половине документа. Счётчик снят; актуальные
числа — только в разделе «Milestone», и там же оговорка про `--limit`.

⚠ **И здесь же стояло «шесть задач вне milestone намеренно» — тоже неправда**, снято вечером
31.08 глубокой сверкой. Задач без milestone ноль: строка в разделе «Milestone» верна, эта была мёртвой.
Две строки одного файла говорили противоположное о том же множестве задач — та самая болезнь
«правится дописыванием, а не переписыванием», о которой этот файл предупреждает выше.

Готовность этапа: каждая механика §5 контракта работает и её держит регрессия
`play/index.html?check` — fixtures F01–F08 (#73).

**Научная база** (проверенные DOI под наши вопросы — гармошка, воздух между витками, модуль риса): `docs/papers.md`.

**Лаборатория скрутки** (смотреть, как крутится, и крутить ручки самой): **только** `sim/lab/lab.sh` (не `python serve.py` — упадёт каждый прогон). Что выбирать и как лаборатория соотносится с игрой — `sim/lab/README.md`.

**Отчёт по симуляции скрутки** (кадры фаз, срезы, числа, что опровергнуто): https://claude.ai/code/artifact/433ac466-b27c-4bec-b7be-ebc035f196fa — источник `docs/reports/kinematics.html`.

**Стенд для теста:** https://newyurk.github.io/rollery/play/ — телефон (портрет), планшет, десктоп. Режим «Пазл» — иконка 🧩 или https://newyurk.github.io/rollery/play/?puzzle. Регрессионная проверка — `play/index.html?check`, эталонные числа в начале `play/checks.js`.

**Код разрезан на файлы 29.08.2026** (было 2861 строк в одном). Canvas 2D, classic scripts, сборки нет; порядок подключения обязателен и объяснён в шапке каждого файла:

- `play/model/` — **ядро, не знающее про браузер**: `util.js` · `catalog.js` (базы, обёртки, ингредиенты) · `geometry.js` (грядка риса, укладка, карта высот, намотка, ядро подворота, срез) — 937 строк, переносится в другой движок почти дословно;
- `play/render/` — `slice.js` (срез) · `sheet.js` (вид сверху) · `screens.js` (экраны и ритуал реза);
- `play/ui/` — `layout.js` (холст, брейкпоинты) · `controls.js` · `album.js`;
- `play/modes/puzzle.js` — единственный игровой режим; `state.js` — состояние и localStorage; `audio.js`; `index.html` — каркас (цикл кадров, ввод, запуск);
- `play/domain/roll.js` — **тонкий facade над геометрией** (30.08, #72): рецепт → ролл → срез,
  граница, за которую потребители переезжают по одному. Пока его единственный потребитель —
  проверки; production-код зовёт геометрию напрямую, как раньше;
- `play/test/` — характеризационные fixtures, слепок legacy-поведения и сверка facade↔legacy;
  подключаются только в `?check`. Карта того, кто что берёт у геометрии, —
  `docs/architecture/geometry-call-audit.md`.

**Корень `https://newyurk.github.io/rollery/` — страница с тремя дверями:** игра, лаборатория (локальная, только при запущенном `sim/lab/lab.sh`), документы. Перенаправления со старых ссылок нет — снято 27.08 по решению владельца: держать совместимость ради прежних тестеров не нужно, новую ссылку она раздаст им сама.

**Источник:** тред Perplexity 25.08.2026 — https://www.perplexity.ai/search/80a4af28-bd3c-44a3-836d-2ee462fc7355. Выжимка — `docs/design-core.md` (тред может пропасть; выжимка — не оригинал, но всё существенное там).

## Статус: ядро подтверждено сторонними тестерами; собирается симуляционный референс

**Решения владельца 25.08.2026 (вечер)** — `RESULT.md`:
- Сторонним тестерам игра зашла, направление можно развивать — главная гипотеза подтверждена (качественно; числа замеров 1–5 добираются на следующих показах).
- Правдива ли картинка среза, станет понятно только после прогона симуляции — референс `sim/` собирается сейчас (2D MLS-MPM на gstaichi 4.6, Python 3.12, `sim/.venv`).
- **Воспроизводимость не обязательна**: сравнение с целью — в процентах, разброс при одинаковой раскладке — авторский стиль; люди делятся альбомами/сетами маки, делают из них картины. Следствие: встроенная симуляция в игре больше не под запретом (`docs/simulation-research.md`, поправка ⚑); условие — разброс от руки игрока (скорость тяги, прижим), не от процессора.

## Что это

Кулинарная игра-крафт с ASMR-слоем: плоский лист → скрутка → разрез, и весь смысл — в срезе, который детерминирован тем, что игрок положил. Тематика **только японская** (решение владельца 31.08.2026): один лист, но разные обёртки — нори, рисовая бумага, соевая, 薄焼き卵, гюхи 求肥. Сладкий ролл остался, но японский. Остальное — `docs/design-core.md`; пересказывать здесь нельзя.

Точность и есть продукт: мы строим конструктор, который должен уметь то же, что настоящий
повар, и сверяемся с источниками.

## Что решено

- **Нори смыкается стык в стык** (26.08.2026): внутрь ролла обёртка не заходит — ни спиралью, ни нахлёстом; лист обрезается по обхвату. Нори внутри — только та, что положил игрок: «В нори» вокруг начинки или слоем (`docs/reality-check.md`). Спиральных баз правило не касается — но рулета и лаваша среди них нет;
японская спиральная база будет, и первый кандидат назван: 鳴門巻き, проверенный настоящей спиралью. Уточнение 29.08: для футомаки на целом листе честный нахлёст 1,85–1,91 оборота — поправка в том же доке.
- **Хосомаки и футомаки — две базы** (26.08.2026): полулист с рисом 7 мм и целый лист с рисом 12 мм (SUZUMO), длина листа физическая (`sheetCm`), витки выводятся из неё. Спираль без подворота как принцип жива и защищена в коде
- **Спираль без подворота** как принцип жива и защищена в коде (`geometry.js`, ветка
  `if (!g.tuck)`): японская спиральная база будет, первый кандидат — 鳴門巻き.

- Отдельный проект, не ветка Roti.
- Модель — феноменологическая (физически правдоподобная), не симуляция: каждый параметр (κ, β, сглаживание, подворот, кромка) отвечает за один наблюдаемый эффект и выверен численно (`docs/geometry-audit.md`, `docs/reality-check.md`). Soft-body не будет.
- Стопка вдавливается в намазку сверху: положенное последним — ближе к центру.
- «Любой результат — узор, не провал»: экрана поражения нет; в пазле промах именуется подсказкой, а не «проигрышем».
- Ритуал реза — часть награды; в пазле свой срез показывается до сравнения.
- Базовый адрес — чистый стенд гипотезы; пазл — за иконкой / `?puzzle` (конфликт с запретом заказов в `HYPOTHESIS.md` снят так).

## Что не решено

- **Ядро.** Пазл — направление владельца, стенд его показывает; решение «это ядро v0» не записано. Альтернативы и критика — `docs/archive/core-v0.md`, `docs/mechanics.md`.
- **Стек.** Canvas 2D записан как «быстро собрать и знакомо», а не как выбор стека; `DECISION.md` нет — решение не пересматривается, оно ещё не принималось. **Критерий выбора — не «Canvas или нет», а что мы делаем:** игра, в которую играют в метро, и инструмент, которым пользуются на кухне у доски, дают разные ответы. Инструмент давит на платформу камерой, съёмкой среза для сверки, оффлайном и запуском с иконки мокрыми руками — это как минимум PWA, скорее нативная оболочка и магазин приложений. Игре ничего этого не нужно.
  **Проверено 26.08.2026:** все 26 функций модели (намотка, ядро, срез, карта материалов — около тысячи строк) **не обращаются** к `ctx`, `canvas`, `document`, `window` и `localStorage`. Значит модель переносится в TypeScript, Unity, Godot или нативный код почти дословно. Единственная предосторожность на будущее — не смешивать её с отрисовкой; сейчас это соблюдено.
- **Жест скрутки.** Тяга циновки — кандидат; живой тест не проводился.
- **Имя.** Рабочее.

## Что дальше

⚠ **СПИСОК НИЖЕ — СНИМОК ДО 31.08, а не текущая очередь.** Актуальная очередь наверху,
в разделе «С чего продолжить». Здесь оставлено то, что описывает СОДЕРЖАНИЕ работ; номера
задач в нём могли закрыться или свернуться (#49 и #47 ушли в #112 и #17, #50 закрыт).

1. ⏳ **Референс `sim/`** (весь пакет теперь один — #60, не в работе): три попытки MPM-сцены → судьи → адверсарная проверка физики → сравнение со стендом по пяти контрольным раскладкам → калибровка κ, β, сглаживания, ядра → `docs/sim-calibration.md` (ещё не написан — это цель пакета, а не готовый док) с тем, что менять в `index.html`.
2. ⏳ **Игра**: альбомы/сеты срезов и «картины» из них (#49), вход жеста как источник авторского стиля (#50); пазл остаётся режимом.
3. ⏳ **Выбор нарезки игроком — рассмотреть** (#46; владелец 26.08 сочла идею на первый взгляд хорошей и отложила разбор на потом). Один огурец — четыре узора; таксономия `cut` в модели есть, нужен UI. **Главный вопрос не код, а не перегрузит ли это выбор игрока.**
4. ⏳ Проверка вёрстки на живых устройствах (#77); спецификация — `docs/ui-review.md` §2.
5. ⏳ Идеи из банка (`docs/ideas.md`): ширины цветной краски #47, мини-ролл как элемент #48, вид сбоку #51, мозаика #49.
6. ⏳ Живой тест жеста скрутки + задержка жест→звук (#52); `DECISION.md` по стеку.
7. ⏳ **Выход из монолита** по `docs/domain-contract.md` §11 (#72): fixtures (#73), плейсмент и материалы (#74).

## Не путать с соседними проектами

Roti Stand (`../roti-stand/`) и «Лягушка» (`../night-skip/`) — другие игры; правило **один чат — один проект**. Оттуда сюда не переносятся ни стек, ни архитектура, ни решения; только общие правила Home (git, язык) и правила происхождения звука из Roti (Sonniss/Splice — можно, Epidemic — нет, стоки не через AI).

## Осторожно

- `HYPOTHESIS.md` после записи не редактируется — только зачёркиванием с датой.
- Узоры и имена — только свои; чужие кадзаримаки-дизайны и персонажи не воспроизводить; краски называть по цвету, не по японским ингредиентам (чек-лист в `docs/design-core.md`).
- Живой предпросмотр среза (👁) и режим «Пазл» на замерах по `HYPOTHESIS.md` выключать — иначе награда сгорает до реза.

## ⚑ Отложено, вернуться обязательно

- **Разобрать каждую начинку отдельно** (как режут / как кладут / что видно в срезе) и **научить модель менять сечение начинки вдоль оси ролла** — сейчас у начинки одно сечение на всю длину, поэтому целая клубника рисуется «колбасой». Решение владельца 26.08.2026. Таблица по начинкам и разбор — **issue #10**.

---

## Хроника

Что делалось по дням, что находилось и что откатывалось — `docs/journal.md`.
Здесь этого нет намеренно: файл читается в начале каждого чата, и летопись в нём
стоила бы примерно восьми тысяч токенов на каждый запуск.
