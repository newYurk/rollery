# Где мы находимся — «Ролльня» (Rollery)

Обновлено 27.08.2026. Этот файл — точка входа: с него можно начать новый чат.

Папка проекта: `~/Desktop/Home/Projects/rollery/` — сам репозиторий (публичный `newYurk/rollery`, GitHub Pages). Рабочее имя «Ролльня» / `rollery` — из треда («ролльня, а не суши-бар»); имя игры не выбрано, перед выбором — проверка товарных знаков.

**Научная база** (проверенные DOI под наши вопросы — гармошка, воздух между витками, модуль риса): `docs/papers.md`.

**Лаборатория скрутки** (смотреть, как крутится, и крутить ручки самой): **только** `sim/lab/lab.sh` (не `python serve.py` — упадёт каждый прогон). Что выбирать и как лаборатория соотносится с игрой — `sim/lab/README-выбор.md`.

**Отчёт по симуляции скрутки** (кадры фаз, срезы, числа, что опровергнуто): https://claude.ai/code/artifact/433ac466-b27c-4bec-b7be-ebc035f196fa — источник `docs/reports/kinematics.html`.

**Стенд для теста:** https://newyurk.github.io/rollery/play/ — телефон (портрет), планшет, десктоп. Режим «Пазл» — иконка 🧩 или https://newyurk.github.io/rollery/play/?puzzle. Код: `play/index.html`, один файл, Canvas 2D, без сборки. Регрессионная проверка — `play/index.html?check`, эталонные числа в начале `play/checks.js`.

**Корень `https://newyurk.github.io/rollery/` — страница с тремя дверями:** игра, лаборатория (локальная, только при запущенном `sim/lab/lab.sh`), документы. Перенаправления со старых ссылок нет — снято 27.08 по решению владельца: «не надо о старых тестерах заботиться, я им выдам новую ссылку».

**Источник:** тред Perplexity 25.08.2026 — https://www.perplexity.ai/search/80a4af28-bd3c-44a3-836d-2ee462fc7355. Выжимка — `docs/design-core.md` (тред может пропасть; выжимка — не оригинал, но всё существенное там).

## Статус: ядро подтверждено сторонними тестерами; собирается симуляционный референс

**Решения владельца 25.08.2026 (вечер)** — `RESULT.md`:
- «Сторонним тестерам зашло, можно развивать» — главная гипотеза подтверждена (качественно; числа замеров 1–5 добираются на следующих показах).
- «Правдива ли картинка среза — узнаем, когда прогоним симуляцию» — референс `sim/` собирается сейчас (2D MLS-MPM на gstaichi 4.6, Python 3.12, `sim/.venv`).
- **Воспроизводимость не обязательна**: сравнение с целью — в процентах, разброс при одинаковой раскладке — авторский стиль; люди делятся альбомами/сетами маки, делают из них картины. Следствие: встроенная симуляция в игре больше не под запретом (`docs/simulation-research.md`, поправка ⚑); условие — разброс от руки игрока (скорость тяги, прижим), не от процессора.

## Что это

Кулинарная игра-крафт с ASMR-слоем: плоский лист → скрутка → разрез, и весь смысл — в срезе, который детерминирован тем, что игрок положил. «Ролльня», не суши-бар: тот же лист — нори с рисом, бисквит с кремом, лаваш, блин. Остальное — `docs/design-core.md`; пересказывать здесь нельзя.

## Что решено

- **Нори смыкается стык в стык** (26.08.2026): внутрь ролла обёртка не заходит — ни спиралью, ни нахлёстом; лист обрезается по обхвату. Нори внутри — только та, что положил игрок: «В нори» вокруг начинки или слоем (`docs/reality-check.md`). Спиральных баз (рулет, лаваш) правило не касается.

- Отдельный проект, не ветка Roti.
- Модель — феноменологическая (физически правдоподобная), не симуляция: каждый параметр (κ, β, сглаживание, подворот, кромка) отвечает за один наблюдаемый эффект и выверен численно (`docs/geometry-audit.md`, `docs/reality-check.md`). Soft-body не будет.
- Стопка вдавливается в намазку сверху: положенное последним — ближе к центру.
- «Любой результат — узор, не провал»: экрана поражения нет; в пазле промах именуется подсказкой, а не «проигрышем».
- Ритуал реза — часть награды; в пазле свой срез показывается до сравнения.
- Базовый адрес — чистый стенд гипотезы; пазл — за иконкой / `?puzzle` (конфликт с запретом заказов в `HYPOTHESIS.md` снят так).

## Что не решено

- **Ядро.** Пазл — направление владельца, стенд его показывает; решение «это ядро v0» не записано. Альтернативы и критика — `docs/core-v0.md`, `docs/mechanics.md`.
- **Стек.** Canvas 2D записан как «быстро собрать и знакомо», а не как выбор стека; `DECISION.md` нет — решение не пересматривается, оно ещё не принималось. **Критерий выбора — не «Canvas или нет», а что мы делаем:** игра, в которую играют в метро, и инструмент, которым пользуются на кухне у доски, дают разные ответы. Инструмент давит на платформу камерой, съёмкой среза для сверки, оффлайном и запуском с иконки мокрыми руками — это как минимум PWA, скорее нативная оболочка и магазин приложений. Игре ничего этого не нужно.
  **Проверено 26.08.2026:** все 26 функций модели (намотка, ядро, срез, карта материалов — около тысячи строк) **не обращаются** к `ctx`, `canvas`, `document`, `window` и `localStorage`. Значит модель переносится в TypeScript, Unity, Godot или нативный код почти дословно. Единственная предосторожность на будущее — не смешивать её с отрисовкой; сейчас это соблюдено.
- **Жест скрутки.** Тяга циновки — кандидат; живой тест не проводился.
- **Имя.** Рабочее.

## Что дальше

1. ⏳ **Референс `sim/`** (в работе): три попытки MPM-сцены → судьи → адверсарная проверка физики → сравнение со стендом по пяти контрольным раскладкам → калибровка κ, β, сглаживания, ядра → `docs/sim-calibration.md` с тем, что менять в `index.html`.
2. ⏳ **Игра**: альбомы/сеты срезов и «картины» из них (идея владельца), вход жеста как источник авторского стиля; пазл остаётся режимом.
3. ⏳ **Выбор нарезки игроком — рассмотреть** (владелец 26.08.2026: «хорошая с первого взгляда, рассмотрим позже»). Нарезка — решение повара, а не свойство продукта: один огурец даёт четыре разных узора (брусок · соломка · кружок · ломтик наискось). **Новый инструмент узора без единой новой механики** — правило «положил → скрутил → разрезал» не меняется, меняется форма элемента; ровно этим работают мастера кадзаримаки. Таксономия нарезки уже в модели (`cut` у каждого ингредиента), нужен только выбор в интерфейсе: поле у выделенной начинки рядом с «В нори» и «⟳», два-три варианта на продукт, подпись в чипе. Разбор — `docs/ideas.md`, раздел «Нарезка как инструмент узора». **Главный вопрос не код, а не перегрузит ли это выбор игрока.**
4. ⏳ Остатки вёрстки по `docs/ui-review.md`: проверка на живых устройствах.
5. ⏳ Идеи из `docs/ideas.md`: размеры цветного риса, мини-ролл как элемент (цветок из прутков); из `docs/reality-check.md`: рис 1 см для футомаки (две базы — хосомаки/футомаки), выключить подворот для рулета (честная спираль ролл-кейка).
6. ⏳ Живой тест жеста скрутки; задержка жест→звук; `DECISION.md` по стеку.

## Не путать с соседними проектами

Roti Stand (`../roti-stand/`) и «Лягушка» (`../night-skip/`) — другие игры; правило **один чат — один проект**. Оттуда сюда не переносятся ни стек, ни архитектура, ни решения; только общие правила Home (git, язык) и правила происхождения звука из Roti (Sonniss/Splice — можно, Epidemic — нет, стоки не через AI).

## Осторожно

- `HYPOTHESIS.md` после записи не редактируется — только зачёркиванием с датой.
- Узоры и имена — только свои; чужие кадзаримаки-дизайны и персонажи не воспроизводить; краски называть по цвету, не по японским ингредиентам (чек-лист в `docs/design-core.md`).
- Живой предпросмотр среза (👁) и режим «Пазл» на замерах по `HYPOTHESIS.md` выключать — иначе награда сгорает до реза.

## ⚑ Отложено, вернуться обязательно

- **Разобрать каждую начинку отдельно** (как режут / как кладут / что видно в срезе) и **научить модель менять сечение начинки вдоль оси ролла** — сейчас у начинки одно сечение на всю длину, поэтому целая клубника рисуется «колбасой». Решение владельца 26.08.2026. Подробно и по каждой начинке — `docs/ideas.md`, раздел в конце.

---

## Хроника

Что делалось по дням, что находилось и что откатывалось — `docs/journal.md`.
Здесь этого нет намеренно: файл читается в начале каждого чата, и летопись в нём
стоила бы примерно восьми тысяч токенов на каждый запуск.
