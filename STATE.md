# Где мы находимся — «Ролльня» (Rollery)

Обновлено 17.09.2026. Этот файл — навигация, а не летопись: лимит 100 строк. Хроника —
`docs/journal.md`; прежний `STATE.md` целиком лежит там же, раздел «До 16.09.2026».

## Где мы

- `main` — v228 (`7b1df2e`), опубликовано. Сохранение материала по срезам вдоль ролла; замеры и
  границы — `runs/physics-audit/filling-fix/RESULT.md`.
- **`codex/rollery-next`** — продолжение 16–17.09 (31 слияние за 17-е), в `main` не влито,
  ждёт независимой проверки. Что в ней по задачам (merge-коммит на задачу, ветки удаляются) —
  issue **#241**. Кратко: спираль вдвое быстрее при том же результате; поворот убран (#168);
  нехватка листа видна и сказана (#242 ч.1); ссылка и альбом не округляют рецепт (#236);
  намотку выбирает модель (#247); у шва нет провала (#243), обёртка не толще листа (#244),
  рис не прокалывает нори (#245), щели у голой полосы нет (#249), обжим и пустые бины (#250);
  на голый край класть нельзя (#253); палитра — страницами по смыслу (#258); вид сверху берёт
  картинку как узор (#256); все 27 иконок нарисованы; полный интерфейс по умолчанию, `?min` —
  минимальный стенд; граф блюда в Obsidian (`Rollery-Obsidian/`).
- Тело в витке пока теряет форму (#134) — ждёт решения владельца.
- Журнал работы и передача — `runs/continuation/` (`JOURNAL.md`, `HANDOFF.md`), вне git.

## Куда идём — направление владельца, 31.08.2026

Конструктор суши: продукт — математика, арт — шкура поверх неё. Ролл не раскручивается
обратно, поэтому инструмент обязан собирать то, что делает настоящий повар, по его правилам.
Отсюда веса: честность чисел (#109), сохранение материала (#134), обратный поиск (#80,
заморожен 01.09 с тремя условиями разморозки).

## Где смотреть

- Игра: https://newyurk.github.io/rollery/play/ — полный интерфейс по умолчанию (с 17.09), `?min` —
  минимальный стенд, `?puzzle` пазл, `?v2` игра на ядре V2, `?check` регрессия в браузере.
  Debug ядра V2 — `play/core-v2/`.
- Основной вид — портрет телефона (iPhone 15 Pro, 393 × 852); планшет и десктоп по решению
  владельца пока не рассматриваются (05.09).
- Локально: `node tools/serve.js [порт] [корень]` → `http://localhost:<порт>/play/`.

## Сторожа

```
node tools/check.js                          # модель без браузера, ~35 с
node play/core-v2/run-fixtures.mjs           # фикстуры V2; пересобирает отчёт
node play/core-v2/compare-legacy.mjs         # V2 против legacy; пересобирает отчёт
node --test play/core-v2/core-v2.test.mjs    # тесты ядра V2
node tools/page-markers.mjs                  # маркеры кэша страниц (--fix поднимает)
./tools/state-check.sh · ./tools/end-of-day.sh
```

На v228: `check.js` ВСЁ ЦЕЛО, известно 37 · V2 66 pass + 2 todo · fixtures PASS.
⚠ Терминальный `check.js` не проверяет раскладку экранов (#167) — `play/index.html?check`
открывать в браузере. ⚠ Зелёный сторож не значит «проверено»: мерки ловят только то, что
умеют (#204, #209). После правки модели смотреть срез глазами.

## Что дальше

1. Жёсткий штамп (#134): направление «рис уступает» замерено хуже (бугор 5,34 → 9,37 мм) —
   ждёт решения владельца, как и отнесение клубники с бананом к гнущимся.
2. Перегенерить вид сверху авокадо и тунца (#256); сладкая ветка — на проверке.
3. #242 ч.2 (урамаки), #251, #252, #257; атлас и документы (#239); `?v=` и закрытие issue —
   при переносе в `main`.
Открыто вне ветки: #237, #238.

## Решения владельца, которые легко нарушить

- 04.09 — начинка лежит только вдоль оси ролла, диагонали нет
  (`docs/decisions/ADR-001-erratum-002-no-diagonal-placement.md`, #168); кнопка поворота убрана 16.09.
- 16.09 — переход кольцо/спираль дискретный; тело в витке держит форму; кольцо «как повар»;
  нехватка листа — честная дыра; нори в спирали = лист; вехи вместо доски; в main — после проверки.
  17.09 — толстый кусок в рисе «как в реальности»: циновка вдавливает, ролл круглый (кольцо и спираль);
  лист и мягкие длинные куски гнутся по витку; на голый край листа класть нельзя (#253); сладкий
  ролл — гюхи и белая бобовая паста, фрукты режут тонко; лист короче 2 витков не принимается, лист
  уровня — доля листа базы; намотка всегда авто.
- 02.09, 17.09 (#247) — кольцо или спираль выбирает модель; кнопки ручного выбора нет с 17.09.
- 31.08 — тематика только японская. 02.09 — подсказки на экране раскладки нет (запрет #253 — не подсказка).
- 25.08 — воспроизводимость не обязательна: разброс от руки игрока, не от процессора.

## Известно — не заводить заново

- Положение начинки в зоне подворота не влияет на срез (#139, #134).
- Кольцо кладёт рис одним оборотом; нори растянута или сжата в 0,83…1,29 раза (#165, мягкий канал).
- Швы витков у тонких обёрток (#162); гладкая кромка риса (#24).
- Лист занимает ~23 % высоты телефона: 190 × 105 мм на портрете иначе не ложится.
- V2 умеет две базы из шести, остальным отвечает `base_unsupported` (#205, #209).

## Осторожно

- `HYPOTHESIS.md` после записи не редактируется — только зачёркиванием с датой.
- Узоры и имена — только свои; краски называть по цвету (чек-лист в `docs/design-core.md`).
- На замерах по `HYPOTHESIS.md` выключать живой предпросмотр (👁) и пазл.
- С источником сравнивается набитый ролл; пустой — только сам с собой (#208).
- В git — только исходники: прогоны, кадры и скриншоты ревью остаются локально.

## Где что лежит

- `play/model/` — геометрия без браузера (`geometry.js`, `catalog.js`, `canon.js`);
  `play/render/`, `play/ui/`, `play/modes/`, `state.js`, `checks.js`; `play/core-v2/` — ядро V2.
- `docs/design-core.md` — замысел; `docs/domain-contract.md` — архитектура; `docs/decisions/` — ADR ядра V2.
- `sim/` — офлайн-референс (`sim/lab/lab.sh`). `HYPOTHESIS.md`, `RESULT.md` — гипотеза и решения 25.08.
- `Rollery-Obsidian/` — граф блюда (99 заметок, у каждой строка «где это в игре»); пересборка
  `tools/obsidian-vault.py`, картинка `obsidian-graph.py`.
