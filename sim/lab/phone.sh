#!/bin/bash
# Оставлен как привычное имя: с 30.08.2026 отдельный скрипт для телефона не нужен —
# lab.sh сам слушает сеть и печатает адрес для телефона. Просто зовём его.
exec "$(dirname "$0")/lab.sh" "$@"
