# Кинематика скрутки для референса — как это делает повар

Записано 26.08.2026 по итогам первого прогона `mpm-shell/run.py` (раскладка 1, сетка 392×102, 12k частиц, 13 с CPU).

**Как читать этот файл (указатель, 29.08.2026).** Это технический дневник
строительства референса; на его разделы («ГЛАВНАЯ ПОПРАВКА», поправки 8–11,
«Геометрия баз») по именам ссылаются восемь README в `sim/` — разделы не
переименовывать и не удалять. Действующая модель циновки — ГЛАВНАЯ ПОПРАВКА
(12:50); текущее состояние и числа — `sim/reference2/README.md`; пересказ для
человека — `docs/reports/kinematics.html` (собран до поправки 11, её там нет).
Оговорка к метрике `wrinkles` (порог «ноль» верен только для нерастяжимой
нори) — `docs/reality-check.md`, раздел «Оговорка к метрике wrinkles».
Живые задачи отсюда заведены в issues: пасты #66 · раздавленный рис #59 ·
пакет поправки 11 #60 · рулет #63 · эмерджентный радиус mat-chain #65 ·
модуль нори / полный текст Jung 2022 #58.

## Что показал первый прогон

Физика в порядке: намотка происходит, нори — сплошная лента без разрывов (max gap 0,07 T при шаге частиц 0,05), сбежавших частиц 0, рис сжат (J = 0,91), устойчиво. **Кинематика неверна** — три дефекта:

1. Циновка — «каток», катящийся по столу от ближнего края: лист наматывается **вокруг точки**, 3,25 оборота нори внутри ролла. У повара ближний край **поднимается и загибается через начинки** (подворот), и лишь потом ролл катится.
2. Каток **толкает рис перед собой** (кадр t=67: бугор риса впереди циновки) — рис счищается с листа; в карте среза осталось 70 % риса.
3. После замыкания циновки **хвост листа остаётся снаружи** кучкой справа — конец не прижат.

## Что нужно (сценарий движения циновки, четыре фазы)

Фаза A — **подъём края** (t ∈ [0, tA]): ближний край листа (x ∈ [0, ~1 T]) захвачен циновкой (кинематический захват: частицы нори в полосе первых 0,5 T получают предписанную скорость — это «пальцы», которые держат край) и движется по дуге: вверх и вперёд, к точке над дальней стороной начинок (x ≈ s_fold, где s_fold = конец зоны начинок + 1 T, или 5 T при пустом листе). Лист при этом сгибается сам (нори жёсткая на растяжение, мягкая на изгиб — в MPM это получается из тонкости полосы).

Фаза B — **подворот** (t ∈ [tA, tB]): захваченный край опускается на лист за начинками и прижимается вниз (предписанная скорость вниз до контакта, затем удержание) — образуется ядро; нори ближнего края оказывается внутри (крючок). Циновка (дуга-цилиндр) в это время обхватывает ядро сверху с силовым контролем радиуса, как сейчас.

Фаза C — **прокатка** (t ∈ [tB, tC]): захват отпущен; цилиндр циновки катится вперёд **без скольжения по листу** (скорость центра = ω·R), обхватывая ролл дугой ~270°, радиус — силовой контроль по давлению P_roll; рис перед циновкой не должен сгребаться — для этого циновка касается ролла только сверху и спереди (дуга не опускается ниже y ≈ 0,3 T впереди ролла), а лист «входит» в ролл снизу за счёт того, что ролл катится ПО листу, а не циновка по столу.

Фаза D — **замыкание и прижим** (t ∈ [tC, tD]): когда точка касания прошла конец листа, дуга циновки замыкается до 360°, хвост (голый нори-клапан) прижат к роллу; затем прессовка: радиус уменьшается до силового равновесия при P_press; для квадратной формы — SDF скруглённого квадрата (уже есть).

## Параметры «руки» (входы, которые должны менять результат)

- `--speed`: скорость движения центра циновки в фазе C (и подъёма в A). Ожидание: быстрее — менее плотная намотка (рис не успевает сжаться), больше воздуха между витками, чуть больший R.
- `--press`: давление P_roll и P_press. Ожидание: сильнее — рис тоньше под начинками, ролл меньше, при переполнении — бугор растёт наружу.
- `--tuck` (0..1): насколько далеко заводится край в фазе B (0,6 — недоворот, 1,0 — до конца зоны начинок, 1,3 — переворот). Ожидание: меняет размер ядра и положение крючка.

## Проверки после доработки (раскладка 1 и 4)

- Оборотов нори по 36 лучам: 1,2–1,6 (не 3).
- Рис в карте среза: ≥ 90 % начального.
- Хвост: после фазы D снаружи ролла нет частиц дальше 0,3 T от контура.
- Раскладка 4: четыре начинки в ядре, порядок слева направо сохранён, ⌀ ≈ 4,5–5 T.

---

## Сколько оборотов делает нори — и почему стык в стык это реальность (уточнение 26.08.2026)

Раньше в этом файле стояла цель «1,2–1,6 оборота». Это **неверно** как цель для правильно собранного ролла, и вот почему.

Лист нори 19 × 21 см. Готовый футомаки — ⌀ 5–6 см, то есть обхват π · 5,5 ≈ **17 см**. Рис покрывает лист, кроме голой полосы 2–3 см у дальнего края. Значит **нори делает один оборот плюс нахлёст** — 1,1–1,2 витка по источникам, до 1,6 у традиционного футомаки на 1⅓–1½ листа. Голая полоса и есть нахлёст, у него японское имя **のりしろ**: 1–1,5 см у хосомаки, 2–2,5 см у футомаки. Она ложится **снаружи**, поверх уже намотанной нори, и склеивается влагой риса — шов кладут **вниз** (три независимых источника: 白ごはん.com, oisiiryouri, 栗原はるみ). На срезе это одна тёмная окружность плюс короткая дуга двойной толщины внизу, а не два кольца.

⚠ ЗДЕСЬ СТОЯЛО «ровно один оборот». Это спорило с нашими же эталонами в `play/checks.js` (1,32 у хосомаки, 1,42 у футомаки) и с источниками, которые дают строго больше единицы. Разбор 31.08, `docs/kitchen-practice.md`. Повар подбирает лист под размер ролла — половина листа на хосомаки, целый на футомаки, лишнее отрезает.

Отсюда:

- **Тёмной дуги от обёртки внутри ролла быть не должно.** Она появляется, только если лист заметно длиннее обхвата (тонкий ролл целым листом) — то есть при ошибке в размере листа. Именно этот случай и давала первая версия модели, наматывая лист **вместе с рисом** дальше одного оборота.
- **Нори внутри — только положенная нарочно:** обёртка вокруг начинки («В нори») или слоем (рис → нори → рыба). Это язык узора, а не побочный эффект скрутки.

**Цель для референса, исправленная:** число оборотов нори по 36 лучам — **1,0–1,2** (один оборот плюс короткий клапан снаружи), минимум 1. Если получается 2 и больше — кинематика или длина листа неверны.

**Единственное расхождение стенда с реальностью** (маленькое и на срезе невидимое): в игре лишний лист обрезается начисто, стык в стык, а в жизни остаётся клапан 2–3 см снаружи. При сравнении карт материалов это даёт различие только в классе «обёртка» на дуге шва; отметить в отчёте строкой и не тянуть под него параметры.

### Ещё одно уточнение (26.08.2026, 07:10): цель по оборотам была посчитана неверно — сверять надо арифметикой

Проверил числами, и оказалось, что прогоны агентов **физически правильные**, а неверной была моя цель «1,0–1,2».

Сколько слоёв обёртки обязано получиться, считается из сохранения площади, а не из желания:

```
площадь = рис (L − клапан) · T  +  нори L · w  +  начинки
Rout = √(площадь / π);   Rcore = √(полуподворот · высота постели / π)
слоёв = (Rout − Rcore) / (T + w)
```

Для целого листа 38,7 T:

| Раскладка | Подворот | Rout | Rcore | Слоёв обёртки | Пересечений лучом |
|---|---|---|---|---|---|
| 1 — пусто | 5 | 3,49 | 1,30 | **1,96** | ≈ 3 (плюс нори подворота) |
| 4 — четыре начинки | 11 | 3,71 | 1,93 | **1,59** | ≈ 2,6 |

Прогоны дают: `kin-grab` 2,97 и 2,06; `kin-mat` 2,64 и 2,39. **Это попадание в расчёт**, а не ошибка кинематики.

**Вывод:** целый лист на ролле такого диаметра действительно даёт два слоя с лишним — поэтому повар и подбирает лист под размер (полулист на хосомаки). Один оборот получается не «правильной скруткой», а **правильной длиной листа**.

**Исправленный критерий для референса:** число слоёв обёртки должно совпадать с расчётной формулой выше в пределах ±0,25 — и всё. Отдельно проверяем, что обёртка — одна непрерывная лента (одна связная компонента).

> ⚠ **ЗДЕСЬ СТОЯЛО «ближний край оказался внутри (подворот), а дальний — снаружи» — критерий
> СНЯТ 31.08**, см. «Критерий приёмки заменяется» ниже в этом же файле. Он описывал не то, что
> происходит на кухне: край ведут не внутрь, а на дальнюю кромку риса. Строка пережила отмену
> на четыре дня и стояла ВЫШЕ раздела, который её отменяет, — читающий сверху вниз брал снятое
> за действующее (найдено триажем 01.09, issue #113).

**Настоящая невязка, которую надо чинить, — не обороты, а рис:** во всех прогонах сохраняется 74–81 % вместо ≥ 90 %. Причина по кадрам — циновка подгребает рис перед роллом. Это и есть предмет работы.

**Про стенд.** В игре лист обрезается ровно по обхвату — это соответствует выбору правильного размера листа, а не выдумке; и потому внутри нет ни лишнего слоя, ни дуги от него. Единственное, что стенд не рисует, — нори самого подворота (тонкая дуга у начинок), и это сознательное решение владельца ради читаемости узора.

### Геометрия баз: длины листов выведены из стенда (26.08.2026, 11:05)

В стенде длина листа не задаётся, а **выводится из числа оборотов** (`sheetLen`): `L = r₀·θ + P·θ²/(4π)`, где `θ = 2π·turns`, `P = T + w`, `r₀ = 0,25` (в единицах толщины намазки суши). Отсюда для каждой базы — в **её собственных** единицах (T этой базы = 1):

| База | T (сушевых ед.) | Обёртка w | Длина листа L | Ожидаемо оборотов | Rout по площади |
|---|---|---|---|---|---|
| Суши-ролл | 1,0 | 0,12 | **38,7** | 3,10 (полный лист; повар обрезает) | 3,72 |
| Рулет | 0,8 | **0,52** | **40,2** | **2,70** | 3,54 |
| Лаваш | 0,45 | **0,44** | **107,6** | **4,50** | 3,18 |

Проверка: `Rout = √(L·(T+w)/π + r₀²)`, `оборотов = (Rout − r₀)/(T + w)` — сходится с числом оборотов базы до второго знака.

**Поправка к заданию агентам.** В первой формулировке спиральной задачи для рулета была указана длина **75** — это ошибка (получилось бы ~7,7 оборота, что агенты и увидели в прогонах 10:55–10:58, и их прогоны были физически верны). Правильно — **40,2**. Для лаваша 107,6 было верно.

**Как проверять спиральные базы:** число оборотов должно совпасть с формулой выше в пределах ±0,25 при заявленной длине листа; если не совпадает — виновата кинематика (воздух между витками или неплотный старт), а не «неправильная цель».

### Дефект «гармошка»: лист складывается вместо того, чтобы гнуться (найден владельцем 26.08.2026, 11:35)

**Что видно.** На фазе B (заведение) обёртка собирается в 2–3 складки — на кадрах `frames_4/*_B.png`
(t ≈ 36…53) нори слева образует гофр, а рис в зоне сгиба взбит в завитки. Повар держит лист ровным;
такой гармошки в жизни не бывает. Это не косметика: складки съедают длину листа, завышают радиус
и портят укладку ядра.

**Причина — три сложенных, все чинятся.**

1. **У ленты нори нет изгибной жёсткости.** Лента — 2 ряда частиц (`W_NORI = 0,12` при шаге частиц
   0,056). Жёсткость на изгиб растёт как куб толщины: `0,12³ ≈ 0,0017` против `0,12` на растяжение —
   то есть в 70 раз слабее. В MPM такая лента гнётся свободно и **теряет устойчивость на масштабе
   сетки**: вместо одной дуги получается несколько складок. Это известная беда тонких оболочек в MPM
   (Jiang, Gast, Teran 2017), и лечится она явной изгибной моделью, а не увеличением `E`.
2. **Край толкают, а не тянут.** Захват ведёт край по дуге, а остальной лист ничем не удерживается:
   материал вдавливается в зону сгиба, деваться ему некуда — он гофрируется. В жизни лист лежит на
   циновке, прижат ладонью и **тянется**, а не сминается.
3. **Радиус сгиба меньше, чем лист может дать.** Петля разворачивается на 180° на длине порядка
   толщины риса. Лист с любой жёсткостью так согнуться не может: повар заворачивает край **через
   стопку начинок**, и радиус сгиба равен её высоте (2–3 T), а не 0,5 T.

**Как чинить (по возрастанию цены).**

- **Прижим листа впереди сгиба** (дёшево, физически честно): пока ролл не дошёл, лист удерживается
  на столе — трением или лёгкой кинематической границей. Тогда материал **вытягивается** в сгиб.
  Это ровно то, что делает вторая рука повара.
- **Радиус сгиба от стопки** (дёшево): траектория захвата — не маленькая дуга, а обход вокруг стопки
  начинок; минимальный радиус сгиба ограничить снизу высотой стопки.
- **Изгибная жёсткость обёртки** (настоящее решение): лента как лагранжева цепочка с пружинами на
  кривизну (штраф за излом), интегрируемая через ту же сетку. Тогда лист физически не может
  сложиться мельче собственного радиуса изгиба.

**Метрика, которую надо добавить и проверять** (её не было ни в одной из четырёх атак — проверяли
разрыв, но не складки):

```
wrinkles = число смен знака кривизны вдоль ленты обёртки, вне зоны сгиба (носа петли)
```

Считать по срединной линии ленты, сглаженной по 3 частицам. **Порог: `wrinkles ≤ 1` на всех фазах;
амплитуда складки < 0,5 T.** Прогон, где лист гофрируется, считается недействительным независимо
от остальных чисел.

---

## ГЛАВНАЯ ПОПРАВКА (26.08.2026, 12:50): циновка — не дуга сверху, а лист, который заворачивается ВМЕСТЕ с роллом

Владелец: «посмотри видео — там раскручивается вместе с бамбуковым ковриком, поэтому это держит форму, и гармошки просто быть не должно. Максимум ещё руками поддерживают верхний слой, чтобы он правильно лёг».

Это и есть причина «гармошки». Все предыдущие фазы (A/B/Btuck/Bhold/C/D) построены на неверной модели: циновка была **жёсткой дугой, прижимающей сверху**, а край нори тянули отдельным кинематическим «захватом». Между захваченным краем и лежащим на столе листом оставался **свободный пролёт** — он и складывался. У повара такого пролёта нет ни в один момент.

### Как на самом деле (подтверждено источниками)

Дословно из руководств: *«With your thumbs beneath the bamboo mat and the rest atop the sushi rice, start rolling by lifting the mat with nori over the fillings, then gently press and hold for a second, then continue rolling»*; *«Using your thumbs to lift the edge of the mat closest to you... Use your fingers to tuck the fillings in tightly as you make the first turn»*; *«bring the near edge of the rice to meet the far rice line»*; *«gently squeeze to tighten the roll as it forms... put pressure from all three sides».*

1. **Циновка лежит ПОД листом с самого начала**, её ближний край совпадает с ближним краем нори. Нори к ней прижата рисом сверху — трение большое, лист повторяет форму циновки точно.
2. **Большие пальцы поднимают циновку** — не нори. Нори едет на циновке. **Свободного пролёта не существует**: лист в каждый момент лежит либо на столе, либо на циновке.
3. **Остальные пальцы держат начинку сверху**, чтобы она не разъехалась при первом обороте. Это вторая опора: начинки не выдавливаются вперёд.
4. **Первый оборот доводит ближний край риса до дальней линии риса** — рис встречается с рисом, начинки замурованы. Затем **прижать и подержать секунду** — это уплотняет ядро.
5. **Дальше циновка катится от себя вместе с роллом**, обнимая его снаружи по всей дуге контакта, с равномерным давлением. Её передний край **постепенно выводится из-под ролла**, чтобы не закататься внутрь.
6. **В конце — обжать со всех сторон**, от торцов к середине.

### Что это значит для модели

**Циновка = гибкая нерастяжимая полоса-граница**, а не дуга окружности:
- своя длина, ближний конец совпадает с ближним краем листа;
- нори к ней **приклеена трением** (высокий коэффициент, скольжение почти нулевое) — значит, форма нори = форма циновки, и **лист физически не может сложиться мельче, чем гнётся циновка**;
- у самой циновки есть минимальный радиус изгиба (бамбуковые палочки шириной около 2–3 мм в масштабе T ≈ 5 мм — это ~0,5 T): **гармошка невозможна по построению**, дальше этого радиуса циновка не гнётся;
- по мере качения циновка «отпускает» ролл сзади: контакт скользит вперёд, задний край выходит наружу.

**Пальцы на начинке** — вторая кинематическая опора на первом обороте: удерживают стопку от выдавливания вперёд, отпускаются после смыкания риса.

**Отсюда фазы переписываются:**

| Фаза | Что движется | Чем задаётся |
|---|---|---|
| 1. Подъём | ближний конец циновки идёт вверх-вперёд; нори едет на ней; пальцы держат начинку | траектория конца циновки, ограниченная её минимальным радиусом изгиба |
| 2. Смыкание | ближняя линия риса доводится до дальней линии риса поверх начинок | цель — контакт рис-рис, а не «угол 180°» |
| 3. Прижим | циновка обжимает ядро, пауза | давление, удержание |
| 4. Качение | циновка катится вперёд вместе с роллом, обнимая его снаружи; задний край выводится | контакт скользит по листу, давление постоянное |
| 5. Обжим | равномерное давление по кругу, форма (круг/квадрат/треугольник) | сила прижима |

**Проверка модели:** складок должно быть **ноль** — не «не больше одной». Если складка появилась, значит нори где-то оторвалась от циновки: это ошибка контакта, а не свойство листа. Метрика складок остаётся, но её порог теперь строгий.

**Техники бывают разные** (плотнее/слабее, с большей или меньшей паузой) — это и есть наш «почерк»: скорость качения, сила прижима, длительность паузы после смыкания. Но **ни одна техника не даёт гармошку**.

### Что это значит для стенда (index.html)

Ничего не ломает: стенд не моделирует процесс, он сразу считает результат намотки. Но объясняет, почему в стенде правильно **не** закладывать складки и почему «почерк» ограничен тремя ручками (скорость, прижим, удержание) — у повара ровно эти степени свободы и есть.

---

## Пробел: пасты и присыпки ведут себя не как начинки (замечено владельцем 26.08.2026, 13:05) *(→ issue #66)*

Владелец: «а как же плотность креветки, рыбы, огурца, майонеза? Майонез просто вдавится в промежутки риса, если его положить внутрь. А это в механике не учтено».

**Пробел настоящий.** И в стенде, и в референсе **все начинки — упругие тела, которые держат форму** и вытесняют намазку. Для огурца, тамаго, авокадо, креветки, куска лосося это верно. Для майонеза, соуса, сливочного сыра, джема, шоколада — **нет**.

**Определяет поведение не плотность, а предел текучести** (материал течёт под давлением или держит форму), плюс проницаемость рисовой постели. Плотности у всех этих продуктов близки к единице — по ней их не различить. Три класса:

| Класс | Примеры | Предел текучести | Что делает под циновкой | Что видно на срезе |
|---|---|---|---|---|
| **Тело** | огурец, тамаго, авокадо, креветка, ломоть лосося, сыр бруском | высокий (упругое тело) | держит форму, вытесняет рис, раздувает ролл | чёткое пятно своей формы |
| **Паста** | майонез, острый соус, сливочный сыр, джем, шоколад | низкий (вязкопластик) | **течёт**: вдавливается в промежутки между рисинками, растекается по контакту, расплющивается в плёнку | тонкий неровный мазок с размытой границей, а не круглая полоска; ролл почти не раздувает |
| **Присыпка** | кунжут, укроп, тобико, орех крошкой | сыпучая среда | заполняет пустоты, не сопротивляется | точки в рисе, форма не своя |

**Ключевое следствие, которого сейчас нет ни в стенде, ни в референсе:** паста **не добавляет объёма ролла** — она садится в поры рисовой постели, а не поверх неё; и **не сохраняет сечение** — её толщина падает, а длина растёт, при сохранении площади.

**Как моделировать в референсе** (машинерия уже есть): у материалов в `MATERIALS` есть предел текучести `tau_y`; сейчас у паст его просто нет, потому что паст нет в списке. Добавить: `mayo`, `sauce`, `cream`, `jam` с низким `tau_y` (порядка 0.003–0.01 против 0.03 у риса и 1e9 у упругих тел) и низким модулем. Опора в литературе — модель среды со сдвиговой зависимостью, Yue et al., Continuum Foam (`10.1145/2751541`, см. `docs/papers.md`): ровно про течение таких масс. Проверить численно: паста при прижиме должна тонуть в рисе на глубину порядка размера зерна и растекаться, а радиус ролла не расти.

**Как моделировать в стенде** (дёшево, три правки):
1. Пометить пасты флагом `paste: true` с параметром текучести.
2. При сборке стопки паста **не поднимает полосу** (садится в поры): её вклад в толщину слоя равен нулю до тех пор, пока она тоньше поры, а её собственная высота уменьшается пропорционально прижиму `beta`, ширина растёт с сохранением площади.
3. Граница пасты с намазкой — не резкая, а с зоной смешивания порядка 0.2 T: рис, пропитанный соусом.

**И это не только физика, но и игровой инструмент.** Паста рисует **тонкую линию**, а не пятно — а тонкий контур это ровно то, чем в кадзаримаки выводят детали узора. То есть после правки у игрока появляется вторая кисть: тела дают пятна, пасты — линии, цветной рис — заливку.

**Статус:** зафиксировано, делается **после** текущей цепочки (циновка → проверки → калибровка), чтобы не смешивать два больших изменения в одном прогоне. Иначе калибровка пойдёт по референсу, который сам не знает про пасты.

---

## Находка владельца 26.08.2026, 13:25: гармошка внутри — следствие раздавленного риса, а не сгиба *(лечение — issue #59)*

Прогон в лаборатории: `mat-sdf`, раскладка 1, `--speed 0.4 --press 1.6 --tuck 0.6`. Владелец: «разве может такая гармошка нори быть внутри? на предпоследних кадрах было OK».

**Наблюдение точное, и оно указывает на причину.** На кадре 29 из 35 (фаза качения) ролл ещё правильный: `R = 3.80`, нори двумя чистыми витками. К последнему кадру после прижима:

| Метрика | Значение | Норма |
|---|---|---|
| Радиус ролла | **2.83** | было 3.80 на кадре 29 |
| Объём материала | **0.616** | ≥ 0.95 |
| Сжатие риса `J` | **0.574** | 0.88…0.99 |
| Оборотов обёртки | **4.06** | по формуле 1.93 |
| Складок | **10** | 0 |

**Цепочка следствий, в порядке причинности:**

1. **Рис раздавлен до 57 % объёма.** Это главная ошибка. В модели рис — сжимаемое тело с коэффициентом Пуассона 0.35, то есть объёмное сжатие ничем не ограничено: дави сильнее — сожмётся сильнее, до нуля. Настоящий варёный рис на 70 % состоит из воды и **практически несжимаем**: он уплотняется настолько, насколько между зёрнами есть воздух — это 5–15 %, — а дальше упирается насмерть.
2. **Ролл схлопнулся** с радиуса 3.80 до 2.83, то есть длина окружности упала на четверть.
3. **Нори не умеет укорачиваться** — она почти нерастяжима. Оказавшись на схлопнувшемся ролле, лист обязан куда-то деть лишнюю длину. Единственный доступный способ — **сложиться гармошкой**. То есть складки здесь не причина, а симптом.
4. **Оборотов стало 4.06 вместо 1.93** по той же причине: тот же лист на вдвое меньшем ролле наматывается вдвое чаще.

**Что чинить (в порядке важности):**

- **Ограничить сжимаемость риса.** Либо коэффициент Пуассона 0.45–0.49 (почти несжимаем), либо явный предел уплотнения: до `J ≈ 0.88` рис отдаёт воздух легко, ниже — модуль объёмного сжатия резко растёт. Второе честнее физически и даёт правильную реакцию на прижим: сильнее давишь — плотнее до предела, а потом ролл просто перестаёт уменьшаться.
- **Прижим ограничивать силой, а не перемещением.** Сейчас циновка додавливает до заданного радиуса; надо — до заданного давления, и если рис уже не поддаётся, движение прекращается.
- **Переименовать метрику.** `conservation = Σ(vol·J) / Σ(vol)` — это **не** сохранение массы (масса в MPM сохраняется всегда, её несут частицы), а **отношение объёмов**. Значение 0.616 читалось как «потеряли 38 % материала», а на самом деле означает «сжали до 62 %». В лаборатории подпись уже исправлена на «Объём (1 = не сжат)».

**Урок для метрик:** проверка «складок ≤ 0» ловит симптом, а не болезнь. Нужна проверка **на сжатие**: `J` риса не ниже 0.88 в любой момент прогона. Без неё сильный прижим всегда будет «лечиться» складками.

---

## Находка владельца 26.08.2026, 13:35: «несколько оборотов — это же не маки»

Владелец, глядя на срез с двумя витками: «когда несколько закручиваются — это получается у нас не маки, мы сейчас тестируем?»

**Права.** Посчитал по сохранению площади для нашего листа 19 см (38,7 T) и слоя риса 5 мм:

| Раскладка | Площадь среза, T² | Диаметр ролла | Оборотов листа |
|---|---|---|---|
| 1 — пустой лист | 38 | 3,5 см | **1,76** |
| 4 — четыре начинки | 50 | 4,0 см | **1,55** |
| плотный футомаки | 68 | 4,7 см | 1,32 |
| очень плотный | 108 | 5,9 см | **1,05** |

**Чтобы получился ровно один оборот, ролл должен быть ⌀ 6,2 см** — площадь среза 119 T², то есть начинок нужно 81 T², **в семь раз больше**, чем в нашей раскладке 4.

**Отсюда прямой вывод: наши контрольные раскладки — не футомаки, а тонкие роллы из большого листа.** Настоящий футомаки **набит** — четыре-пять начинок, среди них крупные (тамаго, тэмпура, омлет), и именно поэтому лист обходит его один раз. Наши четыре тонких кусочка дают ролл ⌀ 4 см — для такого нужен не целый лист, а примерно две трети.

**Что это значит:**

1. **Раскладка 1 (пустой лист) не может дать один оборот — при нашей длине листа.** Формулировка «никогда, сколько ни крути» была неточной (поправка владельца 26.08.2026, 13:40: «может, но это будет маки диаметром 1–2 см» — верно).

   Точный ответ: число оборотов у листа только с рисом растёт как корень из длины, `оборотов ≈ √L / 3,75`. Значит один оборот получается **при коротком листе**:

   | Лист | Оборотов | Диаметр ролла |
   |---|---|---|
   | 4 см | 0,80 | 1,6 см — **не смыкается**, рис торчит |
   | 6 см | 0,98 | 1,9 см — почти смыкается |
   | **6,2 см** | **1,00** | **2,0 см** — ровно один оборот |
   | 7 см | 1,06 | 2,1 см |
   | полулист 9,5 см | 1,25 | 2,5 см |
   | целый 19 см | 1,76 | 3,5 см |

   То есть: **ровно один оборот пустого листа даёт ролл ⌀ 2 см** — тонкий рисовый рулетик, каким и бывает каппамаки без начинки. Короче 6 см лист вообще не смыкается — получается не ролл, а завиток с открытым боком. Наш лист 19 см даёт 1,76 оборота; чтобы свести к одному, нужна либо начинка (в семь раз больше, см. таблицу выше), либо лист втрое короче.
2. **Правило «повар подбирает лист под ролл» подтверждено численно.** Полулист с одной начинкой даёт хосомаки ⌀ 2,7 см и 1,16 оборота — почти ровно один. То есть повар не «умеет крутить в один оборот», он берёт лист нужной длины.
3. **В игре обрезка листа по обхвату — не упрощение, а ровно то же решение.** Наши раскладки такие же неплотные, как контрольные, и без обрезки давали бы полтора-два витка.
4. **Для референса нужна честная раскладка футомаки** — с настоящим количеством начинки (около 80 T², то есть крупные куски), иначе мы калибруем модель на роллах, которых не бывает.

**Что добавить в контрольный набор:** раскладка «настоящий футомаки» — тамаго 4×3, тэмпура-креветка 3×3, огурец 2×2, авокадо 3×2, кампё и шиитаке — суммарно около 80 T². На ней и проверять «один оборот», а не на пустом листе.

---

## Поправка 8 (26.08.2026) — циновка: две модели, одна выжила

После главной поправки («циновка лежит ПОД листом и катится вместе с роллом»)
её переписали в двух вариантах, и они разошлись до противоположного результата.

**`mat-chain` — циновка как цепочка жёстких планок.** Физически заманчиво:
бамбук и правда набран из палочек. На деле модель **рвёт нори во всех
раскладках**: `nori_torn: true`, карта распадается на 2 / 3 / **16** связных
кусков, часть частиц улетает за ролл (до 39 T мимо контура), `stable: false`
везде. Причина — стыки между планками: на стыке кривизна скачком меняет знак,
нори защемляется между соседними планками и рвётся. Настоящая циновка так
себя не ведёт, потому что палочки связаны нитью и катятся по дуге, а не
шарнирятся независимо.

**`mat-sdf` — циновка как аналитическая подвижная граница** (дуга окружности
радиуса R, касающаяся стола в точке контакта, как знаковое расстояние).
Работает: нори цела (`nori_components_map: 1`), 0 улетевших частиц,
**`wrinkles: 0` в итоге во всех трёх раскладках**, сохранение 0,95–0,97.

| | mat-sdf | mat-chain |
|---|---|---|
| нори порвана | нет (1 кусок) | **да** (2 / 3 / 16 кусков) |
| гармошка в итоге | **0 / 0 / 0** | 0 / 1 / **11** |
| частиц вне контура | 0 / 0 / 0 | 104 / 1002 / 617 |
| `stable` | да / да / да | **нет / нет / нет** |

**Референс — `mat-sdf`.** `mat-chain` оставлен как отработанный отрицательный
результат: он показывает, что дискретизация циновки на независимые планки —
источник ложных разрывов, а не улучшение точности.

Промежуточная рябь в `mat-sdf` есть (до 3–4 складок в фазе прижима,
`wrinkle_ok: false`), но она **разглаживается**: `wrinkle_ok_final: true`.
Это ровно то, что видно на видео — под циновкой пробегают морщины и
уходят под нажимом. Требование владельца «гармошки просто быть не должно»
относится к итоговому срезу, и оно выполнено.

### Что это даёт стенду: калибровка

Сим и стенд отвечают на **разные вопросы** и сходятся там, где живёт
настоящий маки. Лист один и тот же — 38,7 T (19,3 см).

| раскладка | Rout сим, T | Rout стенд, T | расхождение |
|---|---|---|---|
| 1 — пустой лист | 3,44 | 2,46 | 40 % |
| 2 — тамаго у края | 3,70 | 2,73 | 36 % |
| 4 — четыре начинки | 3,97 | **4,10** | **3 %** |

Расхождение на пустом листе — не ошибка ни у кого. Сим **наматывает весь
лист** (3,1 оборота, отсюда большой радиус), стенд **обрезает излишек** и
стыкует нори встык за один оборот. Это и есть настоящая техника: лишний лист
не наматывают внутрь, его срезают. Чем полнее ядро, тем меньше излишка — и
тем ближе модели: при четырёх начинках расхождение падает до 3 %.

Отсюда вывод, который стоит записать прямо: **сим-референс с пустым листом
проверять нельзя** — там он моделирует не маки, а «что будет, если намотать
весь лист». Владелец это заметила первой («это же не маки»). Проверочная
раскладка — 4 и 6, с полным ядром.

### Раскладка 6 «futomaki-full-core»: одно число подтвердилось, модель — сломалась

Её не прогоняли раньше, а она — единственная проверка того, на чём стоит
стенд: **закрывается ли лист 38,7 T за один оборот, когда ядро полное.**

**Подтвердилось: `nori_turns = 1.139`** (геометрически 1,26). Это прямое
независимое подтверждение однооборотной модели стенда. Арифметика сходится:
сечение 93,3 T² → R = 5,45 T → обхват 34,2 T → лист 38,7 T даёт 1,13 оборота.

Но сам прогон развалился: нори рвётся на 6 кусков (зазор 0,67 T = **8,8×**
шага частиц — это настоящий разрыв, не артефакт растра: в чистых раскладках
отношение 1,1–1,2×), 8 складок, `stable: false`.

**Проверил гипотезу «листа не хватает» — она неверна.** Прогнал те же начинки
на листе 58 T (в 1,5 раза длиннее; для этого в `run.py` добавлен флаг
`--sheet`, а домен X1 теперь выводится из длины листа, раньше был зашит под
38,7 T). Стало **хуже**: 18 кусков нори вместо 6, 15 складок вместо 8.
Значит дело не в длине листа.

**Настоящая причина — нечем смягчить.** В раскладке 6 начинки занимают
58,8 T² против 33,7 T² риса, то есть **64 % сечения**. Ядро (R 4,87 T)
почти достаёт до внешнего радиуса (5,32 T) — рису остаётся оболочка в
полтолщины, `rice_area_ratio` падает до 0,68–0,74. Нори ложится не на рис,
а прямо на жёсткие бугристые начинки, и тонкая лента теряет устойчивость.

У настоящего футомаки начинка — 30–40 % сечения, рис есть со всех сторон.
Раскладка 6 задумывалась как диагностика числа оборотов, и своё число она
дала; но как ролл она нереалистична, и требовать от неё устойчивости нельзя.

**Что с этим делать:** нужна раскладка 7 — футомаки с реалистичной долей
начинки (~35 % сечения, начинки помельче, рис кругом). Она и должна стать
проверочной для однооборотной модели вместо шестой.

### Побочно починено в `run.py` (обе реализации)

- Комментарии по-русски **внутри тел кернелов** роняли gstaichi: он
  перечитывает исходник через `inspect.getsourcelines`, и на кириллице
  токенизатор падал с `Non-UTF-8 code starting with '\xb2'`. Всплыло только
  на новом пути кода. Переведены на английский — по правилу проекта
  «код и комментарии по-английски» их там и не должно было быть.
- `X0, X1` были зашиты под лист 38,7 T. При более длинном листе ролл уезжал
  за границу расчёта, и прогон завершался вхолостую с кодом 0, без метрик
  и без единого кадра. Теперь `X1` выводится из `L_SHEET`.

### Открытое расхождение: сим даёт ~2 оборота там, где стенд даёт 1

Свести в одну таблицу то, что получилось, и назвать вещи своими именами:

| раскладка | доля начинки в сечении | `nori_turns` | нори цела |
|---|---|---|---|
| 1 пустой | 0 % | 3,11 | да |
| 2 тамаго | 11 % | 2,61 | да |
| 4 четыре | 23 % | 2,25 | да |
| 6 футомаки | **64 %** | 1,14 | **нет** |

Один оборот сим выдаёт только там, где ролл раздут до нереальных 64 %
начинки — и там же разваливается. Во всех работающих раскладках выходит
**2,2–3,1 оборота**, а стенд рисует один. Это не мелочь и не разброс метода.

Разбирать это **до** уточнения толщин нельзя, потому что обе величины,
из которых считается число оборотов, — толщина листа `w` и толщина
намазки `T` — сейчас стоят на глаз и **одинаковые в стенде и в симе**
(`W_NORI = 0.12 T` в `run.py` — та же 0,6 мм, что и в `index.html`).
Сим унаследовал допущение стенда и потому не может его опровергнуть.
Владелец задала этот вопрос 26.08 первой.

Порядок: (1) толщины по источникам; (2) пересчёт площади сечения и обхвата;
(3) только потом — раскладка 7 (реалистичный футомаки, ~35 % начинки)
и сверка числа оборотов.

### Раскладка 7 «futomaki-real» — расхождение подтвердилось на чистом прогоне

Собрана как то, чем раскладка 6 не является: тамаго, лосось, огурец, авокадо,
креветка — **14,5 T² начинки против 33,7 T² риса, 30 % сечения**, рис кругом.

Прогон чистый по всем признакам: нори цела (1 кусок, зазор 1,2× шага),
`wrinkles: 0`, `stable: true`, сохранение 0,976, ⌀ **4,1 см** — размер
настоящего футомаки. То есть модель здорова, и её ответу можно верить.

Ответ: **1,85–1,91 оборота** (`layers_predicted` 1,853, `nori_turns_geom`
1,914). Метрика `nori_turns` = 2,44 завышает — она считает пересечения луча
и приплюсовывает подвёрнутый слой; ориентироваться надо на первые две.

Арифметика сходится независимо: сечение 52,8 T² → R 4,07 T → спираль от
R ядра 2,95 до 4,07, средний обхват ≈ 22 T, лист 38,7 T ⇒ 1,76 оборота.

**Вывод, который придётся принять: строгий один оборот — это упрощение.**
Целый лист нори (19,3 см) вокруг футомаки ⌀ 4,1 см (обхват 12,9 см)
физически даёт **полтора-два оборота**, а не один. Излишек — тот самый
клапан, который ложится снаружи внахлёст и на срезе почти не читается,
потому что прижат и слился.

Сейчас в стенде стоит `oneTurn: true, flap: 0` — излишек **отрезается**.
Для хосомаки (полулист, 9,7 см) это верно: обхват тонкого ролла ⌀ 2,5 см —
7,9 см, полулист даёт 1,2 оборота, почти встык. Для футомаки на целом
листе — неверно: там честный нахлёст в половину-целый оборот.

Что делать: развести хосомаки и футомаки как две базы с разной длиной листа,
вместо одной «суши» с обрезкой. *(✅ Сделано 26.08: базы `hoso` и `futo` с
физическим `sheetCm`, `oneTurn` снят — `docs/journal.md`.)*

### Рабочая гипотеза: расхождение по виткам — это неверная толщина слоя риса

Один и тот же `T = 1.0` (рис 5 мм) стоит и для тонкого, и для толстого ролла.
Но у футомаки слой риса заведомо толще, чем у хосомаки, — и если это учесть,
расхождение исчезает само:

| вариант | рис, T² | сечение, T² | ⌀, см | оборотов |
|---|---|---|---|---|
| рис 5 мм, без начинки | 33,7 | 38,3 | 3,5 | 1,76 |
| **рис 5 мм + начинка (как сейчас)** | 33,7 | 52,8 | 4,1 | **1,50** |
| рис 8 мм + начинка | 53,9 | 78,6 | 5,0 | 1,23 |
| рис 10 мм + начинка | 67,4 | 96,0 | 5,5 | **1,11** |

(обороты по внешнему обхвату — нижняя оценка; по средней спирали ~на 20 % больше)

При толстом слое риса целый лист закрывает футомаки **за один оборот**, нори
внутрь не заходит, и требование владельца («стыкуются нори внешне, сама на
себя внутренно не заходит») выполняется без всякой обрезки. При тонком —
не выполняется никак, хоть какой кинематикой.

Это ровно то, о чём владелец спросила 26.08: «толщину нори, сладкого теста
и лаваша проверь». **Гипотеза, не факт** — числа 8 и 10 мм здесь взяты как
пробные. Подтверждать или опровергать её надо толщинами по источникам,
которые сейчас проверяются; `T` и `w` не имеют ссылок ни в стенде, ни в симе.
*(✅ Подтверждена в тот же день источниками: SUZUMO — рис 7 мм у хосомаки,
12 мм у футомаки; `docs/geometry-audit.md`, «Толщины по источникам».)*

### Поправка к таблице баз (26.08.2026) — прежние числа устарели

Таблица в разделе выше расходится с кодом и больше не годится как задание
агентам. Актуальное (проверено прямо в `index.html`, `1 ед. = 5 мм`):

| База | T (намазка) | w (лист) | L, ед. | L, см | оборотов |
|---|---|---|---|---|---|
| Суши-ролл | 1,00 | 0,12 | 38,7 | 19,3 | 3,10 |
| Рулет | 1,00 | **1,70** | **56,9** | **28,5** | 2,50 |
| Лаваш | 0,45 | **0,20** | **48,4** | **24,2** | 4,50 |

Что изменилось и почему:

- **Рулет** пересчитан 26.08 (бисквит 2 мм → 8,5 мм, крем 4 мм → 5 мм);
  прежние `T 0,8 / w 0,52 / L 40,2` относятся к вдвое уменьшенной базе.
- **Лаваш: `w` в таблице стоял 0,44, в коде — 0,20**, отсюда и «L 107,6»
  вместо настоящих 48,4. Число 107,6 попало в задания агентам как верное
  («для лаваша 107,6 было верно») — это неправда, лист лаваша более чем
  вдвое короче. Все выводы про лаваш, опирающиеся на 107,6, надо
  перепроверять.

Сами `T` и `w` источниками по-прежнему не подтверждены — проверяются.

---

## Поправка 9 (26.08.2026, вечер) — приговор mat-chain был неверен

Поправка 8 объявила mat-chain отработанным отрицательным результатом. Это
неправда, и ошибка была моя, тройная.

**1. Цифры были из промежуточного прогона.** Я прочитал `out/metrics_*.json`,
пока агент ещё работал; он перезаписал их спустя пять минут. Итоговые:

| | mat-chain (итог) | mat-chain (что я процитировал) | mat-sdf |
|---|---|---|---|
| сохранение | **0,960 / 0,968 / 0,968** | 0,949 / 0,958 / 0,957 | 0,951 / 0,965 / 0,971 |
| кусков нори | 5 / 2 / 7 | «2 / 3 / **16**» | 1 / 1 / 1 |
| частиц вне контура | 0 сбежавших | «104 / 1002 / 617» | 0 |
| складок по циновке | **0 / 1 / 1** | не смотрел | 0 / 0 / 0 |

По сохранению массы mat-chain **не хуже, а местами лучше** mat-sdf. И по
собственному отчёту автора попадает в формулу площади на 1 % против 2–6 % у
старого референса.

**2. Я перепутал цвета на картинке.** Написал «нори порвана на куски, зелёные
обрывки разбросаны». На срезе зелёного три: нори `(26,62,44)` — тёмная,
огурец `(86,178,62)` — яркий, авокадо `(152,202,92)` — светлый. Разбросанные
зелёные пятна, которые я принял за обрывки нори, — это **огурец и авокадо**.
Нори на `out/material_4.png` идёт одной непрерывной спиралью.

**3. Механизм я выдумал.** Написал «на стыке планок кривизна скачком меняет
знак, нори защемляется между соседними планками». В mat-chain **нет жёстких
планок со стыками**: это лагранжева цепочка узлов с ограничением на
минимальный радиус изгиба (`MAT_RBEND = 0.5 T`) и сервоприводом давления.
Объяснение описывало модель, которой не существует.

**Про `nori_torn`.** Флаг ставится по порогу `2,5 × шаг частиц`,
унаследованному от `../reference`. Автор mat-chain сам пометил его как
слишком строгий для листа, намотанного в 2,5 оборота: настоящего разрыва на
кадрах нет, это растяжение шага в тугой спирали. Проверено глазами —
так и есть.

**Проверил и отбросил ещё одну свою гипотезу.** Заметил, что у `--hold`
(пауза в конце) разные умолчания: mat-sdf `1.0`, mat-chain `0.0`, и оба
гонялись на дефолтах. Похоже на нечестное сравнение. Перепрогнал mat-chain
с `--hold 1.0`: сохранение чуть лучше (0,956 / 0,970 / 0,971), но складок на
раскладке 4 стало 16 вместо 3. Причина не в этом.

### Что действительно различает две модели

**За mat-sdf:** срез заметно чище. Начинки держат форму, контур круглый.
У mat-chain огурец и авокадо размазываются в ядро, а на раскладке 2
половина тамаго выдавливается вперёд и остаётся на столе (6,7 % вне
контура) — это настоящий дефект, автор его задокументировал.

**За mat-chain:** радиус ролла должен был **получаться из контакта**
(сервопривод давления по измеренной реакции), а не задаваться. Это
принципиально честнее прописанной аналитической дуги mat-sdf. Но автор
честно пишет (README, п. 5), что полностью так не заработало: свободная
цепочка либо схлопывается, либо уезжает плоской лентой, поэтому пришлось
задать мишень по длине дуги и оставить свободным только радиус. Сам
называет это «шагом назад от задания и главным кандидатом на переделку».

**Вывод: выбор не закрыт, я объявил его рано.** Сегодня mat-sdf даёт
картинку, которой можно пользоваться, и на неё опираются калибровочные числа.
mat-chain — более честная постановка с незакрытой реализацией. Правильный
следующий шаг — не выбрасывать его, а доделать эмерджентный радиус.

---

## Поправка 10 (26.08.2026, поздний вечер) — медиана прятала дефект mat-chain

Поправка 9 отменила приговор mat-chain и опиралась на два числа: сохранение массы
и попадание радиуса в формулу площади. Судья очной ставки прогнал обе модели заново
(улики — `sim/reference2/out/judge/`; в git этой папки нет, прогоны остаются локально), и картина уточнилась.

| раскладка | | mat-sdf | mat-chain |
|---|---|---|---|
| 1 | Rout **медиана** / предсказание | 3,44 / 3,49 | **3,52** / 3,49 |
| 1 | Rout **максимум** | 3,57 | **5,22** |
| 2 | медиана / максимум | 3,71 / 3,83 | 3,67 / **4,54** |
| 4 | медиана / максимум | 3,98 / 4,07 | 4,00 / **4,64** |
| | сохранение массы | 0,954 / 0,965 / 0,971 | 0,958 / 0,967 / 0,964 |
| | кусков нори | 1 / 1 / 1 | **3** / 1 / 1 |

**Что подтвердилось из поправки 9.** Сохранение массы у mat-chain действительно не
хуже — 0,958 / 0,967 / 0,964 против 0,954 / 0,965 / 0,971, разница в пределах шума
самого метода. Нори на 16 кусков не рвётся. Это остаётся в силе.

**Что я упустил.** Я процитировал **медианный** радиус (в формулу попадает на 1 %) и
не посмотрел на максимальный. А максимум у mat-chain на 32–49 % выше медианы, и на
кадре видно почему: **хвост листа не завёрнут** — лежит плоско на столе, тянется от
ролла вправо на несколько единиц, ядро полое. Ролл круглый «в среднем», но не
замкнут. Медиана — та самая статистика, которая этот дефект скрывает.

То есть mat-chain проваливается не там, где я сначала сказал (не рвёт нори и не
теряет массу), а в другом: **не доводит скрутку до конца**. Правильная метрика для
«ролл это или клякса» — максимум, а не медиана, и в отчёт по обеим моделям надо
брать оба числа.

**Победитель сведён в `sim/reference2/`** — mat-sdf плюс три вещи, взятые у mat-chain:
ограничение `R_fold` сохранением площади, `wrinkles_mat` (порог складки задаёт бамбук,
а не постель) и `mat_bend_violations` по всей ленте.

Туда же добавлена проверка, которой не было ни у кого: **`pressed_ok`**. Она ловит
случай, когда циновка заканчивает прогон в 0,93 T (4,7 мм) от ролла с нулевой площадью
контакта — а все прежние флаги такой прогон пропускали как здоровый (`stable`, складок
0, нори цела, порядок в ядре сохранён, 0 сбежавших частиц). Проверяется зазор и доля
пролёта, реально касающаяся ролла; для квадратного прижима порог свой, потому что
круглый ролл внутри квадрата структурно касается только четырёх граней.

---

## Поправка 11 (26.08.2026, ночь) — пересчёт толщин: арифметика чистая, приёмка получена в единственном режиме, где она выходит

**Не выдержал.** Обе адверсарные проверки — отрицательные. Переименование единиц безупречно, но приёмочные числа (`nori_torn False`, `stable True`) существуют только при штатных 16 000 частиц на сетке 240, то есть при **ppc 1,90 — ниже минимума MPM в 2 частицы на клетку**. Один шаг измельчения в любую сторону — и лента рвётся.

### Главное: развилка «предсказание или артефакт» закрыта — артефакт

Раскладка 1, сетка 240, **E = 25** (прочтение B, нори мягкая, «починки» нет), меняются только частицы:

| частиц | ppc | зазор, U | порог | рвётся |
|---|---|---|---|---|
| 16 000 | 1,90 | 1,9517 | 0,1435 | да |
| 22 014 | 2,23 | 1,9806 | 0,1222 | да |
| 30 000 | 2,61 | 1,8608 | 0,1047 | да |
| **44 444** | **3,18** | **0,0636** | 0,0860 | **нет** |
| 64 000 | 3,81 | 0,0555 | 0,0717 | нет |

Разрыв клапана, ради которого `E_NORI_W` подняли в шесть раз, **исчезает от одних частиц при нетронутом E**. При ppc ≥ 3,2 оба прочтения проходят и различаются на 0,02 U (E = 150 → 0,0413; E = 25 → 0,0636). Подъём E не восстановил мембрану — он поднял порог численного разрушения. Контрольная строка автора «22k частиц: не шаг частиц» упёрлась ровно в тот шаг, которого не хватило: переход лежит между ppc 2,6 и 3,2.

**Починка не переживает измельчения сетки при штатных частицах.** E = 150, сетка 400, 16k (ppc 1,14) → зазор 1,1713, **рвётся**, 13 разрывов, 8 из них под рисовой постелью, где дырка физически невозможна; вернуть ppc (44k) → 0,0421, цела. При совпадающем ppc ≈ 1,9 картина одинакова на сетках 240 и 400 → управляет **ppc, а не dx**. Прогоны на HEAD без правок копии: 240 держит, 320 / 400 / 400(повтор) / 400(`--threads 1`) рвутся (зазоры 0,777 / 1,095 / 0,512 / 1,171 при пороге 0,1435). Контроль на старой геометрии (`git show 21f4246`): 240 и 320 держат, 400 рвётся (0,395) — чувствительность была и до правки, но правка сдвинула отказ на шаг раньше и увеличила щель втрое. Строку README «1.66x finer grid: not the grid» надо пометить: она измерена при E = 25, то есть **до** починки.

### Что прошло проверку

Геометрия не поехала: `LAYOUTS` байт-в-байт равен `21f4246`; все 10 мест с голым `T` в родителе `d3c7b29^` отображены верно (`T`→`T_RICE`, `T+W_NORI`→`H_SHEET`), подмен нет. Начинки в миллиметрах те же (tamago 12,0 × 10,0 мм, замерено по облаку частиц, не по словарю). Rout сошёлся точнее, чем до правки: 4,064 / 4,530 / 4,630 против предсказанных 4,093 / 4,510 / 4,622 (−0,7 % / +0,4 % / +0,2 %). `escaped = 0` везде, минимальный запас до clamp-бокса 4,86 U по x и 0,197 U снизу. Раскладка 7 (реалистичный футомаки), которую раньше не гоняли, отрабатывает.

### Измерительный код поехал вслед за толщинами, и приёмка этого не увидела

1. **Растр рисует нори вчетверо толще, чем она есть.** `run.py:657` и `:681`: `rad = 0.6 * (max(hp, nori_dy) if c == CLASS_NORI else hp) / px`. Ветка `max` молча перевернулась: было `nori_dy` 0,060 > `hp` 0,049 → лента 97–101 % истинной площади; стало `nori_dy` 0,010 << `hp` 0,057–0,065 → **349 / 332 / 413 %**, связных кусков нори в карте 1 → 9 / 46 / 1. Всё, что читается с карты классов (и картинка для владельца), унаследовало смещение.
2. **Планка разрыва уехала сама.** `torn = max_gap > 2.5 * info['nori_dx']` (`run.py:1130`), а `nori_dx ≈ hp = sqrt(площадь / n)` (`run.py:301`): утолщение риса подняло площадь и смягчило порог на 17 % (L1: 0,1222 → 0,1435). Это ещё и не критерий разрушения: 2,5 шага = 150 % деформации, модели повреждения нет.
3. **Порог складок ослаб на 21 %** (`run.py:798`, `WR_KAPPA_MIN = 1/H_SHEET`, 0,893 → 0,704), предел изгиба `mat_bend_limit_R_T` 0,56 → 0,51 U. Со **старым** порогом на том же облаке L1 даёт 12 складок против 16 — то есть из скачка 0 → 16 порог объясняет 4, остальное — форма ленты. Числа по обе стороны правки несравнимы.
4. **Складки и `mat_bend` не сошлись по шагу времени.** E = 150, L1: CFL 0,30 / 0,15 / 0,075 → складок 16 / 4 / 0, нарушений изгиба 7 / 3 / 0 при почти неизменном зазоре. Разделение чистое: разрыв — пространственный (частицы), складки — временные (dt).

**До и после, все пять раскладок** (авторские дампы 15:29–15:33 против HEAD; 2, 3, 5 автором после правки не гонялись):

| | сохранение | складки | mat_bend |
|---|---|---|---|
| L1 | 0,9512 → 0,9570 | 0 → 16 | 0 → 7 |
| L2 | 0,9651 → 0,9678 | 3 → 8 | 1 → 4 |
| L3 | 0,9498 → 0,9567 | 0 → 6 | 0 → 6 |
| L4 | 0,9705 → 0,9730 | 2 → 14 | 3 → 7 |
| L5 | 0,9714 → 0,9758 | 1 → 5 | 2 → 5 |

Плюс не входившее в приёмочный список: `core_order_preserved` True → **False** на раскладке 4 (огурец выходит на обод без обёртки над ним, видно на `out/material_4_sk.png`), `wrinkle_ok` False везде, `turns_match_formula_best` False на всех трёх и хуже на 4 и 7.

### Что чинить *(→ issue #60)*

1. **Поднять базовое число частиц или ввести floor по ppc** — проверка на старте, падающая при `dx / nori_dx < 3`. Вся приёмка получена при ppc 1,90. Заодно: `nr = max(2, int(round(W_NORI / hp)))` (`run.py:316`) при W_NORI = 0,02 «заслуживает» второй ряд только с ~300 000 частиц — поперечное разрешение ленты зафиксировано на двух рядах навсегда, это надо записать рядом.
2. **Снять `E_NORI_W = 3.0` как «починку» и переписать шапку `MATERIALS`** (`run.py:147–162`): строку «E=150 grid 240 gap 0.069 <- E*w restored: fixed» заменить развёрткой по частицам и назвать причину — недоразрешённость. Правка сократила изгибную жёсткость ленты (E·w³) в 36 раз, восстановив только мембранную половину; `sim/KINEMATICS.md:107–111` прямо запрещает лечить складки жёсткостью.
3. **Починить штамп нори** (`run.py:657`, `:681`): нужен анизотропный штамп (вдоль ленты по `dxn`, поперёк по `nori_dy`) либо явное разделение «толщина ленты» и «шаг выборки». Заодно перемерить `BG_HOLE_T = 0.35` — комментарий на `run.py:546` сам просит об этом.
4. **Перекалибровать `WR_KAPPA_MIN` и `mat_bend_limit_R_T`**, а обе метрики пометить как несошедшиеся при CFL = 0,3 (см. п. 4 выше) или гонять калибровку при CFL 0,075.
5. **Расширить приёмку**: `core_order_preserved`, `wrinkle_ok`, `mat_bend_violations`, `turns_match_formula_best` — либо в список, либо в док с объяснением, почему не считаются.
6. **Переписать `torn` как критерий деформации разрушения** или переименовать: сейчас `nori_torn False` не доказательство целости — даже в принятом прогоне p99 вытяжки 17 %.
7. Мелочи: `run.py:1138` требует сохранения ≥ 0,97, гейт `run.py:1824` — ≥ 0,95, L1 = 0,9570 сидит между; комментарий раскладки 7 обещает «~30 % filling», по её же числам 21,6 %; `rice_area_ratio` 0,856 → 0,755 — это разрешение растра, не физика (читать `rice_area_ratio_ref`, 0,856 → 0,808); `WR_NOSE_T` в счётчики складок вообще не входит (маска `ok`, `run.py:903`, использует только `WR_EDGE_T`); «LAYOUTS не тронуты» верно про текст, но `base_y = H_SHEET` подняла все начинки 1,12 → 1,42 U.

### Рулет (спираль): по виткам сошёлся, и это ничего не доказывает *(живые хвосты — issue #63)*

Новая папка `sim/spiral-roll/` (копия `sim/spiral/`, +67 строк, раскладки 1–7 не тронуты). Прежние наработки устарели: все три прототипа считали рулет на `w_wrap = 0,52`, их README-числа (`turns 2.742`, `Rout 4.417 T`) к толщине 1,70 U отношения не имеют. Аналитика для `U = 5 мм, T = 1,0, w = 1,70` → шаг 2,70 U, `L = 56,9 U`, **Rout 6,997 U (⌀ 70 мм), 2,499 витка**.

**Витки укладываются в ±0,25 во всех тринадцати прогонах, худший промах −0,27 — включая прогон, где 19,8 % среза дыра, обёртка развалилась на три куска, а контур груша.** Проверка по виткам не различает годный рулет и негодный: витки — заметённый угол, он почти не чувствует воздух между витками. Различают `Rout` и доля пустоты. По ним при унаследованном `press = 0,60` (подбирался под лист 1,52 U) рулет **не сходится**: радиус +6,5 %, воздуха 15,5 %. При `press = 1,2` — 2,508 витка против 2,499, Rout 7,009 против 6,997; на сетке 400 — 2,528 / 6,992, воздух 2,4 %, то есть устойчиво к разрешению. **Оговорка, без которой это подгонка:** `press = 1,2` подбирался по `Rout` и воздуху, поэтому совпадение `Rout` — фитинг одного параметра руки, а не независимое подтверждение формулы. Обратный контроль честный: `press = 1,2` на старой геометрии пережимает (крем J = 0,891).

Спираль нарядная при `press ≥ 1,2` (`out/material_8_p12g400.png`, `out/material_11_p12.png`): 2,5 витка бисквита — главный узор среза, крем белыми прослойками; зеркало маки, где узор даёт рис. Не получилось: обёртка рвётся во всех прогонах, включая старую геометрию, и **не сходится к нулю по сетке** (0,539 → 0,527 при сетке в 1,33 раза мельче), трещина растёт от внешней грани изгиба внутрь — подпись изгибного разрушения, а не солверного шума. Ядро схлопывается (`hole_r_mean_T = 0` везде), витки совпали с формулой при `r0 ≈ 0`, а не при 0,25 и не при 1,8. Найден и отдельный дефект: `seed_r_core = 0.45` при этой толщине геометрически невозможен (якобиан внутри −2,0, внутренняя половина витка отображается сквозь ось) — то же самое, что `R0 = 0,25 U` на стенде.

### НЕ ПРОВЕРЕНО

- **Модуль упругости нори не измерен** — какое прочтение верно, прогонами не решается. Но формулировка «источника у нас нет» неверна: `docs/papers.md` — Jung / Yoon / Matsukawa 2022, *Food Hydrocolloids* 124, 107223, аннотирована как источник модулей оболочки; ~~DOI не проверен~~ DOI проверен 26.08 (резолвится, том 124 подтверждён), но полный текст закрыт и чисел в аннотации нет — доступ решает владелец, развилка живёт как issue #58. `docs/simulation-research.md` уже задаёт нори «почти нерастяжимой» — аргумент против прочтения B, независимый от прогонов.
- **`E = 6,0` бисквита и `0,6` крема ничем не подтверждены**, источника в `docs/` нет. Что трещина обёртки не численная — проверено; её величина — нет.
- **Реального среза для сравнения нет**; полая ли сердцевина у настоящего рулета — не проверено. Лаваш не гонялся, влияние `taper` на витки не проверено, ревизия `press` для несладких баз не сделана.
- Раскладка 4 при 44k частиц всё ещё рвётся (зазор 0,6451) — ей нужно больше; где её порог, не измерено.
- Улики скептиков — `/tmp/sk/out/`, `/tmp/out/`, `/tmp/old_run.py` — **временные**, в репозиторий не коммитилось ничего; `sim/reference2/out/metrics_*_sk*.json` лежат локально и в git не идут.
---

## Подворот доводят не до начинки, а до дальней кромки риса (31.08.2026)

Владелец увидела в прогонах лаборатории хвостик нори внутри ролла, с рисом по обе
стороны от него, и сказала, что так быть не должно. Она права, и ошибка нашлась —
она количественная, а не в самом движении.

**Где ошибка.** `sim/mat-sdf/run.py`: `S_FOLD_MARGIN = 1.0` (конец зоны начинок плюс 1 T)
и `FOLD_CAP = 0.45` — дальше 45 % листа край не заводится никогда. То же правило
продублировано в `sim/judge.py:53`. То есть лист складывается примерно **вдвое**:
кромка приземляется в середине, а весь остаток листа наматывается уже снаружи неё.
Отсюда и погребённый крючок, и 1,3–1,4 оборота вместо одного.

**Как на кухне.** Край ведут **до дальней кромки риса** — до границы «рис / голая
нори», то есть на **88–95 % листа**. Одиннадцать источников, японских и английских,
говорят это почти дословно одинаково:

| Источник | Куда ведут край |
|---|---|
| 白ごはん.com | 「向こうの寿司飯の端（海苔の空白部分）まで**一気に**」 |
| MAKIZUSHI倶楽部 | 「**海苔の手前の端がすし飯の奥側の端に合わさるように**」 |
| 銀座わたり (хосомаки) | 「**上のシャリと海苔の境目ギリギリに**持ってくる」 |
| oisiiryouri | 「向こう側の**「のりしろ部分」と、巻きすの手前を合わせるように**」 |
| OSUSHI STUDIO | 「向こう側の酢飯の端に合わせるように一気に…**「一回転」で巻き切ることが大切**」 |
| makesushi.com | «until the edge is in line with the “space” on the other side… **a single seam**» |

Перелёт эти же источники называют ошибкой по имени: 「すし飯をのせていない海苔を越える
失敗」. Недолёт на **половину листа** они не рассматривают вовсе — настолько он вне
рассмотрения.

**Критерий приёмки заменяется.** Строка «ближний край оказался внутри (подворот)»
описывает не то, что происходит на кухне. Вместо неё:

1. ближняя кромка нори **приземляется на дальнюю кромку риса** — в окно «граница риса …
   конец нориcиро»; недолёт и перелёт оба брак;
2. ближняя и дальняя кромки **риса** сходятся торцами в шве, а не обнимают нори;
3. по лучу из центра нори пересекается **ровно один раз**, кроме дуги шва длиной
   в нориcиро (1,5–3 см у футомаки, 0,5–1 см у хосомаки);
4. ближняя кромка нори лежит **в стенке**, под клапаном, — не между начинкой и рисом.

**Что из этих четырёх реализовано (сверено 01.09, issue #113).**

| | в `sim/judge.py` | в игре |
|---|---|---|
| 1 — посадка в окно | ✅ `landing_frac`, `landing_ok` (`fold_landing` из `sim/fold.py`) | ✅ `sFold` — граница петли `TAU·(R+P)` |
| 2 — рис к рису торцами | ❌ **измерить нечем**, см. ниже | ⚠ снято построением: в кольце (#132) постель непрерывна, два конца — одна лента |
| 3 — одно пересечение нори по лучу | ✅ `nori_crossings` → `cross_rays_med`, `single_layer_ok` (заведено 01.09) | ✅ сторож практики «Нори не уходит спиралью внутрь риса» — 0,00 доли лучей при допуске 0,02 |
| 4 — кромка в стенке под клапаном | ✅ `edge_in_wall` (заведено 01.09) | ❌ клапана в кольце нет; понятие требует пересмотра |

**Критерий 2 не реализован не по забывчивости.** В `.npz` лежат координаты и класс частицы,
но НЕ её исходное положение на листе, а «торцы риса» — это два конца исходной постели. После
скрутки отличить их от любых других частиц риса нечем. Чтобы критерий стал измеримым, прогон
должен сохранять исходную координату вдоль листа — аналог `nc`, который у нори уже есть. Это
правка `run.py` и пересъёмка всех прогонов, то есть отдельная задача, а не работа судьи.

**Что показали новые критерии на замороженных прогонах (01.09, 10 прогонов).** Оба красные, и
оба подтверждают числами то, что этот файл до сих пор описывал прозой:

| | результат |
|---|---|
| `cross_rays_med` | **2–3** пересечения по лучу вместо одного, максимум до 12 |
| `single_layer_ok` | **False во всех десяти** |
| `edge_in_wall` | True в 4 из 8 (у L1 начинок нет, критерий неприменим) |

То есть «целый лист на ролле такого диаметра даёт два слоя с лишним» — не оценка, а замер.
Прогоны переснимать под это не надо: они и заведены как характеризация того, что есть.

**Что менять не надо.** Само движение подтверждено заново и полностью: подъём края,
начинка под пальцами, одно быстрое приземление, встречная тяга. Исправленная модель
(циновка под листом с самого начала, без свободного пролёта) остаётся верной — меняется
только расстояние, на которое ведут край.

**Поправка того же дня: стенд НЕ согласован — я проверила не то.** Сначала здесь стояло,
что расходится только симуляция. Это неверно, и ошибка была в мерке: сторож практики мерил
`spreadEnd` — докуда размазан РИС (89 %), — и принимал это за то, куда приземляется КРАЙ НОРИ.
Величины разные, диапазоны случайно совпали, проверка загорелась зелёным и подтвердила ложь.

На деле формула живёт в **трёх** местах и всюду одна: `play/model/geometry.js` («let sFold =
Math.min(0.45 * L, …, end + 1.0)»), `sim/mat-sdf/run.py`, `sim/judge.py`. Замер на живом
стенде: подворот садится на **14,3 % листа** у хосомаки и **23,1 %** у футомаки — то есть
**стенд ошибается сильнее лаборатории**, а не меньше. Владелец увидела хвостик и в игре;
она была права и здесь. Верно из прежнего только то, что в стенде не рисуется нори самого
подворота, — но это отрисовка, а не место складки.

**Чего в источниках нет** (и чего поэтому не надо изображать знанием):

- где кромка оказывается **после** скрутки — все инструкции обрываются на «шов вниз,
  дай отлежаться». Пункты 1–4 выше — выкладка из «один оборот + нориcиро снаружи»,
  прямой цитаты на них нет;
- описания среза со стороны нори: про срез говорят только «круг или квадрат»;
- **термина для первой складки**: 巻き始め и 巻き込む — обиходные слова, приём не назван.
  芯 в источниках — это твёрдая начинка-ось (огурец), а не сердечник из риса;
- **внутреннего хвостика нори** — ни как нормы, ни как дефекта, ни советов, как его
  избежать. Названные по имени дефекты другие: はじける (лопнуть по шву), ほどける,
  具が偏る, 断面がつぶれる;
- **поведения риса на сгибе**: ни залома, ни клина. Есть только «не дави зерно» и
  наблюдение, что у ближнего края и у шва рис получается **тоньше**, а не собирается
  в клин — и с этим борются, поднимая дальний край.

**Расхождение, которое надо держать на виду.** 銀座わたり про футомаки говорит иначе:
「具の端に持っていくイメージで」 — до края начинки, и про 中巻き 「全体の1/3のあたりに」.
Тот же источник про хосомаки говорит «до границы риса и нори», то есть противоречит
себе. Толковать это как «край заводят только за начинку» нельзя, но и умолчать нельзя.
