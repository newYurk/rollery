# Окружение офлайн-референса sim/ (запуск — только sim/lab/lab.sh, он активирует sim/.venv).
# Снято 17.09.2026 с работающего окружения и проверено против него же:
# `uv pip install --dry-run --offline -r sim/requirements.txt` → «Would make no changes».
# Сама папка .venv в git не лежит: это рецепт, чтобы её можно было убрать и собрать заново,
# не вспоминая версии.
#
# Python 3.12.13 (CPython под управлением uv), uv 0.11.8. Собрать:
#   uv venv --python 3.12.13 sim/.venv
#   uv pip install --python sim/.venv/bin/python -r sim/requirements.txt
#
# Прямые зависимости кода sim/: gstaichi 4.6.0 (симуляция на GPU), numpy, matplotlib, pillow.
# Остальное — их зависимости; закреплены тоже, иначе через месяц соберётся другое окружение.
annotated_types==0.8.0
cffi==2.1.1
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
dill==0.4.1
fonttools==4.63.0
gstaichi==4.6.0
kiwisolver==1.5.0
markdown_it_py==4.2.0
matplotlib==3.11.1
mdurl==0.1.2
numpy==2.5.2
packaging==26.3
pillow==12.3.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pygments==2.21.0
pyparsing==3.3.2
python_dateutil==2.9.0.post0
rich==15.0.0
six==1.17.0
typing_extensions==4.16.0
typing_inspection==0.4.4
