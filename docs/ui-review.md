# Ревью вёрстки и адаптива стенда «Ролльня»

**25.08.2026, агент-дизайнер.** Прогон `index.html` (сборка с пазлом, формами и safe-area) в изолированном Chrome на семи viewport'ах; скриншоты — `docs/ui-review/<viewport>-<экран>.jpg` (72 файла, 4,5 МБ). Визуальный язык — тёмный фон, циновка, чипы, диск среза — **не трогаем**: он работает, владельцу нравится играть. Ниже только вёрстка, адаптив и цели касания.

Замеры `layout()` по viewport'ам (лист с включённым предпросмотром):

| viewport | лист (w×h) | кнопки y | чипы y | диск среза | ячейка тарелки |
|---|---|---|---|---|---|
| 375×667 iPhone SE | **244×183** | 473 | 527 | 284 | 112 |
| 390×844 iPhone 15 | 342×360 | 650 | 704 | 304 | 165 |
| 430×932 Pro Max | 382×439 | 734 | 788 | 335 | 185 |
| 834×1194 iPad портрет | 520×598 | 944 | 998 | 624 | 269 |
| 1194×834 iPad альбом | 467×350 | 640 | 694 | 405 | 182 |
| 844×390 телефон альбом | **−125×−94** | 196 | 250 | 85 | 78 |
| 1440×900 десктоп | 520×416 | 706 | 760 | 452 | 223 |


> **Скриншоты — только локально.** 72 файла ревью (4,5 МБ) лежат в `docs/ui-review/` на диске, но в git не попадают: это отработанная улика, а не действующий документ — находки и спецификация ниже самодостаточны. Если понадобится показать картинки, правильный способ — один самодостаточный HTML-отчёт со встроенными изображениями (как `docs/reports/kinematics.html`) или артефакт.


## 1. Находки

*(Снимок 25.08 — по этим находкам раскладка была переписана в тот же день по
спецификации §2 (`docs/journal.md`, 25.08). Живые остатки: панель иконок —
issue #21, растянутый лист — #23, проверка на живых устройствах — #77.)*

Серьёзность: **Б** — блокер (пользоваться нельзя), **С** — серьёзно (мешает играть или выглядит сломанным), **К** — косметика.

| # | viewport | экран | что не так | сер. | скриншот |
|---|---|---|---|---|---|
| 1 | 844×390 (телефон альбом) | все | `availH` отрицательный → лист −125×−94, `getSpreadTex` получает отрицательный размер, кадр падает — **чёрный экран** с включённым предпросмотром или в пазле; без предпросмотра лист 180×94, кнопки и чипы занимают всю высоту | **Б** | `844x390-lay.jpg`, `844x390-lay-nopreview.jpg`, `844x390-puzzle8-lay.jpg` |
| 2 | 375×667 (SE), любой телефон с safe-area | раскладка | Лист 244×183 (с safe-area 47/34 — ещё меньше): предпросмотр 136 px + два ряда чипов 122 px + кнопки + циновка съедают всё; лист — главный объект — получает остаток. Класть начинки в полосу высотой 183 px неудобно, патчи 2 единицы → 9 px | **Б** | `375x667-lay.jpg`, `375x667-safe-lay.jpg` |
| 3 | 375–430 портрет | после реза (стенд) | Четыре кнопки в ряд: «Нарезать на 6» обрезана до «Нарезать на» / «арезать на 6», «Ещё начинки» и «Новый лист» упираются в края кнопок. Текст рисуется без проверки ширины | **С** | `375x667-revealed.jpg`, `390x844-revealed.jpg`, `430x932-revealed.jpg` |
| 4 | 375, 390 портрет | пазл, 6 кусочков | Ряд из шести целей шириной во весь экран, а стрелки ‹ › стоят на x = 12 и W − 48 — **перекрывают первый и последний диск** | **С** | `390x844-puzzle8-lay.jpg`, `375x667-puzzle8-lay.jpg` |
| 5 | 375, SE с safe-area | раскладка | Подпись циновки «↑ потяни циновку вверх — скрутить» шире самой циновки (280 px) — обрезается с обеих сторон | **С** | `375x667-safe-lay.jpg`, `375x667-puzzle-lay.jpg` |
| 6 | все телефоны | раскладка | Два ряда чипов по 40 px: цель касания < 44 px, подписи 9,5 px (минимум для чтения на телефоне — 11). На SE второй ряд подпирает счётчик «срезов» (647 против 661) | **С** | `375x667-lay.jpg` |
| 7 | 834×1194, 1440×900, 1194×834 | раскладка | Планшет и десктоп используют телефонную колонку: лист ≤ 520, предпросмотр 116 px, чипы 40 px с подписями 9,5 px, по бокам пустота 150–460 px. На iPad альбом лист сплющен до 467×350 (0,75 высоты от ширины) | **С** | `834x1194-lay.jpg`, `1440x900-lay.jpg`, `1194x834-lay.jpg` |
| 8 | 834×1194, 1194×834, 1440×900 | пазл: сравнение | Диски 150 px (один срез) и **64 px** (шесть кусочков) на экране 1194–1440 px — цель нечитаема; подписи «цель / твой» прижаты к левому краю экрана (x = 16), в 400–600 px от рядов; кнопки на `btnY` из раскладки — между вердиктом и кнопками дыра 400–650 px | **С** | `1194x834-puzzle8-compare.jpg`, `1440x900-puzzle8-compare.jpg`, `834x1194-puzzle-compare.jpg` |
| 9 | 390×844, 375×667 | пазл: сравнение (один срез) | Два диска 150 px и дыра до кнопок ~450 px; дискам есть куда расти (до 167 px при двух в ряд или 260 px один над другим) | К | `390x844-puzzle-compare.jpg` |
| 10 | 375–430 | скручено | Доска шире экрана: `len = 0,86·W`, доска = len + 52 → на 375 px касается краёв, на 390 — 1,5 px от края | К | `390x844-rolled.jpg` |
| 11 | 375–430 | скручено / после реза | Экран на 60 % пуст: ролл и одна кнопка; ролл мог бы быть крупнее (радиус ×1,3), подпись «тапни там, где резать» — ближе к роллу | К | `390x844-rolled.jpg` |
| 12 | 375×667 | тарелка → кусочек | Подпись «кусочек 3 из 6 · тапни, чтобы закрыть» ложится на номера «5 6» затемнённой сетки | К | `375x667-plate-big.jpg` |
| 13 | все | раскладка | Подсказка на двух строках при W < 400: строки 54/70 px упираются в область предпросмотра/цели (запас 8 px); при трёх иконках и «стенд» заголовок в порядке | К | `390x844-lay.jpg` |
| 14 | все, где включён предпросмотр | раскладка | «живой срез · 3,0 витка» справа от диска: на телефоне 60 px пустоты слева, композиция не по центру | К | `390x844-lay.jpg` |
| 15 | 1194×834 | тарелка | 6×1 ряд: диски 182 px, сверху и снизу по 300 px пустоты — ряд можно поднять до 260 px | К | `1194x834-plate.jpg` |
| 16 | safe-area (симуляция 47/34 и 0/21/47/47) | все | Верх и низ сдвигаются верно (заголовок, иконки, подсказка, счётчик). **Не учтены `SAFE.left/right`** для листа, кнопок и чипов в альбоме: ширина считается от `W`, а не от `W − left − right` | К (после фикса 1 — С) | `375x667-safe-lay.jpg`, `844x390-safe-lay.jpg` |
| 17 | все | верхняя панель | Пять иконок 34–38 px при высоте 36 px — цель касания ниже 44 px; на телефоне ряд иконок съедает 190 px из 390 | К | `390x844-lay.jpg` |
| 18 | все | контраст | Подсказки `#b8ad95` на `#171713` — контраст ~7:1, норм; подписи чипов `#a79d86` 9,5 px — 5,5:1 при мелком кегле читаются плохо; счётчик `#6f6754` — 2,9:1, ниже AA (это служебный текст, допустимо, но лучше 4,5:1 или убрать) | К | — |

Что **уже хорошо**: диск среза после реза на телефонах (284–335 px — читается), ритуал и раскрытие, тарелка 2×3 на портрете, чипы как объект (глиф + подпись), верхняя панель после safe-area, перенос подсказки на две строки, кнопка-ряд на 520 px на планшете выглядит уместно, режим сравнения на телефоне с тремя кусочками (`375x667-puzzle6-compare.jpg`) — эталон того, как должен выглядеть остальной пазл.

## 2. Спецификация адаптива

Одна система: **отступы 8 / 12 / 16 / 24**, радиусы 10 (иконки) / 12 (кнопки, чипы) / 14 (циновка), **типографика три размера**: 17/700 заголовок, 15/600 кнопки и вердикт (вердикт 22/700 — единственное исключение), 13/500 подсказки, подписи, счётчик; подписи чипов — 11 минимум. Высота касания везде **44** (кнопки, чипы, иконки панели: 40×40 с зоной 44).

### Брейкпоинты

Считать по обоим измерениям; содержимое — в рамке `cw = W − SAFE.left − SAFE.right`, `ch = H − SAFE.top − SAFE.bottom`.

| режим | условие | компоновка |
|---|---|---|
| **P** телефон портрет | `cw < 600 && ch ≥ 500` | одна колонка (как сейчас), но с бюджетом высоты — ниже |
| **L** телефон альбом | `ch < 500` | две колонки: лист слева, панель справа |
| **T** планшет / узкий десктоп | `cw ≥ 600 && ch ≥ 500 && cw < 1100` | две колонки: лист + боковая колонка 260 |
| **D** широкий десктоп | `cw ≥ 1100` | то же, боковая колонка 320, блок центрирован, макс. ширина блока 1040 |

### P — телефон портрет (бюджет высоты, лист в приоритете)

Порядок распределения `ch`: панель `52` (заголовок 26 + подсказка одна строка 13 px на y = 44; **две строки только если** `cw < 400`, тогда 68) → снизу вверх: отступ 12 → **чипы 44 + подпись 16 = 60, один ряд**, горизонтальный скролл свайпом, если `n·52 > cw − 24` (с «затуханием» по краям 16 px); второй ряд — только если после всего лист ≥ `1,0·sw` → кнопки 44 → отступ 12 → циновка-ручка 40 → отступ 8 → **лист**: `sw = cw − 32`, `sh = clamp(остаток, 0,8·sw, 1,15·sw)`.
Если остаток < `0,8·sw` — предпросмотр/цель **не занимают отдельную полосу**, а становятся накладкой в правом верхнем углу циновки: диск 88 px с полупрозрачной подложкой (в пазле — ряд кусочков по 44 px в верхней полосе циновки). Отдельная полоса над листом (диск 116, ряд 84) — только при `ch ≥ 800`. На SE это даёт лист 343×275 вместо 244×183.
Подпись циновки: если `measureText > w − 24` — «↑ скрутить».
Ряд кнопок: макс. **3** в ряд, ширина `cw − 32`; четвёртый элемент (🔗) уходит иконкой в панель; текст кнопки — `fitText(label, w − 20)`: 15 → 13 → 12 → обрезка с «…».
Счётчик «срезов» — в панель справа от подсказки 11 px или скрыть при `ch < 700`.
Ролл: `len = min(cw − 80, 640)`, доска `len + 52` всегда внутри `cw − 16`.
Стрелки уровней: не в углах экрана, а **в строке подсказки**: «‹ Уровень 8 · 3 нач. · 6 кус. ›» — стрелки как две зоны касания 44×44 по краям строки; ряд целей — ширина `cw − 32`, диски `min(84, (cw − 32 − 8·(k−1)) / k)`.

### L — телефон альбом (`ch < 500`)

Две колонки без предпросмотра над листом. Левая: лист `sh = ch − 52 − 8 − 32 (ручка) − 8`, `sw = min(1,3·sh, 0,55·cw)`; ручка 32 под листом. Правая (`cw − sw − 48`, ≥ 260): предпросмотр/цель 96 px сверху, под ним кнопки в два ряда по две (высота 40), под ними чипы сеткой 6×2 по 36 px без подписей (подпись выбранного — одной строкой под сеткой). Диск среза после реза: `min(0,8·ch, 0,4·cw)`, кнопки справа от него колонкой. Тарелка 6×1, ячейка `min((cw − 64) / 6, 0,5·ch)`.

### T / D — планшет и десктоп

Блок из двух колонок, центрированный: левая — лист `sw = min(560, cw − колонка − 64)`, `sh = min(ch − 52 − 24 − 44 − 40 − 32, 1,15·sw)`, не ниже `0,9·sw`; под листом ручка 40, под ней ряд кнопок шириной `sw` (макс. 3), ниже чипы **одним рядом 52 px с подписями 12 px** (12 чипов × 58 = 696 — влезает при `sw ≥ 520`; иначе два ряда по 6). Правая колонка (260 в T, 320 в D): предпросмотр 200 px (T) / 260 px (D) с подписью под ним по центру; в пазле — цель 200/260 для одного среза или сетка 3×2 из 84/100 px для кусочков; под ней «‹ Уровень N ›» и «⟳ Другой», «🔗». Экран сравнения — в тех же координатах: ряд «цель» и ряд «твой» по `min((cw − 160) / k, 0,26·ch)` (шесть кусочков на 1194 → 172 px, на 1440 → 213 px), подписи «цель / твой» слева от ряда на расстоянии 16 от первого диска, вердикт под рядами, **кнопки через 24 px под вердиктом**, а не на `btnY`. Один срез: два диска `min(0,3·cw, 0,45·ch)` (iPad портрет — 250, десктоп — 400). Тарелка: `cw ≥ 900` — 6×1 (ячейка `min((cw − 80) / 6, 0,5·ch)`), иначе 3×2.

### Общее

- Диск среза после реза: `faceSize = min(0,78·cw, 0,6·(ch − 52 − 44 − 40))` — как сейчас, но от `cw/ch`.
- Все `x` считать от `SAFE.left`, все ширины — от `cw`.
- Никаких отрицательных размеров: `layout()` в конце делает `sw = max(sw, 120); sh = max(sh, 90)` и при нехватке высоты сначала выключает полосу предпросмотра, потом второй ряд чипов, потом переносит подсказку в одну строку.
- Оверлей кусочка на тарелке: затемнение 0,92 и подпись **над** диском.

## 3. Приоритеты (эффект / стоимость)

1. **Бюджет высоты и альбом телефона** (находки 1, 2, 5, 16): защита от отрицательных размеров, лист в приоритете, предпросмотр-накладка при `ch < 800`, компоновка L, `cw/ch` с safe-area — **~2,5 ч**. Снимает оба блокера.
2. **Кнопки** (3): `fitText`, максимум три в ряд, 🔗 в панель — **~1 ч**.
3. **Две колонки на планшете и десктопе** (7, 8, 15): режимы T/D, предпросмотр и цель в боковой колонке, сравнение в тех же координатах — **~3 ч**. Самый заметный эффект для iPad, с которого пришёл скриншот владельца.
4. **Экран сравнения** (8, 9): размеры дисков от `cw/ch`, подписи у рядов, кнопки под вердиктом — **~1 ч** (частично входит в п. 3).
5. **Чипы и стрелки уровней** (4, 6, 12, 13): один ряд 44 px со скроллом, подписи 11, стрелки в строке подсказки, оверлей тарелки — **~1,5 ч**.

Итого ~9 ч на всё; первые два пункта (3,5 ч) убирают блокеры и обрезанный текст.


---

## Контраст нори на тёмном фоне — исправлено 26.08.2026

**Замечание владельца:** у нарезанных маки нори сливается с тёмным фоном; вместо тёмного фона кусочки надо показывать на том же, на чём ролл сворачивали.

**Числа подтвердили.** Нори `#22342b` на фоне `#171713` — контраст **1,36:1** при минимуме 3:1 для не-текста: внешний контур ролла физически неотличим от фона, кусочек читался как пятно риса без границы. Номера кусочков 1–6 были на грани (3,2:1). При этом решение уже лежало в игре: у циновки `#c9a96c` контраст с нори **5,88:1**.

**Сделано по идее владельца:** кусочки лежат на той же доске, на которой сворачивали. Под тарелкой из шести — **одна общая доска** (читается как сервировочная, а не как шесть подставок), под одиночным срезом и в альбоме — своя плитка своей базы. У светлых обёрток кромку держит контактная тень по силуэту. Подключено везде, где показывается срез: живой предпросмотр, цель пазла, ритуал реза (доска ролла гаснет, доска среза проявляется), крупный срез, нарезка в движении, тарелка, кусочек по тапу, миниатюры и раскрытая запись альбома, оба ряда сравнения. Номера перекрашены — 4,8:1 на циновке.

**Проверено** на 430×932, 390×700 (короткий экран — режим накладки), 932×430 (ландшафт) и 1440×900; на всех трёх базах; раскладка нигде не поехала, шесть кусочков помещаются.

**Осталось на усмотрение владельца** *(→ issue #76)*:
1. У светлых обёрток контраст с их же подложкой невысокий (рулет на кремовой салфетке 1,80:1, лаваш 1,65:1) — кромку держит тень, выглядит как еда на салфетке. Если покажется вяло, можно дать им доску потемнее, но тогда она перестанет совпадать с той, на которой сворачивали.
2. Тарелка из шести на телефоне стала крупным светлым прямоугольником. Если кажется тяжёлой — притушить доску на 15 % к фону; контраст с нори останется 4,5:1.
3. Не делали: светлое кольцо-обводку вокруг ролла — дешевле, но на суши читается как вторая нори и врёт про блюдо.
