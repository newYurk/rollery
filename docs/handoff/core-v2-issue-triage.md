# Core V2 — triage ledger: 61 открытых issue

- **Статус:** ФИНАЛ, частично применено. Все стадии отработали чисто: 55 из 55 агентов,
  0 ошибок. По прямой команде «закрой лишние» ниже исполнена безопасная часть пакетов
  2 и 3 — см. «Что реально применено».
- **Дата:** 2026-09-04

## Что реально применено (2026-09-04, ночь)

**Закрыто 22** — все не зависят от исхода ревью контракта (`contractDependent: false`),
каждое с комментарием по формату статус/причина/источник/сохранено/условие возврата:
#3, #18, #34, #38, #45, #46, #51, #52, #54, #55, #56, #58, #62, #68, #71, #75, #77, #82,
#94, #103, #111, #128. DEFER-закрытия получили метку `отложено`.

**Придержано 14** — помечены `contractDependent: true`, ссылаются на ADR-001 как на новый
источник истины, а ADR-001 только что получил вердикт `FAIL`: #48, #53, #66, #70, #72, #80,
#83, #85, #125, #137, #145, #154, #155, #163. Закрывать после того, как контракт починен
(пакет 1) — не раньше, иначе закрытие будет ссылаться на несуществующее решение.

**Не тронуты по решению владельца** — #112, #60 остаются открытыми как описи (см. пакет 3).

**Не тронуты, потому что новые** — #168, #169, #170 созданы владельцем в ночь на 04.09
(erratum 002/003 и идея «угадай срез»), их не было в снимке 61 на момент старта триажа.

Открытых сейчас: **42** (было 61 на старте триажа; 39 старых + 3 новых).


- **Метод:** 8 тематических партий разобрали все 61 issue независимо (читая тело и
  комментарии каждого, не только заголовок) → сведение в родителей и конфликты →
  для каждого issue, предложенного «под нож», отдельный агент-защитник, которому
  разрешено вернуть его в активные, если что-то конкретное теряется → синтез решений
  владельца и пакетов применения.
- **Сырьё:** `runs/core-v2-handoff/raw/tri-result-final.json` (вне git, см. правило проекта).

## Как читать

| Пометка | Значит |
|---|---|
| подтверждено | защитник согласился, действие не изменилось |
| ИСПРАВЛЕНО | защитник заменил действие — в таблице уже новое |
| (пусто) | issue не предлагался под нож (KEEP ACTIVE / NEEDS…) |

## Прочитать первым

Параллельная адверсарная проверка контракта Core V2 (см. `docs/handoff/core-v2-validation-findings.md`)
вынесла **вердикт FAIL** — контракт нельзя реализовать как написано: несущий термин
`closure window` не определён нигде, кроме самих контрактных документов, и без него
не составить вход даже для F02 (единственного fixture первого PR с ингредиентом).

Из этого прямое следствие для триажа: **пакет 1 («Контракт: закрыть дыры Core V2»,
issues #57, #93, #74, #24, #17, #10, #73, #140) должен идти ДО, а не вместе с
остальными пакетами.** Начинать очистку доски с пакетов 2–5 при непринятом контракте
рискованно — минимум семь SUPERSEDE-закрытий (#72, #83, #145, #137, #125, #90, #36)
цитируют ADR-001 как источник истины, а он сам всё ещё `proposed` и только что получил
вердикт FAIL. Синтез ниже сам называет этот риск первым пунктом «честных рисков» —
он не пропущен, а осознанно принят с условием возврата на каждой карточке.

Вечером 03.09 (после того, как этот триаж стартовал) владелец добавила в контракт два
erratum — 002 (нет диагонали тел) и 003 (цвет риса отдельно от тел). Короткая ручная
проверка (см. последний раздел `core-v2-validation-findings.md`) не нашла у них
противоречий с находками триажа: `patch_rotated` (erratum 002) не тестируется ни одним
fixture — тот же класс дефекта, что и остальные, но не блокирует первый PR, поскольку
F01/F02 не используют поворот вовсе.

## Счёт

| Действие | Число |
|---|---|
| KEEP ACTIVE | 13 |
| NEEDS OWNER DECISION | 2 |
| NEEDS EVIDENCE | 1 |
| DEFER | 25 |
| SUPERSEDE | 8 |
| MERGE INTO PARENT | 9 |
| CLOSE AS NOT PLANNED | 3 |
| **Итого разобрано** | **61 из 61** |

Кластерное сведение (до защиты) оценивало **14**; итоговый пакет применения ниже даёт **16**.

## Решения владельца — 8 вопросов

Каждый вопрос отвечается одной строкой; ниже — рекомендация агента, не решение.

### 1. Core V2 alpha обязан явно отказывать диагностикой на материалах без своего контура и толщины (нори-лист, салат, паста, присыпка — 13 из 27 позиций каталога), а не молча рисовать их прямоугольным пятном с высотой?

**Варианты:**
- Да — честный отказ (unsupported), пока не появится ADR-002 про мембраны и массы
- Нет — рисовать прямоугольником как временное приближение, явно пометить условностью

**Рекомендация:** Честный отказ дешевле и уже есть прецедент: так же поступили с тэмаки, топологией урамаки и квадратом/треугольником в самом ADR-001.

**Разблокирует:** #74, #66, #53

**Цена простоя:** F01–F06 проверяют только твёрдые тела; кернел, который молча врёт про 13 из 27 позиций каталога, пройдёт зелёным, и дыра уедет в alpha незамеченной.

### 2. Подтверждаете список не-целей V2 alpha — тэмаки, топология урамаки, кадзари-маки, мини-роллы, сращивание листов, реальная деформация/разрушение, режим записанной руки — как отложенные до отдельных будущих ADR?

**Варианты:**
- Да, весь список как есть
- Да, но кроме одного-двух пунктов — назвать каких
- Нет, что-то нужно вернуть в alpha уже сейчас

**Рекомендация:** Да, весь список — по каждому пункту уже есть код или замер, показывающий, что альтернатива дороже: #124 (урамаки вывернут по-настоящему), #152 (ядро стало коробкой), откат варианта B 03.09.

**Разблокирует:** #145, #154, #137, #155, #48, #80, #34, #58, #55, #68

**Цена простоя:** Девять отложенных карточек не могут закрыться со ссылкой «ADR-001 подтверждён» — они простаивают в подвешенном статусе, а не в чётком «отложено».

### 3. Профиль формы куска на виде сверху (конус/веретено видно на срезе, но не на листе) и наложение начинок ярусом (RecipeV2.Patch.placement:'stacked', никем не ограниченное) — принимаем как явную временную потерю альфы, или задерживаем первый PR ради починки?

**Варианты:**
- Принять потерю, но записать её явно в контракте — не молчаливым выпадением, как уже пропало placement:'spread'
- Задержать первый PR, добавить недостающие поля сейчас

**Рекомендация:** Принять потерю, но только с явной записью в ADR-001 — иначе дыра унаследуется в F01–F06 hash так же тихо, как уже пропало значение placement:'spread'.

**Разблокирует:** #10, #140

**Цена простоя:** Клубника и креветка продолжают рисоваться на листе прямоугольником вместо конуса; одиннадцать источников по ярусам (#140) остаются недостроенной механикой без даты.

### 4. Зазор рисовой постели у стенок ролла (#138) — вопрос только картинки, или должен войти в геометрию модели (площадь, карта материалов)?

**Варианты:**
- Только вид — дешёвая правка рендера
- Геометрия и коридоры — модельная правка, входит в объём F01/F02

**Рекомендация:** Начать с вида: разблокирует #138 без риска для сроков первого PR; геометрическую версию заводить отдельно, если станет видно, что расхождение влияет на замеры.

**Разблокирует:** #138

**Цена простоя:** #138 (P0, фундамент) не получает единственного next step — агент не вправе сам выбрать между дешёвой и дорогой версией правки.

### 5. Оставляем spreadEnd = 0,88/0,89 (текущее осознанное назначение) или переходим на 0,905 (строгое чтение источников, одна доля на обе базы)?

**Варианты:**
- Оставить 0,88/0,89
- Перейти на 0,905

**Рекомендация:** Решить до первого принятого прогона F02 — после него число замораживается в fixture, и смена становится дорогой пересъёмкой всех эталонов.

**Разблокирует:** #109

**Цена простоя:** #109 не может закрыть самый срочный под-список чисел — тот, что входит прямо в F01/F02.

### 6. Кусок начинки тянется вместе с изгибающимся листом (композит гнётся), или лист несёт свою длину дуги отдельно, а кусок хранит форму и площадь и проскальзывает по листу?

**Варианты:**
- Композит: кусок растягивается вместе с листом
- Проскальзывание: кусок держит форму, скользит по листу (~2,4 мм)

**Рекомендация:** Без отдельного замера сжатия куска под скруткой гадать нельзя; если решение нужно прямо сейчас без замера — проскальзывание дешевле, оно не требует новой физики деформации, которую сам ADR и так выносит за рамки alpha.

**Разблокирует:** #134, #165, #113, #51

**Цена простоя:** Разброс площади куска (1,14×) остаётся необъяснённым, диаметр каждого ролла продолжает плавать от коммита к коммиту, а вариант B второй раз пробовать не на чем.

### 7. Чем игрок раскладывает начинку — кисть для всего, кисть только для риса и красок, или без кисти вовсе (поштучно, с улучшенными мобильными подсказками)?

**Варианты:**
- Кисть для всего
- Кисть только для риса и красок, куски по-прежнему поштучно
- Без кисти вовсе

**Рекомендация:** Кисть только для риса и красок — сохраняет позиционную точность кусков (нужна раскладке из решения A), но снимает проблему 9-пиксельной цели на телефоне для самого частого действия.

**Разблокирует:** #46, #17, #53

**Цена простоя:** Игра продолжает целиться в мелкие куски на телефоне; #46 не может выбрать окончательную таксономию нарезки.

### 8. Раз модель права, что положение группы начинок внутри зоны подворота не меняет срез, — показываем игроку саму зону, даём ему то, что действительно откликается (ярусы / порядок в стопке / живой срез при скрутке), или пока ничего не делаем?

**Варианты:**
- Показать зону подворота (UI-подсказка)
- Дать отклик через ярусы/порядок в стопке/конец оси
- Живой срез во время скрутки
- Пока ничего — до вехи «Оболочка и режимы»

**Рекомендация:** Пока ничего — веха и так заморожена собственным решением владелицы; но стоит зафиксировать это явным «нет, не сейчас», а не тишиной, чтобы родительская карточка не висела неопределённо.

**Разблокирует:** #139, #140, #94

**Цена простоя:** Три родственные карточки продолжают существовать как открытый вопрос без даже временного ответа «пока никак».

## Пакеты применения — по порядку

### 1. Контракт: закрыть дыры Core V2 до слияния первого PR

**Issues:** #57, #93, #74, #24, #17, #10, #73, #140

**Эффект:** Восемь разных «дыр контракта» сводятся в один новый трекер с чек-листом из пяти решений владельца (см. ownerDecisions). Ревьюер первого PR (F01+F02) смотрит в одно место, а не в восемь карточек; ни одна находка не теряется — каждая переезжает строкой с сохранённым обоснованием и адресом в коде.

**Обратимость:** Каждое слияние — отдельный closing comment со ссылкой на новый трекер; любую из восьми можно переоткрыть поштучно одним кликом. Сам новый трекер можно удалить без потери данных — весь текст остаётся в истории закрытых issues.

### 2. Закрыть решённое — без остатка

**Issues:** #72, #36, #83, #85, #145, #137, #125, #90, #71, #111, #82

**Эффект:** Одиннадцать карточек, чьи вопросы уже отвечены кодом, замером или решением владельца от 31.08–03.09, уходят с доски. Каждая несёт в комментарии, чем именно она заменена — конкретный пункт ADR-001, файл, коммит, а не общие слова.

**Обратимость:** Каждое закрытие называет условие возврата явно (например: «если состязательное ревью снимет инвариант 6 — переоткрыть»). Возврат — один клик на конкретную карточку, без каскада на остальные десять.

### 3. Заморозить отложенное

**Issues:** #128, #38, #46, #45, #34, #155, #48, #163, #154, #68, #58, #51, #55, #66, #3, #18, #53, #56, #70, #62, #80, #54, #103, #75, #52, #77, #94

**Эффект:** Двадцать семь карточек уходят в морозильник с меткой «отложено» и условием возврата в комментарии. is:closed label:отложено становится единственным местом, где искать «что мы решили пока не делать и почему». Открытыми из этой темы намеренно остаются только #112 и #60 — как опись семи и восьми свёрнутых задач, а не как работа на сегодня.

**Обратимость:** Тот же принцип: один клик на карточку возвращает её на доску, текст и числа никуда не делись — просто перестали быть на активном экране.

### 4. Актуализировать выживших

**Issues:** #165, #113, #134, #109, #112, #60, #79, #139

**Эффект:** Оставшиеся открытые карточки перестают ссылаться на закрытые номера, цитировать снятые числа или путать legacy-фикстуры (§9, F01–F08) с фикстурами Core V2 (F01–F06, core-v2-fixtures.md). #79 и #139 меняют смысл названия и становятся трекерами «показ на живых людях» и «чем игрок управляет», без новых номеров; #139 предварительно получает правку play/checks.js:377-383 (снять отменённый критерий mv<=25 и ссылку на закрытую постановку) — иначе он тащит на себе противоречащий контракту сторож.

**Обратимость:** Правки тела issue — обычные правки, GitHub хранит историю версий; откатить можно построчно.

### 5. Гигиена метки «ждёт решения»

**Issues:** #134, #110

**Эффект:** «ждёт решения» после этого стоит ровно на двух карточках плюс восьми пунктах чек-листа нового трекера — и каждый раз означает реальный, здесь перечисленный вопрос владельцу, а не протухший или решаемый агентом форк.

**Обратимость:** Метки возвращаются одним кликом; переименование ничего не удаляет.

## Честные риски этой чистки

- Весь план держится на контракте (ADR-001), который сейчас сам под состязательным ревью со статусом proposed. Если ревью существенно перепишет инварианты или список не-целей — минимум семь SUPERSEDE-закрытий (#72,#83,#145,#137,#125,#90,#36) и все восемь карточек, слитых в новый трекер контрактных дыр, нуждаются в пересмотре. Условие возврата записано в каждом комментарии, но это не защита от каскада, если правок много одновременно.
- Слияние восьми разнотемных карточек (#57,#93,#74,#24,#17,#10,#73,#140) в один новый трекер — моё собственное решение по читаемости доски, а не то, что буквально предлагали кластерный анализ (представлял это абстрактным «родителем», не обязательно новым issue) или защита (для каждой карточки по отдельности настаивала «держать именно ЭТУ открытой»). Риск — конкретная адресность каждой находки размывается внутри общего чек-листа.
- Поправки к #36 и #90 (снять с «активно» на «закрыть») пришли только из кросс-сверки на уровне кластеров, а не из отдельного адверсарного прогона защиты, который применялся к #74/#24/#17/#10/#73/#140/#139. С ними обошлись менее строго, чем с остальными шестью, которые тот же процесс перевернул с точностью до наоборот.
- Входные данные по #125 сами себе противоречат: обоснование защиты целиком говорит «SUPERSEDE верен», а поле итогового действия ошибочно называет «KEEP ACTIVE». Я доверилась тексту, а не полю, и точечно проверила фактическую часть (ARRIS=0 в play/model/geometry.js:437) — подтвердилась, — но сам этот сбой в данных говорит, что процесс не застрахован от механических ошибок расшифровки на входе.
- Решение оставить #112 и #60 открытыми как «описи» — моё, не владелицы. Если она хочет доску короче (14, а не 16), оба закрываются тем же движением, что и остальной пакет 3, без потери содержимого — это лёгкая правка после согласования.
- Синтез не перечитывал код заново по каждому из 61 пункта — доверился построчным адресам защиты и кластерного анализа. Точечная проверка (ARRIS, отсутствие боевых потребителей у facade, ссылка на #71 в geometry-audit.md, комментарий «#36» в puzzle.js:134) подтвердилась трижды из трёх — но это выборка, не полная пересверка.
- Двадцать семь карточек уходят в «отложено» одним пакетом. Если хотя бы в одной условие возврата написано неточно (устаревшая строка кода, не тот номер issue), она рискует остаться забытой дольше, чем открытая карточка с неверным приоритетом — «отложено» гасит видимость на активной доске сильнее.

## Предложенные родительские issues

### Дыры контракта Core V2: закрыть до слияния первого PR

**Дети:** #57, #93, #138

**Почему один issue:** ADR-001 проходит адверсарное ревью прямо сейчас, и семь находок триажа — это правки контракта, а не задачи. Ни одна не переживёт первый PR: F02 фиксирует размеры каталога численно после первого принятого запуска, F06 требует детерминированный hash карты материалов, и после слияния любая из этих правок означает пересъёмку всех fixtures. Семь находок: (1) closure window объявлен инвариантом 5 и на нём построены F02/F03, но ЧИСЛА у него в контракте нет — developer-brief требует объявить «выбранное closure window» в PR-заметке, то есть разработчик возьмёт его с потолка; четыре источника #57 (не ближе 20 мм от кромки, не заводить за середину листа, твёрдое-сердечник ближе к себе) — единственное, что у проекта на этот коридор есть, и вносить их надо как коридор для сверки, а не как подгонку. (2) Конец оси скрутки нигде в RecipeV2 не сериализуется (#93: патчи заданы uMm, поля «с какого конца крутим» нет), а он переворачивает узор наизнанку — сохранённый ролл откроется другим узором, тот же класс дефекта, что был с обёрткой. (3) Рис в materialMapOf — ветка «всё остальное» (#138); если он войдёт в карту материалов той же свалкой, дефект замёрзнет вместе с hash. (4) Локальная толщина риса не выражается вовсе: в контракте rice: { profileId } — один именованный профиль базы, а #17 и кадзари-механика требуют локального профиля. (5) Непомеченные догадки станут эталоном в F02 (#109): лист 105 мм, профиль рисовой постели, огурец 14 × 9,9 мм, R0, kappa, beta, tuckMin. (6) «F01–F08» теперь значит два РАЗНЫХ набора — §9 domain-contract (legacy) и core-v2-fixtures (F01–F06), из-за чего вопрос «F02 зелёный?» двусмыслен (#73). (7) F01 и F02 — хосомаки, а нарушение длины видно на футомаки (отношение рис/окружность 0,626 против 1,044 у хосомаки): первый PR может быть зелёным целиком, не тронув дефект #165, ради которого V2 и затевалась — надо либо втащить пустой футомаки в первый PR, либо честно записать, что F01/F02 к #165 не относятся.

**Готово когда:** Каждая из семи находок либо внесена правкой в ADR-001 / core-v2-fixtures / developer-brief, либо явно отклонена с записью причины в ADR — и это сделано ДО слияния PR с F01/F02.

**Судьба детей:** #57 и #93 закрываются со ссылкой: их живой остаток — ровно замечание к контракту, а вторые половины уже имеют дом (подсказка в UI — #110; ручка выбора конца оси — родитель «Чем игрок управляет»). #138 остаётся ОТКРЫТОЙ подзадачей: её первая половина (зазоры у стенок — только вид или ещё и геометрия и коридоры) — вопрос владельца, а не замечание к контракту. Находки из #109, #17, #73 и #165 идут строками в тело родителя, сами задачи живут своей жизнью.

### Ближайший показ на живых людях: снять всё за один заход

**Дети:** #79, #52, #77

**Почему один issue:** Три карточки просят одного и того же события и порознь не наступают. #79 — числа замеров 1–5 по HYPOTHESIS.md. #52 — тяга циновки и задержка жест→звук, где сама задача признаёт: отдельная поездка ради неё не окупается. #77 — вёрстка на живых устройствах: все проверки ui-review делались в эмулированном Chrome, а оба фикса 02.09 (#157, #158) пришли с телефона владельца. Порознь это три повода не ехать, вместе — один повод поехать. Замер дорожает со временем: объект замера 1 изменился (пиксельный вид по умолчанию, спрайтовые иконки, чиптюн, звуковая заставка, окно «что внутри»), а после #152 изменилась и картинка среза, по которой ставился замер 3. И #103 («зачем крутить второй ролл») стоит именно на этих числах — без них брейншторм будет спором догадок.

**Готово когда:** Показ проведён; в RESULT.md записан разбор по каждому замеру (1 — снят как несравнимый с прежними порогами: условия стенда другие; 3 — отложен до починки #165 + #134, иначе меряет дефект, а не гипотезу; 2, 4, 5 — сняты при выключенных 👁 и «Пазле»); тяга циновки и задержка жест→звук проверены руками тестера; раскладка снята на двух чужих телефонах.

**Судьба детей:** Все три закрываются со ссылкой. Дешевле не заводить новый номер, а переименовать #79: её список замеров и есть тело родителя, и она уже блокирует #103. Перед прогоном список к проверке вёрстки пересобрать по коду, а не брать таблицу ui-review §2 — это снимок 25.08, а раскладку с тех пор переписывали трижды (честная пропорция листа 31.08, верхняя панель 02.09).

### ADR-002: представление патча и свойства материала (после V2 alpha)

**Дети:** #74, #10, #53, #66

**Почему один issue:** Четыре карточки заморожены одной и той же строкой ADR-001 — «контентные и физические расширения не блокируются навсегда, но не добавляются до прохождения первых fixtures» — и требуют одного решения: как выглядит патч и где живут свойства вещества. #74 наполовину уже принят (классы rigid/soft/paste/brittle отменены 01.09, тезис «роль — свойство укладки» вошёл в контракт полем Patch.placement); остаток — мембрана и масса, то есть не-прямоугольные патчи. #10 — то же с другой стороны: профиль вдоль оси сделан (AXIAL в geometry.js входит в srCompute), остались три класса геометрии и пополнение каталога числами. #53 честно называет себя сменой ядра: в RecipeV2 Patch — прямоугольник в миллиметрах, маска требует правки контракта, а мазки уже вынесены в не-цели до отдельного ADR. #66 — пасты и присыпки: smearAt размазывает материал только при paint && crumbly, майо и джем остаются телом с высотой. Пока они стоят порознь, каждая выглядит отложенной по своей причине; причина одна, и размораживать их надо одним движением, а не по очереди.

**Готово когда:** ADR-002 написан и принят: не-прямоугольные патчи (мембрана/масса), свойства каталога вместо классов с выводимым наверху словарём, пасты и присыпки, вход каталога в kernel через data-adapter и способ выразить локальный профиль риса. После принятия под каждый пункт заводится своя задача с fixture; до принятия ни один пункт не делается.

**Судьба детей:** Все четыре закрываются со ссылкой, замеры переносятся в тело родителя (сектор огурца 14 × 9,9 мм и площадь 3,08 ед²; профили AXIAL; предикат smearAt как готовый рычаг; отмена классов с доводом «тамаго мягкий и при этом осевой»). Важно: #3 (обсыпка) и #18 (кунжут) сворачиваются НЕ в #66, а сюда же — иначе присыпки останутся висеть в закрытой отложенной карточке без хозяина. Мёртвые поля placementClass и compressibility в play/inverse/materials.js уходят мелочью в #109 и ADR не ждут.

### Чем игрок управляет, если подворот съедает положение

**Дети:** #139, #140, #94

**Почему один issue:** #139 переклассифицирован 02.09: модель права, положение группы внутри досягаемости подворота (замерено: до u ≈ 0,70) на срез не влияет, потому что в жизни не влияет. Но последний абзац той же карточки говорит прямо: «Оба варианта — решение владельца, не моё», и наблюдение владельца («двигаю ингредиент внизу листа — ничего не меняется на срезе») остаётся верным. Закрыть #139 в инвариант 5 ADR — значит закрыть геометрическую половину и потерять продуктовую: развилку «показать игроку зону подворота или дать ему то, что откликается». Кандидатов на «то, что откликается» уже четыре, и каждый лежит отдельной карточкой без хозяина: ярусы (#140 — одиннадцать японских источников и своё правило укладки: мягкое на твёрдое, мелко нарезанное вниз), живой срез во время скрутки (#94 — «математика позволяет, канва потянет»; он показывает, как подворот утаскивает начинку в середину, то есть объясняет отсутствие отклика вместо того, чтобы его подделывать), порядок в стопке и конец оси скрутки (из #93 — узор наизнанку получается физическим действием повара, а не кнопкой правки). Это не четыре задачи, а четыре ответа на один вопрос; обсуждать их порознь — трижды решать одно.

**Готово когда:** Владелец выбрала один ответ (или явно: ни один), выбор записан в docs/mechanics.md, и под него заведена ОДНА конкретная задача со своим критерием готовности.

**Судьба детей:** Все три закрываются со ссылкой, содержание переносится в тело родителя разделами-кандидатами: таблица ярусов по одиннадцати источникам и правила укладки из #140; техническая осуществимость живого среза (windRout по намотанной длине, ~30 кадров offscreen, вид слегка сбоку) из #94; замеренная досягаемость подворота, три отвергнутых пути и мягкий канал сторожа из #139. Четвёртым кандидатом приходит «конец оси скрутки» из #93. Дешевле не заводить новый номер, а переименовать #139: карточка уже на доске, и слова владельца лежат в ней. Метки: «ждёт решения» + «отложено», веха «Оболочка и режимы».

## Конфликты между партиями, разрешённые при сведении

- **#139, #140, #94** — Батч geometry закрывает #139 как снятую постановку, а батчи puzzle и ui откладывают #140 и #94 с условием расфиксации «когда решена продуктовая развилка #139». После закрытия #139 оба ребёнка указывают на закрытый номер, а сама развилка — по последнему абзацу #139 явно названная решением владельца, а не агента, — остаётся без карточки. Геометрическую половину действительно заменяет инвариант 5, продуктовую не заменяет ничто.
  **Решение:** Завести родителя «Чем игрок управляет, если подворот съедает положение» и закрыть в него #139, #140, #94 (дешевле — переименовать #139 в этот родитель). Геометрическая половина #139 уходит в ADR-001 инвариант 5 и F03/F04, как и предложено; продуктовая живёт в родителе с меткой «ждёт решения».

- **#90, #71** — Батч sim ставит #90 KEEP ACTIVE, но его же следующий шаг — «перенести текст в docs/simulation-research.md и закрыть issue как перенесённый». Одновременно #71 сворачивается В #90 как в родителя. Ребёнка нельзя свернуть в родителя, который сам растворяется: строка про два-три зерна попадёт в закрываемую карточку.
  **Решение:** Обе закрываются одним движением, и это CLOSE, а не KEEP: текст #90 плюс строка #71 переезжают отдельным разделом «Границы лаборатории» в docs/simulation-research.md, ссылка в STATE.md переставляется с issue на док, а устаревшая строка про приёмку (?check и fixtures #73) переписывается на F01–F06. В счёте открытых #90 не участвует; в вехе «Референс sim» остаётся одна открытая карточка — #60.

- **#73, #83, #85, #38, #112** — Батч arch закрывает #73, а закрывающие комментарии #83 и #85 передают работу именно в #73 («расширение play/checks.js на режим пазла», «молчаливый fallback betterFit — отдельная находка для проверок #73»). Плюс #38 ждёт возвращения пазла в работу. Три сироты указывают на закрытую карточку. Отдельно: пока legacy-набор называется F01–F08, вопрос «F02 зелёный?» означает два разных набора.
  **Решение:** Весь пазловый остаток собирается в #112 — единственную открытую опись пазла: молчаливый fallback betterFit (та же болезнь, что инвариант 5 запрещает ядру), PASS 0,72 → 0,82 по docs/noise-floor.md, цель касания ≥ 44 px во всех режимах и четыре уровня со shape 'square'/'triangle', ставшие сомнительными по инварианту 7. #38 сворачивается в #112. До закрытия #73 переименовать legacy-набор в L01–L07 в play/test/fixtures/fixtures.js, STATE.md и §9 — правка дешёвая, но её надо сделать сознательно.

- **#92, #70, #73** — Два батча рассуждают об одном файле противоположно. #73 закрывается доводом «play/checks.js помечен Reference then replace, вкладываться в удаляемый компонент нечего», а #92 остаётся активным, и в него же сворачивается #70 — то есть в тот же файл вкладываются дальше.
  **Решение:** Линия проходит не по файлу, а по тому, что стережёт мерка. Legacy остаётся единственным играющим движком на всё время разработки V2, и пункт 2 #92 (полоса краски шириной в зерно) стережёт обещание игры «рисуем и формой, и цветом», которого Core V2 не моделирует вовсе (ADR-001: «Не изменяет: UI, рендер, каталог»). Приёмочные fixtures для снимаемого ядра — вложение в удаляемое; мерки живой игры — нет. Оговорка при слиянии #70: числа 0,985 и 0,86 замерены на суперэллипсе, которого нет с 01.09, и в docs/shapes.md рядом лежит второй набор (0,96 / 0,81 / 0,89) — перезамерить до того, как что-либо закреплять.

- **#62, #137, #34** — #62 сворачивается двумя половинами: F → #138, H («форма как отдельная фаза с повторами прижима») → #137. Но #137 другим батчем закрывается как superseded, а его собственный остаток адресован будущему ADR о режиме руки, которого ещё нет. H переезжает в закрытую карточку.
  **Решение:** F → #138, как предложено. H идёт туда же, куда весь остаток руки: строкой в список «что обязан определить ADR о режиме руки» в ADR-001-erratum-001, а его единственный открытый носитель на доске — #34 (та же расфиксация: отдельный ADR после V2 alpha). Заводить родителя под руку не из чего: после закрытия #137 и слияния #62 открытых детей остаётся один.

- **#165, #134, #113** — Геометрия ставит #165 KEEP ACTIVE, P0, «исполнитель — F01», а #134 — NEEDS OWNER DECISION. Но STATE.md записывает, что развилка «растягивается ли КУСОК начинки вместе с изгибающимся листом» блокирует ОБЕ и прямо говорит «не начинать работу, не выбрав», а откат варианта B доказал замером, что порознь они не чинятся (дуга 1,235 → 1,012, но площадь куска 1,14 → 1,20 и некруглость хосомаки 59–68 % при потолке 30). Получается активная P0-задача, у которой собственное предусловие не отвечено.
  **Решение:** Вопрос задаётся ОДИН раз и записывается на обеих карточках: носитель — #134, #165 ссылается. Ближайший шаг #165 — только спецификационная половина: три симптома → поля FixtureReport (coveredLengthMm, uncoveredLengthMm, phantomLengthMm, uMin/uMax, seam) и mutation test «параметризация по дуге → пропорция номера бина», плюс замечание в ревью (находка 7 родителя по контракту). Для #113 отдельно: посадку подворота не меряет ни один fixture — критерии 3 и 4 (одно пересечение нори по лучу, кромка в стенке) вносятся в F01 численно, а не прозой. Метку «ждёт решения» с #165 и #113 снять: развилка у них общая с #134 и уже названа.

- **#36, #72** — #36 оставлен активным ради одного живого пункта — «decodePuzzle собирает руку из data.h[0..3] без проверки длины, короткий массив доходит до hd.air.toFixed(3)». Код это опровергает: play/modes/puzzle.js:135 зовёт handOf с комментарием «Рука из ссылки может прийти короткой или с мусором — handOf дополнит по полю (#36)», handOf в play/model/util.js:35–38 дополняет ПОЛЕ ЗА ПОЛЕМ с проверкой isFinite, а play/model/geometry.js:2325 несёт пометку «#36, правка 01.09» ровно над этим местом; там же реализован LRU (modelCaches.delete(modelCaches.keys().next().value)), хотя батч пишет «решено не чинить». Оставшийся пункт — позвать validateRecipe на боевом пути — это подключение фасада в legacy, то есть ровно та миграция на месте, за которую закрывается #72.
  **Решение:** #36 не KEEP, а SUPERSEDE/закрыть: порядок патчей → инвариант F05, turns как вход → инвариант 6, падение и LRU починены 01.09, неизвестный kind фильтруется, двойной wind отпал. Единственный остаток — фасад без боевого потребителя (проверено: evaluateRoll/createRecipe/deriveSheetLayout зовёт только play/test/domain/roll-facade-checks.js) — уже записан находкой 5 в закрывающем комментарии #72 и переезжает в шаг 6 плана миграции.

- **#145, #154** — Одно намерение записано дважды: #145 целиком и раздел 3 в #154 — это один и тот же список видов маки как условие переключения. Батч topology сам это заметил, но обе карточки трогает разными действиями.
  **Решение:** Остаётся #154 (отложена): её незаменимая часть — четыре неизмеренных утверждения практики («жёсткое ближе», «медленная скрутка сдвигает начинку», «шов вниз», «режут мокрым ножом»), величин под которые в модели нет вовсе. Раздел 3 переписывается: ждать тэмаки, кадзари и сращивания листов значит не переключиться никогда — они объявлены не-целями; критерием становится «F01–F06 зелёные и физика режимов общая». #145 закрывается, полнота ряда живёт в таблице не-целей ADR-001.

- **#3, #18, #66** — Два батча сворачивают живое содержимое (обсыпка от #3, кунжут от #18) в #66, а третий батч #66 откладывает. Если #66 позже закроют как «идею» — а метка «идея» на ней и стоит, — присыпки исчезнут вместе с ней.
  **Решение:** #66 не назначается носителем: он закрывается в родителя «ADR-002», и содержимое #3 и #18 переносится туда же отдельным пунктом «пасты и присыпки: кунжут, тобико, обсыпка снаружи». Носителем должен быть тот, у кого есть критерий готовности, а не тот, кто дольше висит.

- **#128, #24, #17, #51, #55, #80, #163, #155, #154, #54, #103, #45, #34, #60, #68, #112** — Двадцать пять предложений из шестидесяти одного — DEFER, при том что метка «отложено» стоит ровно на двух карточках (#103 и #45). Если отложенные остаются открытыми, доска после всей работы читается как 31 открытая, а не 14, и порог «≤20» не достигается ни при каком триаже. Батчи считают свой вклад в цель, не назвав это.
  **Решение:** Правило владельца решает это само: «открытая задача = следующий шаг + критерий завершения + причина делать СЕЙЧАС; всё остальное — отложенное, решение или заметка». У отложенной причины делать сейчас нет по определению, значит она не открытая задача. Отложенные закрываются с меткой «отложено» и с условием расфиксации в закрывающем комментарии; морозильник читается фильтром is:closed label:отложено, и ничего не теряется. Исключение можно сделать для двух описей — #112 (пазл, семь свёрнутых задач) и #60 (лаборатория, восемь), — если владелец хочет видеть их на доске: тогда счёт 16, а не 14. Закрывать ради счётчика, не двигая модель, при этом по-прежнему нельзя: ни одна из шести непотеряемых (#165, #113, #109, #134, #92, #144) не закрывается.

## Вопрос ярлыков

**Новая англоязычная таксономия (type:/status:/scope:/P0-P2): НЕ вводить.**

Считаю по фактическому употреблению на 61 открытой задаче: «ждёт решения» — 18, «идея» — 15, без метки вовсе — 19, «фундамент» — 5, «мелочь» — 5, bug — 4, «отложено» — 2, sim — 2, documentation — 1. Из этого видно, что сломан не словарь, а его применение. «Ждёт решения» стоит на 18 карточках, и восемь независимых проходов триажа нашли решение уже принятым на шестнадцати из них: #83 и #137 закрыты инвариантом 6 ADR-001, #46 — решением владельца 31.08 про кисть, #38 — замером в docs/noise-floor.md (там прямо написано «Поднять до 0,82»), #58 — прогонами по ppc, #125 — обнулением ARRIS 01.09, #90 вообще ничего не спрашивает, #76 нужен замер, а не решение. И та же метка ОТСУТСТВУЕТ на двух карточках, которые ждут именно её: #134 (растягивается ли кусок вместе с изгибающимся листом — единственная развилка, которую источники не решают и которая меняет диаметр каждого ролла) и #110 (кисть — исчезают перетаскивание, поворот, единица отмены). То есть фильтр «что от меня ждут» показывает сегодня 18 строк, из которых правдивы две, и обе не помечены. Второе: «отложено» стоит на двух карточках при двадцати пяти отложенных по существу — доска не отличает «делаем» от «вернёмся», а без этого различия порог ≤20 нечем измерить. Английский слой type:/status:/scope:/P0 не лечит ни то, ни другое: scope: дублирует пять уже разложенных вех (без вехи не осталось ни одной задачи), status: дублирует «отложено» и «ждёт решения», type: дублирует bug/documentation/enhancement/«идея», а P0 на доске из четырнадцати карточек с одним названным фундаментом (Core V2) сортировать нечего. Цена при этом реальная: перелейбить 61 задачу, развести два словаря с пересекающимся смыслом и потерять метки, у которых есть описания и которые владелец писала под свои вопросы. Русские метки — её инструмент чтения; менять инструмент, чтобы не чинить употребление, — это вторая неверная прослойка поверх первой.

Конкретные правки вместо новой таксономии:

- Снять «ждёт решения» с шестнадцати карточек, где решение уже принято, принято не владельцем или заменено замером: #3, #17, #38, #46, #53, #58, #68, #76, #83, #90, #103, #113, #125, #128, #137, #165. Оставить на #45 и #138.
- Поставить «ждёт решения» туда, где её нет, а ждут именно её: #134 (сейчас только «фундамент»), #110 (сейчас без меток вовсе) и новый родитель «Чем игрок управляет». После этого метка означает ровно пять карточек, и её можно читать как список к разговору.
- Ставить «отложено» на КАЖДУЮ отложенную карточку при закрытии (сейчас метка на двух из двадцати пяти). Это и есть механизм морозильника: is:closed label:отложено — единственный способ не потерять условие расфиксации и не держать его на активной доске.
- Пометить выживших без метки: #109 → «фундамент» (STATE.md называет аудит чисел продуктовой работой, а не гигиеной), #110 → «ждёт решения». После этого среди активных не остаётся ни одной карточки без метки.
- Не заводить P0/P1/P2, type:, status:, scope: и не переводить существующие метки на английский: приоритет уже выражен меткой «фундамент» плюс порядком в STATE.md, область — вехой.
- Ввести не метку, а правило для «идеи» (15 карточек): «идея» без «отложено» — по определению карточка без следующего шага и без причины делать сейчас, то есть кандидат на закрытие в родителя или в docs/ideas.md. Правило разбирает пятнадцать карточек без единого нового ярлыка.

## Переоспоренные вердикты — 11 вернулись или изменились

Каждый был предложен «под нож» в первом проходе; защита нашла, что теряется, и изменила действие.

- **#139** было `SUPERSEDE` → стало `KEEP ACTIVE`
  Теряется: Обязательство перепрописать сторожа `play/checks.js:377–383`. Сейчас он требует `mv <= 25` — то есть требует, чтобы зеркальная раскладка ОТЛИЧАЛАСЬ, — а это ровно отклонённая 02.09 гипотеза. Мерка вечно жёлтая за то, что модель права; комментарий над ней утверждает «решение не принято»; сообщение ссылается на `(issue #139)`. Ни один другой issue, ни один док, ни ADR-001, ни `core-v2-legacy-boundary.md` этого не несут. Закрытие оставляет в коде, который владелец видит в `?check` каждый день, отменённый критерий с ссылкой в закрытую задачу — приглашение будущему агенту «починить» модель обратно под отклонённую гипотезу.

Следующий шаг (один, завершаемый): в `play/checks.js:377–383` снять потолок 25 как критерий (либо развернуть в обратную сторону — внутри досягаемости подворота совпадение ожидается), переписать комментарий с «модель не знает и решение не принято» на «замеренное свойство кольца, переклассифицировано 02.09», ссылку `(issue #139)` заменить на `docs/design-core.md` + ADR-001 инвариант 5.

Критерий готовности: `?check` не называет это расхождением; в коде нет утверждения «решение не принято»; ссылок на #139 в production-коде не осталось.

После этого — и только после — #139 закрывается по supersede: геометрия → ADR-001 инвариант 5 и F03/F04; «показать зону подворота» → #57; «дать то, что откликается» → #140; веха «Оболочка и режимы». Закрывающий текст из предложения при этом использовать нельзя как есть: строку «остаётся, но с подписью…» надо заменить на «подпись перепрописана, коммит такой-то».

- **#74** было `DEFER` → стало `KEEP ACTIVE`
  Теряется: Единственная в проекте записанная и подкреплённая источниками обязанность: прямоугольный патч не выражает 13 из 27 позиций каталога (`cut ∈ {лист, паста, присыпка}`), и Core V2 обязан не рисовать их молча, а отказывать. Конкретно исчезает: (а) возврат `spread` в enum `Patch.placement` (ADR-001:68 — три значения против четырёх в §5.7 контракта) либо явная запись, что четвёртое значение отменено; (б) строка «мембрана и масса — материалы без собственной толщины и контура» в списке «V2 пока не моделирует» и соответствующий код `unsupported:` в таблице «Поведение вне модели»; (в) fixture или мутация, которая красит попытку подать такой материал как обычный патч — сейчас F01–F06 и весь список мутаций работают только на жёстких телах, так что V2 пройдёт зелёным, изображая дэмбу бруском 6 × 1,0. Ни adversarial-валидатор (ему запрещено поднимать мазки и scope), ни #66 (только массы, метка «идея»), ни #10 (профиль вдоль оси у тела), ни docs/ingredients.md (витрина, не roadmap) этого не несут. Шаг стоит минут, но исполним только пока ADR-001 в статусе `proposed`.

- **#24** было `DEFER` → стало `KEEP ACTIVE`
  Теряется: Обязательство перенести в контракт Core V2 уже выигранное геометрическое свойство: профиль рисовой постели зависит от координаты среза (`bed[i] = spreadAt(u, g, vSlice)`), кромка гуляет на ползерна. В ADR-001 рис задан одним `profileId` без оси `v`, а срез моделируется ровно один — рваность в V2 ненаблюдаема по построению, и ни один из F01-F06 не покраснеет, если она исчезнет. Держит её только сторож Т в `play/checks.js`, который граничный документ объявил не-oracle и «replace», и у которого нет mutation test — то есть он не проходит собственный критерий переноса. Отложить сейчас — значит проснуться после того, как F01/F02 напишут постель как функцию одного `u`, и заново доказывать то, что уже доказано замером 02.09.

Ближайший шаг (дешёвый, в окно ревью контракта): внести в `core-v2-legacy-boundary.md` и в fixtures два пункта — (1) профиль постели в V2 есть функция (u, v), `sampleSection` берёт его от `vSliceMm`, а не только от `rice.profileId`; (2) fixture с двумя вызовами при разных `vSliceMm`: координата кромки риса обязана расходиться не меньше чем на 0,4 зерна, mutation test — сгладить кромку, проверка краснеет. Критерий готовности: либо эти строки в контракте, либо явная запись в ADR, что V2 alpha намеренно отказывается от рваности и почему. После этого остаток (кромка на срезе, 0,35 против 3,50) уходит в «отложено» ровно с тем условием, которое триаж и назвал.

- **#17** было `DEFER` → стало `KEEP ACTIVE`
  Теряется: 1. Единственное место, где записано, что `riceDip` невыразим в контракте Core V2. Ингредиент есть в игре сегодня (`catalog.js:405-406`, палитра через `state.js:196`), он вычитает толщину постели, а `RecipeV2` даёт рису одно поле `rice: { profileId }` и `Patch` с неотрицательным `heightMm` и тремя значениями `placement`, ни одно из которых не означает правку профиля постели. Фикстуры F01–F06 этого не ловят: рисового патча нет ни в одной, а в `FixtureReport` нет поля профиля постели — реализация, молча выбросившая грядку и ложбинку, пройдёт зелёной. Адверсариальное ревью тоже не поймает: его устав ограничен восемью вопросами про длину листа и детерминизм и запрещает расширять scope. Отложить — значит дать ADR-001 стать `accepted` со списком не-целей, где локальный профиль риса не назван, то есть получить согласие владельца на потерю, о которой ей не сказали.

2. Сверка задачи с кодом от 01.09. Ни #17, ни `STATE.md:529` не знают, что главный рычаг задачи построен: обе записи всё ещё утверждают «осталась механика ввода» и «игрок в профиль не вмешивается никак». Отложить сейчас — законсервировать это расхождение до неопределённого срока и разбудить задачу по #110 с телом, которое врёт в центральном пункте.

3. Единственный носитель лабораторного вопроса из комментария 30.08: кривая w_срез/w_лист(w_лист, r, P) и минимальная выживающая ширина рисовой прокладки, плюс названное препятствие — укладчик кладёт постель прямоугольником постоянной толщины (`sample_layout`), профиля у неё нет. Больше нигде этого нет: #60 свернул в себя восемь sim-задач, но это пакет ремонта прибора (подворот, floor по ppc, стемп нори, `WR_K`, torn), а не список вопросов к нему; #92 прямо говорит, что её пороги — детекторы изменения `SMOOTH_R`, а не требования, то есть ничто не сверяет `SMOOTH_R` с реальностью. После 01.09 модель уже утверждает, что прокладки выживают, — и за этим утверждением нет ни одного замера. Этот пункт сам по себе на «сейчас» не тянет (упирается в #60), но исчезнуть вместе с задачей он не должен.

- **#10** было `DEFER` → стало `KEEP ACTIVE`
  Теряется: Единственная запись обязательства «профиль вдоль оси обязан быть в теле, в patchSRange И в отрисовке разом». Домен его получил 01.09, вид сверху — нет: `pieceTopSprite` (play/render/sheet.js:95) рисует полный прямоугольник `wPx × hPx`, `kA` туда не доходит, поэтому клубника-конус и хвост креветки на листе шире, чем на срезе. Мерки на это в `play/checks.js` нет ни одной, фикстуры F01–F06 отрисовку не покрывают вовсе и явно снимают с себя «реализм ингредиента», а `Patch` в ADR-001 — коробка в миллиметрах без формы. Плюс три дока (`glossary.md:87`, `domain-contract.md:694`, `STATE.md:404–405/489`) до сих пор утверждают, что кусок — призма. Отложив #10, теряем не идею «сделать красиво», а зафиксированное расхождение домена с картинкой, единственного его свидетеля и повод предъявить контракту Core V2, что прямоугольный патч отменяет уже оплаченную источниками работу по формам начинок.

- **#73** было `SUPERSEDE` → стало `KEEP ACTIVE`
  Теряется: Единственное в проекте требование, что изменение входа обязано менять результат. Приёмка Core V2 (F01–F06 + семь мутаций) состоит только из верхних границ и требований стабильности: F03 ограничивает изменение центра сверху (Δ = 0 проходит), F05 требует совпадения hashes при перестановке патчей, F02 требует стабильности центра. Ядро, в котором `uMm` не действует, проходит все шесть зелёным, и ни одна мутация его не красит; более того, измеренная причина #134 (полка сортирует по `u` и стыкует встык, поэтому действует ПОРЯДКОК) удовлетворяет F05 по построению. Сегодня чувствительность к положению меряется ровно в одном месте — пара `F02~сдвиг` в `play/test/probe.js` и `play/test/legacy/baseline-data.js:817`, — и она уже показала утрату: в `d78ed32` (#152, 02.09) `F02~сдвиг` ушёл 0.568 → 1, `F02~F05 рука` 0.9572 → 1, то есть сдвиг куска и смена почерка перестали менять срез, а сторож остался зелёным, потому что слепок характеризационный. Закрыть #73 по `core-v2-fixtures.md` — значит перенести набор сторожей минус этот, ровно в тот момент, когда legacy его потерял, а замена его не требует; и снять с доски единственную задачу, стоящую в STATE.md как критерий готовности этапа («полнота доказывается, а не заявляется»). Обязательство при этом никто не подхватывает: #134 несёт сам дефект, а не требование к приёмке V2, а `ADR-001` ещё `proposed` и не принят.

- **#140** было `DEFER` → стало `KEEP ACTIVE`
  Теряется: Единственное место, где написано, что значит `placement: 'stacked'` — значение, которое `RecipeV2` уже принимает, но ни один инвариант ADR-001 не ограничивает, ни одна фикстура F01–F06 не трогает (вход F05 задан НЕПЕРЕКРЫВАЮЩИМСЯ, а ярус рождается из перекрытия), и под которое в `Patch` нет обещанного самой же F05 поля `layer`/`placementOrder`. При DEFER контракт замерзает с висячим значением, и первый, кто дойдёт до перекрывающихся патчей, определит ярусы догадкой — при том что правило есть готовое и выводимое: «мягкое и листовое на твёрдое», «мелко нарезанное вниз» (`CANON_CRUMBLY`), потолок три яруса, несущая способность из `cut` и `wU` (тамаго держит формой при `stiff` 0,18, не твёрдостью). Это ровно тот дефект, который erratum-001 уже вырезала на соседнем поле `RecordedHand` — «неясное обещание без физического смысла, диапазонов и fixtures», — и здесь он остался незамеченным.

Конкретный следующий шаг (укладывается в комментарий и одну страницу, вехи не требует): подать erratum-002 к ADR-001 в форме erratum-001 — либо снять `'stacked'` из `Patch` на время V2 alpha (честно: ярусов в alpha нет), либо определить его, добавив `layer` и фикстуру F07 с ДВУМЯ ПЕРЕКРЫВАЮЩИМИСЯ патчами, на которой мутация «последний в массиве побеждает» наконец сможет упасть. Критерий завершения: в ADR-001 не осталось значения placement без инварианта и без фикстуры. Срок: пока ADR в статусе `proposed` и идёт состязательное ревью; после `accepted` и первого PR правка становится изменением принятого контракта.

- **#46** было `MERGE INTO PARENT` → стало `DEFER`
  Теряется: Якорь для трёх мест в живом коде, где #46 назван причиной текущего устройства: профиль `'ломтик'` и профиль `'четверть'` в `play/model/geometry.js` намеренно не приписаны ни одной начинке (`play/model/catalog.js:221–233` — «Форма ждёт #46»), и сторож списка форм в `play/test/practice.js:934` намеренно считает заведённые профили, а не применённые, обосновывая зазор словами «пока нарезку выбирает каталог, а не игрок (#46)». Закрытие #46 в #110 (веха «Оболочка и режимы», «ничего из математики её не ждёт») оставляет два подтверждённых источником профиля без потребителя и без владельца — прямо под #109, аудит мёртвых констант. Условие снятия отсрочки: развилка #110 решена (кисть для всего · кисть только для риса и красок · кисти нет). Требование к тому, кто когда-нибудь будет #46 закрывать: тем же коммитом перенаправить или снять эти три ссылки, иначе решение о нарезке потеряется в виде двух сирот в `CUT_PROFILES`.

- **#3** было `MERGE INTO PARENT` → стало `MERGE INTO PARENT`
  Теряется: Обязательство, зафиксированное дословно в коде (play/checks.js, дважды: «Когда #3 закроется — убрать развилку И здесь, и в разделе 3в»), и связь этого незамыкания с #165 (STATE.md прямо называет остаток проблемы урамаки частью #165: «рису нужно 1,3–1,6 витка, а кольцо кладёт один, и концы постели не встречаются торцами»). Merge только в #66 адресует лишь обсыпку; топологический/незамыкающийся хвост issue должен явно уехать в #165, иначе dead-code ветки в checks.js останутся без владельца, а #165 — без ссылки на то, что часть его контекста пришла из #3.

- **#70** было `MERGE INTO PARENT` → стало `MERGE INTO PARENT`
  Теряется: Если закрыть #70 слиянием в #92 как предложено: (1) обязательство перемерить и закрепить исполняемо различимость круг/квадрат/треугольник осядет в issue, чей заявленный checklist (рельеф риса / цвет / экран) прямо не включает эту тему и чей собственный текст её из своего scope исключает — велик риск, что при закрытии #92 по пунктам 2–3 (цвет, экран) пункт про форму просто забудут, ведь формально он там чужой; (2) теряется единственное упоминание исторического регресса (similarity одно время силентно не проверяла форму на пазл-уровнях, docs/shapes.md:219) как обоснования, почему нужна именно исполняемая проверка, а не текст в доке — без этого обоснования будущий читатель #92 не поймёт, зачем туда вообще приплетена форма среза; (3) более точный родитель — #62, который уже ревизован 01.09 на той же FACE_SETS-модели и с той же формулировкой «тело протухло» — теряется как связь, если слить в #92 вместо него.

- **#56** было `CLOSE AS NOT PLANNED` → стало `DEFER`
  Теряется: Не сама развилка A2/A4 — она надёжно живёт в docs/reality-check.md независимо от issue. Теряется правильный статус в словаре действий владелицы: закрыв как "CLOSE AS NOT PLANNED" вместо "DEFER", ледгер утверждает, что задачу решили не делать, хотя закрывающий комментарий сам называет точное условие возвращения (rice.profileId в Core V2 после alpha) — то есть по факту это отложенная, а не отклонённая задача. У "отложено" есть встроенный крючок на пересмотр по условию; у "не планируется" его нет, и без issue, который кто-то вручную вспомнит, идея о втором профиле рисовой постели (валик у ближнего края) рискует не всплыть сама, когда rice.profileId реально появится в контракте. Дополнительно теряется явная пометка contractDependent — условие возвращения жёстко привязано к полю rice.profileId из ADR-001-core-v2-scope.md, который сейчас под ревью и может измениться.

## Полная опись — все 61

| # | Действие | Приоритет | Стабильность тела | Защита | Следующий шаг / условие |
|---|---|---|---|---|---|
| #165 Кольцо: длина листа не сохраняется по построению — слои риса | KEEP ACTIVE | P0 | устарело + контракт V2 |  | Перевести три симптома тела задачи в ожидаемые поля FixtureReport F01 (coveredLengthMm, uncoveredLengthMm, phantomLengthMm, uMin/uMax, seam) и в mutation test «заменить параметризацию по дуге на пр... |
| #163 Урамаки: необъяснённый сдвиг диаметра 2,3 мм стоит в канале  | DEFER | P2 | устарело + контракт V2 | подтверждено | Расфиксировать немедленно, если канал «СДВИГ ⌀» снова напечатает необъяснённую строку на любой базе, или когда урамаки получит собственную топологию в V2. Точка отсчёта для бисекции сохранена в зад... |
| #155 Декоративный ролл собирают из деталей: пять мини-роллов → цв | DEFER | P2 | контракт V2 | подтверждено | Расфиксировать, когда Core V2 alpha пройдёт F01–F06 и вложенная скрутка («ролл внутри ролла») получит собственный ADR. Дизайн многошагового пазла от геометрии не зависит и может идти параллельно в ... |
| #154 Направление: симулятор первым, пазл на нём — условия останов | DEFER | P1 | устарело + контракт V2 | подтверждено | Расфиксировать, когда Core V2 alpha пройдёт F01–F06 и пазл будет переведён на общий kernel (шаг 6 плана миграции ADR-001); тогда переписать три раздела: критерием станет прохождение fixtures и совп... |
| #145 Вмещает ли модель ВСЕ виды маки: тюмаки нет вовсе, урамаки — | SUPERSEDE | none | устарело + контракт V2 | подтверждено | Заменяется ADR-001 (раздел «V2 пока не моделирует» + таблица «Поведение вне модели»); кадзари — #155, трекер полноты ряда — #154 §3. |
| #144 Версионность у HTML-страниц: без маркера правка вёрстки не д | KEEP ACTIVE | P2 | устарело |  | Два шага в `tools/end-of-day.sh`: (1) выводить список страниц из ссылок в `docs/index.html` и корневом `index.html`, а не держать heredoc-таблицу руками; (2) поднять проверку с «маркер есть» до «ма... |
| #140 Ярусы в пазле: многослойность разрешена источниками, и прави | KEEP ACTIVE | P2 |  | ИСПРАВЛЕНО (было DEFER) | Расфиксировать, когда решена продуктовая развилка #139 (показать зону подворота или дать откликающееся) либо открыта веха «Оболочка и режимы». Первый шаг после этого — вывести несущую способность и... |
| #139 Положение группы начинок на листе не влияет на срез: у кольц | KEEP ACTIVE | P2 | устарело + контракт V2 | ИСПРАВЛЕНО (было SUPERSEDE) | Замена: ADR-001, инвариант 5 (ограниченная область применимости) и fixtures F03/F04 — closure window становится явной, возвращаемой в отчёте границей модели; продуктовая половина уходит в #140 и в ... |
| #138 Рис — это «всё остальное», а не вещество: нет зазоров у стен | KEEP ACTIVE | P0 | устарело + контракт V2 |  | До первого PR Core V2 внести правку в спецификацию карты материалов: рис — явный материал со своей границей, а не ветка «всё остальное». Подать её замечанием к ADR-001 в идущий сейчас адверсарный р... |
| #137 Два пути: игра с заучиваемыми правилами и симулятор руки — н | SUPERSEDE | P2 | устарело + контракт V2 | подтверждено | Замещается связкой `docs/decisions/ADR-001-core-v2-scope.md` (инвариант 6 + не-цель «реальную деформацию, разрушение, скольжение и стохастический результат руки») и `docs/decisions/ADR-001-erratum-... |
| #136 Хвост триажа: девять развилок, которые нельзя закрыть правко | KEEP ACTIVE | P2 | устарело |  | Один проход: вычеркнуть из тела сделанное и оставить то, что проверено живым — `fruit.ingredients` зовёт снятые `pinkcream` и `choco` (`catalog.js:191`), у режима «Пазл» нет выжимки в витрине (`mec... |
| #134 Начинку двигаешь — она стоит, потом прыгает к корке и теряет | NEEDS OWNER DECISION | P1 | устарело + контракт V2 |  | Вопрос владельцу одной строкой: растягивается ли КУСОК начинки вместе с изгибающимся листом (гнётся композит) или свою длину хранит только носитель, а кусок хранит длину и площадь и проскальзывает ... |
| #128 Ветка worktree-line-render: непиксельная отрисовка линиями,  | DEFER | none |  | подтверждено | Расформировать, когда владелец выберет вид игры (#111): линейная отрисовка идёт третьим кандидатом наравне с пиксельным и гладким. До тех пор — один дешёвый шаг сейчас: внести ветку в список «извес... |
| #125 Фаска называется физическим размером, а на деле это 25 % выс | SUPERSEDE | P2 | устарело + контракт V2 | подтверждено | Замещается записью в `docs/domain-contract.md` §6 п. 4 (фаска обнулена, скругление, если нужно, место ему в намотке) и не-целью ADR-001 «реальную деформацию, разрушение, скольжение» — то есть обжим... |
| #113 Подворот садится на 14–23 % листа вместо 88–95 % — и в игре, | KEEP ACTIVE | P1 | устарело + контракт V2 |  | Переписать постановку в теле: назвать проверяемую пару величин (sFold против периметра первого витка вокруг ядра, норма — своя на каждую базу: хосомаки 90–96 %, тюмаки ~69, футомаки 70–75) и переда... |
| #112 Прогрессия пазла: от одного ингредиента с духом до мозаики и | DEFER | P1 | устарело | подтверждено | Расфиксировать, когда Core V2 пройдёт F01–F06 и очередь дойдёт до миграции пазла (ADR-001, план п. 6), либо когда владелец разморозит веху «Оболочка и режимы». До того — одна безопасная правка тела... |
| #111 Состав игры: японская тематика, обёртки, нори не крутить спи | CLOSE AS NOT PLANNED | none | устарело | подтверждено | Содержимое живёт в docs/ingredients.md (5-бис), docs/mechanics.md §2, sim/KINEMATICS.md и в #83; отдельной задачей возвращается только свойство обёртки «может ли идти витками». |
| #110 Развилка: чем игрок раскладывает — кисть, поштучно или смеша | NEEDS OWNER DECISION | P2 | устарело |  | Вопрос владельцу одной строкой: кисть для всего, кисть только для риса и красок, или кисти нет вовсе — и если куски кладутся поштучно, чем их двигать на телефоне, где кусок шириной 9 px? Отдельно и... |
| #109 Аудит честности чисел: 11 помечено неверно, ~65 догадок без  | KEEP ACTIVE | P1 | устарело |  | Сначала подмножество, которое войдёт в F01/F02 Core V2: длина листа 105 мм, профиль рисовой постели хосомаки, габариты огурца (14 × 9,9 мм), `R0`, `kappa`, `beta`, `tuckMin` — пометить статус до то... |
| #103 Брейншторм: карта развилок «зачем крутить второй ролл» | DEFER | P2 | устарело | подтверждено | Расснять, когда по #79 появятся числа — или когда будет записано, что замеры 1–5 сняты как неактуальные. До этого развилка 1 решается догадками, а именно от этого задача и предостерегает. При рассн... |
| #94 Живой срез во время скрутки: показывать, как элементы встают | DEFER | P2 | устарело | подтверждено | Расснять, когда откроется веха «Оболочка и режимы» и будет решаться #139: там #94 сравнивается с #140 и с «показать зону подворота» как три варианта одного ответа. Первым проверять самый дешёвый ви... |
| #93 Скрутка: снизу вверх зафиксировано, остаётся выбор конца оси | KEEP ACTIVE | P1 | контракт V2 |  | Подать в идущее адверсарное ревью ADR-001 требование: конец оси скрутки обязан быть частью сериализуемого рецепта — либо полем `RecipeV2`, либо явной нормализацией u при компиляции рецепта (u → L −... |
| #92 Grain-check: различимость минимальных элементов формы и цвет | KEEP ACTIVE | P2 | устарело |  | Реализовать пункт 2: полоса краски шириной в зерно (GRAIN) через настоящий spreadColor против соседнего белого, цветовое расстояние выше порога, доказать красной при включении сглаживания цвета «за... |
| #90 Чего лаборатория не даст никогда: границы прибора, записанны | KEEP ACTIVE | P2 | устарело |  | Перенести текст отдельным разделом «Границы лаборатории» в docs/simulation-research.md; поправить в нём строку про приёмку (теперь F01–F06 из core-v2-fixtures.md, а не только ?check и #73); переста... |
| #85 Пазл: цель касания падает до 37–43 px при собственной норме  | SUPERSEDE | P2 | контракт V2 | подтверждено | Заменяется тем же ADR-001 инв. 6 через #83. Требование «цель касания ≥ 44 px во всех режимах» переносится в приёмку миграции пазла на Core V2 и в проверки #73; отдельно остаётся заметить молчаливый... |
| #83 Пазл живёт по своей физике листа: два решения проекта не све | SUPERSEDE | P1 | устарело + контракт V2 | подтверждено | Заменяется ADR-001 (инв. 1, 2, 6) и планом миграции п. 6 «после принятия V2 мигрировать один базовый ручной сценарий, затем puzzle». Перебалансировка уровней — следствие ADR, живёт в #112; сторож н... |
| #82 Каталог оптимизаций горячего пути materialMap → materialAt | CLOSE AS NOT PLANNED | none |  | подтверждено | Список кандидатов остаётся в `docs/archive/review-validation.md` §«Оптимизации». Вернуться, если замер на живом устройстве (#77) покажет просадку кадра и профиль назовёт виновником `materialAt`/`ma... |
| #80 Inverse design: ЗАМОРОЖЕНО 01.09 — спецификация писалась до  | DEFER | P2 | устарело + контракт V2 | подтверждено | Размораживать, когда Core V2 прошла F01–F06 и подключена адаптером (ADR-001, раздел «V2 пока не моделирует», и план миграции пп. 2–4). Первый шаг ПОСЛЕ разморозки — переписать `docs/inverse-design.... |
| #79 Добрать числа замеров 1–5 по HYPOTHESIS.md на следующих пока | KEEP ACTIVE | P2 | устарело |  | Записать в RESULT.md разбор по замерам: 1 — снимается в изменённых условиях и с прежними порогами не сравнивается; 3 — откладывается до починки #165 + #134, иначе меряет дефект, а не гипотезу; 2, 4... |
| #77 Проверка вёрстки на живых устройствах | DEFER | P2 | устарело | подтверждено | Расснять перед ближайшим показом живым людям по #79: сломанная раскладка на чужом телефоне обесценит замер 1 сильнее, чем вся полировка вместе взятая. Второе условие — открытие вехи «Оболочка и реж... |
| #76 Контраст: три решения владельца по светлым обёрткам и доске | NEEDS EVIDENCE | P2 | устарело |  | Замерить контраст обёртки egg (#e8b551) с той подложкой, на которой реально лежит срез, у узумаки и у фруктовой базы, и контраст общей доски тарелки из шести с фоном на телефоне; порог — 3:1 для не... |
| #75 Ёкаи как тематическая оболочка обратной связи | MERGE INTO PARENT | none |  | подтверждено | Родитель — #103, развилка 2. Содержательное сравнение «ёкаи против маскота» и обе предупредительные пометки переносятся в комментарий #103 перед закрытием. |
| #74 Роль — свойство укладки, а не вещества: surface/embedded/sta | KEEP ACTIVE | P2 | устарело | ИСПРАВЛЕНО (было DEFER) | Расфиксировать, когда V2 alpha зелена (F01–F06) и открывается отдельный ADR на не-прямоугольные патчи и топологию слоёв — там же живут мембрана/масса и «рис как оболочка» (урамаки), которые ADR-001... |
| #73 Fixtures F01–F08 и девять инвариантов для регрессии | KEEP ACTIVE | P2 | устарело + контракт V2 | ИСПРАВЛЕНО (было SUPERSEDE) | Замена — `docs/decisions/core-v2-fixtures.md` (F01–F06 + mutation tests) и восемь инвариантов ADR-001. До закрытия обязательно развести имена: «F01–F08» теперь значит два разных набора, legacy-набо... |
| #72 Выход из монолита: миграция по docs/domain-contract.md §11 | SUPERSEDE | P2 | устарело + контракт V2 | подтверждено | Замена — план миграции ADR-001 (шаги 1–6) плюс `core-v2-legacy-boundary.md`; шесть находок аудита (GRAIN_C, touchModel, мёртвые geometry()/covers(), три копии шва, фасад без потребителя, ES-модули)... |
| #71 Записать границу модели: рис в слое — два-три зерна, сплошна | MERGE INTO PARENT | none |  | подтверждено | Родитель — #90. Строку про два-три зерна внести в его раздел о границах и перенести вместе с ним в docs (см. предложение по #90). |
| #70 Эталоны различимости форм из shapes.md → play/checks.js | MERGE INTO PARENT | none | устарело + контракт V2 | ИСПРАВЛЕНО (было MERGE INTO PARENT) | Родитель — #92. Туда же переносится найденное здесь требование: перед закреплением порога различимости форм его надо перезамерить на текущей модели. |
| #68 Симуляция в игре (XPBD/XPBI) как кандидат v1 — решить по ито | DEFER | P2 |  | подтверждено | Разбудить, когда #60 доведён до калибровки и есть числа, чем встроенная симуляция отличалась бы от феноменологической модели (RESULT.md 25.08: условие — разброс от руки игрока, не от процессора). Д... |
| #66 Пасты и присыпки как классы материала (стенд + референс) | DEFER | P2 | устарело + контракт V2 | подтверждено | Разморозить, когда Core V2 alpha прошла F01–F06 и появился отдельный ADR о свойствах материала. Формулировать не классом, а свойством каталога с выводимым наверху словарём — этого требует §5.5 doma... |
| #62 Стенд: следы прижима формы — E, F, H из shapes.md | MERGE INTO PARENT | none | устарело | подтверждено | Родитель — #138 (рис как вещество) для пункта F; H отдельной строкой переезжает в #137 (два слоя правил). |
| #60 Референс sim: весь пакет работ — сначала подворот, потом всё | DEFER | P2 | устарело | подтверждено | Разморозить, когда Core V2 зелён на F01–F06 и появится вопрос, переживший фильтр #90. Входить с пункта 1 (floor по ppc), а НЕ с пункта 0 — он закрыт. Результаты лаборатории при разморозке идут в V2... |
| #58 Развилка модуля нори: мембранная жёсткость E·w или «в шесть  | MERGE INTO PARENT | none | устарело | подтверждено | Родитель — #60, пункт 2. Просьбу к владельцу про полный текст Jung 2022 перенести туда же одной строкой. |
| #57 Правило раскладки начинки как подсказка в UI | KEEP ACTIVE | P0 | контракт V2 |  | Внести четыре источника (начинать не ближе 20 мм от ближней кромки, не заводить за середину листа, твёрдое-сердечник ближе к себе) в определение closure window Core V2 — как кулинарный коридор для ... |
| #56 Валик у ближнего края (手前に少し山) как опция | DEFER | none |  | ИСПРАВЛЕНО (было CLOSE AS NOT PLANNED) | Вернуть, когда профиль рисовой постели станет настоящим параметром с несколькими вариантами (`rice.profileId` в Core V2, после V2 alpha) — тогда валик заводится вторым профилем вместе с fixture, а ... |
| #55 Переполнение витка → трещина обёртки как узор | DEFER | P2 | устарело | подтверждено | Разбудить, когда появится многовитковая намотка (#142) ИЛИ на текущей геометрии найдётся раскладка со stackTopAt ≥ 2,5 T. При разморозке первым делом перепроверить само число 2,5 T — оно стоит в do... |
| #54 Имя игры: проверка товарных знаков + консультация IP-юриста  | DEFER | P2 |  | подтверждено | Расчехлять по первому из двух событий: (а) имя фиксируется для публичного употребления — витрина магазина, логотип, домен, обложка, — либо маскот из #75 переходит из идеи в рисунок (тогда в проверк... |
| #53 Маски: произвольные формы начинок — рисование на листе | DEFER | P2 | устарело + контракт V2 | подтверждено | Разморозить, когда Core V2 alpha прошла F01–F06 и отдельный ADR решает представление патча (прямоугольник против маски). Тогда же поднять два предусловия из docs/kazari-plan.md, которые от смены яд... |
| #52 Живой тест жеста скрутки (тяга циновки) | DEFER | P2 |  | подтверждено | Разбудить к ближайшему замеру на живых людях (#79) или показу тестерам — там за один заход проверить и тягу циновки, и задержку жест→звук. Отдельного захода не планировать. |
| #51 Вид сбоку в мм: предсказание сжатия и предупреждение о разры | DEFER | P2 | устарело | подтверждено | Разбудить, когда Core V2 отдаёт measurements через адаптер (после F01–F06) и открывается веха «Оболочка и режимы»: тогда вид сбоку — чтение готового профиля, а не новая модель. Порог разрыва к тому... |
| #48 Мини-ролл (пруток) как элемент узора | MERGE INTO PARENT | none | контракт V2 | подтверждено | Родитель — #155 (декоративный ролл собирают из деталей); числа примитива переносятся туда закрывающим комментарием. |
| #46 Выбор нарезки игроком | DEFER | P1 | устарело | ИСПРАВЛЕНО (было MERGE INTO PARENT) | Родитель — #110 «раскладка кистью»: нарезка входит в кисть и задаёт шаг кусков вдоль штриха. |
| #45 Тип ролла как следствие раскладки, а не выбор игрока | DEFER | P2 | устарело | подтверждено | Расфиксировать, когда откроется веха про смысл и режимы (слова владельца 31.08). Первым делом — перемерить порог разрыва: старая таблица считала обхват как π·⌀ и потому назвала шесть начинок вместо... |
| #38 Порог пазла PASS=0,72 ниже замеренной полосы «мимо» | DEFER | P2 |  | подтверждено | Расфиксировать, когда пазл вернётся в работу — миграция пазла на Core V2 (ADR-001, план п. 6) или разморозка вехи «Оболочка и режимы». Тогда: поднять PASS до 0,82 одной строкой (play/modes/puzzle.j... |
| #36 Довалидировать хвост ревью 26.08: валидация входа, порядок п | KEEP ACTIVE | P2 | устарело |  | Один коммит: проверять длину `data.h` в `decodePuzzle` и звать `validateRecipe` на боевом пути (`state.js` берёт `m.lists` как есть). Критерий готовности — битая пазл-ссылка даёт диагностику, а не ... |
| #34 Плотность скрутки как жест НА СРЕЗЕ: крутить узор после наре | DEFER | P2 |  | подтверждено | Расфиксировать, когда появится отдельный ADR про режим руки (erratum-001: «Режим руки вводится только отдельным ADR после V2 alpha»). Тогда вариант А — работа агента (кнопка «Перекрутить», `S.mode ... |
| #24 Рис заканчивается зёрнами, а не линией: все четыре кромки гр | KEEP ACTIVE | P2 | устарело | ИСПРАВЛЕНО (было DEFER) | Расфиксировать, когда V2 alpha подключена к debug-рендеру и полировка отрисовки среза возвращается в работу — либо раньше, если сторож Т покажет, что рваность плоских кромок пропала (edgeRagAmp() =... |
| #21 Панель иконок: семь эмодзи без подписей, и значок базы ещё и | KEEP ACTIVE | P2 | устарело |  | Подписать кнопки верхней панели так же, как подписаны чипы внизу, и развести круглые глифы между собой; вторым заходом — дорисовать недостающие спрайты начинок (в play/assets/icons/ пятнадцать файл... |
| #18 Нет зелёного перца и кунжута — оба видны на фото настоящего  | MERGE INTO PARENT | none | устарело | подтверждено | Родитель — #66 («Пасты и присыпки как классы материала»): кунжут заводится там как отсутствующая присыпка, вместе с обсыпкой снаружи. |
| #17 Рис как материал рисования: игрок не задаёт ни общую, ни лок | KEEP ACTIVE | P2 | устарело | ИСПРАВЛЕНО (было DEFER) | Расфиксировать, когда решена кисть (#110) — она задаёт механику ввода — и когда контракт научится выражать локальный профиль риса: сейчас в RecipeV2 поле `rice: { profileId }`, то есть один именова... |
| #10 Разобрать каждую начинку отдельно + профиль сечения вдоль ос | KEEP ACTIVE | P2 | устарело | ИСПРАВЛЕНО (было DEFER) | Расфиксировать, когда очередной базе или fixture понадобится начинка, которой нет в каталоге, — либо когда открывается ADR на не-прямоугольные патчи (мембрана/масса), общий с #74. До тех пор источн... |
| #3 Урамаки: правила не написаны, числа в коде — заглушка | MERGE INTO PARENT | none | устарело | ИСПРАВЛЕНО (было MERGE INTO PARENT) | Родитель — #66 (пасты и присыпки как классы материала); кунжут туда же из #18. |